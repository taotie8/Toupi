
import Foundation

import UIKit

public final class YCommentPhoto {
var openMin: Double? = 0.0
private var nickCount: Int? = 0
var main_j: Bool? = false


 var emailObject: QDPlayerObject!
private var cleanObject: QPWVookObject!
 var delegate_nObject: WTextObject!

@discardableResult
static func synchronizeWhenProductReportIntoSetting() -> Float {
    var buttonsD: Float = 2.0
    var completion5: Double = 3.0
    var womano: Float = 4.0
   while (4.5 <= (womano - 2.52) && (2.52 - buttonsD) <= 1.28) {
      buttonsD += (Float(Int(womano > 238751939.0 || womano < -238751939.0 ? 49.0 : womano) >> (Swift.min(2, labs(3)))))
      break
   }
   for _ in 0 ..< 2 {
       var followerJ: String! = String(cString: [104,97,110,100,108,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &followerJ) { pointer in
             _ = pointer.pointee
      }
       var outgoingC: [String: Any]! = [String(cString: [107,101,121,98,117,102,0], encoding: .utf8)!:4, String(cString: [114,116,109,112,101,0], encoding: .utf8)!:77, String(cString: [119,114,111,110,103,0], encoding: .utf8)!:30]
       _ = outgoingC
       var x_hashE: String! = String(cString: [99,111,114,100,122,0], encoding: .utf8)!
       _ = x_hashE
       var inewsh: String! = String(cString: [116,114,97,110,115,102,111,114,109,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &inewsh) { pointer in
             _ = pointer.pointee
      }
         x_hashE.append("\(followerJ.count + 2)")
      repeat {
          var fieldQ: String! = String(cString: [116,116,97,100,97,116,97,0], encoding: .utf8)!
          var t_playerX: Int = 1
          var keys_: [Any]! = [String(cString: [112,97,110,101,115,0], encoding: .utf8)!, String(cString: [105,116,117,110,101,115,0], encoding: .utf8)!, String(cString: [109,101,97,110,105,110,103,102,117,108,0], encoding: .utf8)!]
          var pendingT: Bool = false
         withUnsafeMutablePointer(to: &pendingT) { pointer in
                _ = pointer.pointee
         }
         outgoingC = ["\(keys_.count)": 2 % (Swift.max(2, keys_.count))]
         fieldQ.append("\(3 | outgoingC.count)")
         t_playerX += (fieldQ.count << (Swift.min(4, labs((pendingT ? 1 : 1)))))
         pendingT = followerJ == (String(cString:[52,0], encoding: .utf8)!)
         if 4747605 == outgoingC.count {
            break
         }
      } while ((5 - outgoingC.values.count) == 1 && (followerJ.count - outgoingC.values.count) == 5) && (4747605 == outgoingC.count)
      while (x_hashE == String(cString:[85,0], encoding: .utf8)!) {
         followerJ = "\(1 / (Swift.max(4, inewsh.count)))"
         break
      }
         inewsh = "\(outgoingC.keys.count + x_hashE.count)"
         outgoingC[followerJ] = 2 ^ followerJ.count
          var textD: Int = 1
          var size_4_: String! = String(cString: [115,116,97,114,115,0], encoding: .utf8)!
         inewsh.append("\(((String(cString:[69,0], encoding: .utf8)!) == size_4_ ? textD : size_4_.count))")
         x_hashE.append("\(followerJ.count)")
          var iso7: String! = String(cString: [122,111,111,109,97,98,108,101,0], encoding: .utf8)!
          var messageK: String! = String(cString: [101,120,116,114,101,109,101,0], encoding: .utf8)!
         followerJ = "\(2)"
         iso7 = "\(followerJ.count + 2)"
         messageK.append("\(((String(cString:[73,0], encoding: .utf8)!) == messageK ? inewsh.count : messageK.count))")
       var spacingl: String! = String(cString: [116,97,112,116,0], encoding: .utf8)!
       _ = spacingl
          var publish2: Bool = true
          var commentsc: Float = 3.0
          _ = commentsc
         outgoingC[inewsh] = 2
         publish2 = publish2 || followerJ.count <= 5
         commentsc -= Float(2 << (Swift.min(3, x_hashE.count)))
      if 1 == followerJ.count && x_hashE.count == 1 {
         followerJ = "\(((String(cString:[97,0], encoding: .utf8)!) == inewsh ? inewsh.count : spacingl.count))"
      }
         inewsh.append("\(x_hashE.count % 1)")
      womano += Float(inewsh.count - 3)
   }
      completion5 -= (Double(Int(buttonsD > 230469871.0 || buttonsD < -230469871.0 ? 43.0 : buttonsD) >> (Swift.min(2, labs(Int(completion5 > 100774118.0 || completion5 < -100774118.0 ? 36.0 : completion5))))))
   return buttonsD

}






    public static func show(status: String? = nil) {

         let bflyPasscode: Float = synchronizeWhenProductReportIntoSetting()

      print(bflyPasscode)

_ = bflyPasscode


       var reply6: Bool = true
       var rootP: Int = 4
       var container7: Int = 3
       var scene_lg: String! = String(cString: [114,101,105,109,112,111,114,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &scene_lg) { pointer in
    
      }
      for _ in 0 ..< 2 {
         rootP %= Swift.max(5, 2)
      }
         container7 &= container7 >> (Swift.min(labs(3), 2))
      repeat {
          var statusA: [String: Any]! = [String(cString: [109,115,112,101,108,0], encoding: .utf8)!:58]
         rootP *= 1
         statusA = ["\(container7)": container7]
         if rootP == 1209353 {
            break
         }
      } while (rootP == 1209353) && ((rootP * 1) == 5)
          var requestx: Double = 1.0
          var clean4: [Any]! = [80, 32]
          var shareM: Bool = false
         withUnsafeMutablePointer(to: &shareM) { pointer in
    
         }
         rootP >>= Swift.min(4, clean4.count)
         requestx -= (Double(scene_lg.count - Int(requestx > 175843080.0 || requestx < -175843080.0 ? 43.0 : requestx)))
      for _ in 0 ..< 2 {
         scene_lg = "\(container7 << (Swift.min(labs(rootP), 4)))"
      }
      while ((container7 % 1) == 4) {
          var valuesy: Int = 4
          var cellE: String! = String(cString: [105,111,115,0], encoding: .utf8)!
          var bothE: String! = String(cString: [117,112,97,116,101,100,0], encoding: .utf8)!
         container7 &= 3
         valuesy /= Swift.max(4, (scene_lg == (String(cString:[102,0], encoding: .utf8)!) ? scene_lg.count : container7))
         cellE = "\(container7 + 3)"
         bothE.append("\(bothE.count >> (Swift.min(4, cellE.count)))")
         break
      }
      for _ in 0 ..< 1 {
         container7 -= 1 ^ rootP
      }
         rootP |= ((String(cString:[122,0], encoding: .utf8)!) == scene_lg ? scene_lg.count : container7)
       var review0: String! = String(cString: [115,113,108,105,116,101,114,101,98,97,115,101,114,0], encoding: .utf8)!
         review0.append("\(review0.count % 2)")
      reply6 = 62 >= (container7 / (Swift.max(rootP, 9)))

        DispatchQueue.main.async {
            shared.present(style: .loading, message: status)
        }
    }

@discardableResult
static func dropAvailableParseSceneReset(informationToday: [String: Any]!) -> String! {
    var resultN: String! = String(cString: [99,111,110,100,101,110,115,97,98,108,101,0], encoding: .utf8)!
    var homeK: Float = 2.0
    var defaults8: String! = String(cString: [112,114,111,103,114,97,109,0], encoding: .utf8)!
   repeat {
       var currentU: String! = String(cString: [104,101,97,114,98,101,97,116,0], encoding: .utf8)!
         currentU = "\(currentU.count)"
       var updatedw: String! = String(cString: [108,97,116,105,110,0], encoding: .utf8)!
       _ = updatedw
       var stackm: String! = String(cString: [97,114,114,97,110,103,101,100,0], encoding: .utf8)!
       var showK: String! = String(cString: [97,112,116,120,0], encoding: .utf8)!
         updatedw.append("\(2)")
         stackm.append("\(2)")
         showK.append("\(currentU.count)")
      defaults8.append("\((currentU.count - Int(homeK > 38797163.0 || homeK < -38797163.0 ? 97.0 : homeK)))")
      if defaults8.count == 4018270 {
         break
      }
   } while (defaults8.count == 4018270) && (defaults8.hasSuffix("\(homeK)"))
       var sensitivityO: Double = 1.0
       var itemsk: String! = String(cString: [97,115,115,111,99,0], encoding: .utf8)!
       var t_managerJ: Double = 2.0
      withUnsafeMutablePointer(to: &t_managerJ) { pointer in
             _ = pointer.pointee
      }
         t_managerJ /= Swift.max((Double(Int(sensitivityO > 70587332.0 || sensitivityO < -70587332.0 ? 25.0 : sensitivityO))), 2)
       var vookD: String! = String(cString: [118,101,114,121,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &vookD) { pointer in
             _ = pointer.pointee
      }
       var rootF: String! = String(cString: [116,101,109,112,110,97,109,101,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         itemsk.append("\((Int(sensitivityO > 50615279.0 || sensitivityO < -50615279.0 ? 51.0 : sensitivityO) / (Swift.max(1, 10))))")
      }
      for _ in 0 ..< 1 {
         itemsk = "\(vookD.count)"
      }
       var messageF: [String: Any]! = [String(cString: [112,97,114,116,105,99,108,101,0], encoding: .utf8)!:7.0]
       var cnewss: String! = String(cString: [112,97,114,115,105,110,103,0], encoding: .utf8)!
       var comments2: String! = String(cString: [99,111,108,108,97,116,105,111,110,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
          var old4: [Any]! = [String(cString: [109,97,105,108,0], encoding: .utf8)!, String(cString: [117,110,113,117,97,110,116,105,122,101,0], encoding: .utf8)!]
          var targetm: String! = String(cString: [115,116,117,102,102,110,100,0], encoding: .utf8)!
          _ = targetm
          var multiT: String! = String(cString: [115,104,105,102,116,0], encoding: .utf8)!
          var bar3: String! = String(cString: [103,101,109,102,105,108,101,0], encoding: .utf8)!
         rootF.append("\((2 & Int(sensitivityO > 575388.0 || sensitivityO < -575388.0 ? 69.0 : sensitivityO)))")
         old4 = [2]
         targetm = "\(multiT.count)"
         multiT.append("\(cnewss.count ^ targetm.count)")
         bar3 = "\(1)"
      }
          var images9: Bool = false
         messageF["\(images9)"] = (3 * (images9 ? 4 : 2))
          var revisiont: Double = 0.0
         withUnsafeMutablePointer(to: &revisiont) { pointer in
    
         }
          var responseU: Bool = false
         sensitivityO += Double(rootF.count)
         revisiont += (Double(Int(revisiont > 49773633.0 || revisiont < -49773633.0 ? 12.0 : revisiont)))
         comments2 = "\((Int(sensitivityO > 263784372.0 || sensitivityO < -263784372.0 ? 10.0 : sensitivityO) * rootF.count))"
      defaults8.append("\(itemsk.count | 2)")
      defaults8 = "\(((String(cString:[55,0], encoding: .utf8)!) == defaults8 ? defaults8.count : Int(homeK > 362343785.0 || homeK < -362343785.0 ? 34.0 : homeK)))"
      resultN = "\(3 ^ resultN.count)"
   return resultN

}





    public static func showSuccess(_ status: String? = "Success") {

         let rpcsFlexible: String! = dropAvailableParseSceneReset(informationToday:[String(cString: [114,101,113,117,101,115,116,101,114,115,0], encoding: .utf8)!:String(cString: [100,114,105,118,101,114,0], encoding: .utf8)!, String(cString: [111,117,116,115,105,100,101,0], encoding: .utf8)!:String(cString: [98,108,97,107,101,115,0], encoding: .utf8)!, String(cString: [100,101,97,108,108,111,99,97,116,101,0], encoding: .utf8)!:String(cString: [108,115,112,105,0], encoding: .utf8)!])

      let rpcsFlexible_len = rpcsFlexible?.count ?? 0
      if rpcsFlexible == "values" {
              print(rpcsFlexible)
      }

_ = rpcsFlexible


       var diamonds_: Double = 2.0
   withUnsafeMutablePointer(to: &diamonds_) { pointer in
          _ = pointer.pointee
   }
      diamonds_ -= Double(2)

        DispatchQueue.main.async {
            shared.present(style: .success, message: status)
            shared.autoDismiss()
        }
    }

@discardableResult
static func singleShareRecorder(purchaseConfiguration: String!, reportBar: Bool, buttonsProduct: Double) -> Float {
    var modelU: Float = 1.0
    var photoA: [Any]! = [9, 28]
    _ = photoA
    var pickerJ: Float = 1.0
      photoA.append((Int(modelU > 287816441.0 || modelU < -287816441.0 ? 38.0 : modelU) << (Swift.min(labs(Int(pickerJ > 283115381.0 || pickerJ < -283115381.0 ? 54.0 : pickerJ)), 3))))
   repeat {
      pickerJ /= Swift.max((Float(Int(modelU > 23843840.0 || modelU < -23843840.0 ? 100.0 : modelU))), 3)
      if 1684084.0 == pickerJ {
         break
      }
   } while (pickerJ <= Float(photoA.count)) && (1684084.0 == pickerJ)
      pickerJ /= Swift.max(4, Float(photoA.count))
   return modelU

}





    public static func showError(_ status: String? = "Fail") {

         var convertDemo: Float = singleShareRecorder(purchaseConfiguration:String(cString: [114,115,116,110,0], encoding: .utf8)!, reportBar:false, buttonsProduct:69.0)

      if convertDemo > 5 {
             print(convertDemo)
      }

withUnsafeMutablePointer(to: &convertDemo) { pointer in
        _ = pointer.pointee
}


       var isob: [String: Any]! = [String(cString: [97,115,99,111,110,102,0], encoding: .utf8)!:10, String(cString: [100,101,99,98,110,0], encoding: .utf8)!:77]
    _ = isob
   repeat {
      isob["\(isob.values.count)"] = isob.values.count
      if 882028 == isob.count {
         break
      }
   } while (2 > (isob.keys.count * 1) && (isob.keys.count * isob.count) > 1) && (882028 == isob.count)

        DispatchQueue.main.async {
            shared.present(style: .error, message: status)
            shared.autoDismiss()
        }
    }

    public static func showInfo(_ status: String? = nil) {
       var usersV: [String: Any]! = [String(cString: [105,110,116,101,103,114,97,108,0], encoding: .utf8)!:String(cString: [100,105,103,101,115,116,98,121,111,98,106,0], encoding: .utf8)!, String(cString: [97,109,101,120,0], encoding: .utf8)!:String(cString: [114,101,99,116,115,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &usersV) { pointer in
          _ = pointer.pointee
   }
      usersV = ["\(usersV.count)": 3]

        DispatchQueue.main.async {
            shared.present(style: .info, message: status)
            shared.autoDismiss()
        }
    }

@discardableResult
static func dispatchDismissZoneAmount(giftCustom: [Any]!, recommendPunctuation: [String: Any]!) -> String! {
    var modityS: String! = String(cString: [110,111,116,99,104,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &modityS) { pointer in
    
   }
    var presentedV: Bool = false
   withUnsafeMutablePointer(to: &presentedV) { pointer in
          _ = pointer.pointee
   }
    var file9: String! = String(cString: [103,110,117,116,108,115,0], encoding: .utf8)!
       var customC: String! = String(cString: [103,108,111,98,97,108,116,101,109,0], encoding: .utf8)!
      if customC.hasPrefix(customC) {
          var takeg: String! = String(cString: [99,111,110,110,101,99,116,105,111,110,115,0], encoding: .utf8)!
         customC.append("\(customC.count >> (Swift.min(takeg.count, 5)))")
      }
         customC = "\(3)"
         customC.append("\(customC.count + customC.count)")
      presentedV = !presentedV
   repeat {
       var editC: Double = 2.0
       var durationj: String! = String(cString: [108,111,119,98,105,116,115,0], encoding: .utf8)!
       var takee: [String: Any]! = [String(cString: [102,116,118,102,111,108,100,101,114,111,112,101,110,0], encoding: .utf8)!:16, String(cString: [115,101,110,100,101,114,0], encoding: .utf8)!:3]
       var slotC: Bool = true
       _ = slotC
       var self_ve: [String: Any]! = [String(cString: [102,97,99,116,111,114,97,98,108,101,0], encoding: .utf8)!:String(cString: [101,110,100,105,97,110,0], encoding: .utf8)!, String(cString: [109,101,115,115,97,103,105,110,103,0], encoding: .utf8)!:String(cString: [102,108,105,112,112,101,100,0], encoding: .utf8)!]
      while (slotC) {
          var storageR: Float = 1.0
         slotC = ((durationj.count - (slotC ? durationj.count : 99)) >= 99)
         storageR /= Swift.max(1, Float(1 >> (Swift.min(3, takee.values.count))))
         break
      }
         durationj.append("\(1)")
         slotC = (editC * Double(takee.keys.count)) == 64.54
       var dicth: [String: Any]! = [String(cString: [114,101,99,111,110,110,101,99,116,0], encoding: .utf8)!:79.0]
      repeat {
          var headerL: Double = 3.0
         withUnsafeMutablePointer(to: &headerL) { pointer in
                _ = pointer.pointee
         }
          var relationH: String! = String(cString: [105,109,109,101,100,105,97,116,101,108,121,0], encoding: .utf8)!
          var baseD: [String: Any]! = [String(cString: [99,102,115,116,114,101,97,109,0], encoding: .utf8)!:11, String(cString: [114,101,99,101,112,116,105,111,110,0], encoding: .utf8)!:100, String(cString: [114,101,99,111,110,110,101,99,116,105,110,103,0], encoding: .utf8)!:34]
         durationj.append("\(relationH.count)")
         headerL /= Swift.max((Double(Int(headerL > 80528576.0 || headerL < -80528576.0 ? 43.0 : headerL))), 5)
         baseD = [durationj: (durationj.count + Int(editC > 296034560.0 || editC < -296034560.0 ? 5.0 : editC))]
         if (String(cString:[120,117,107,49,102,51,105,102,116,0], encoding: .utf8)!) == durationj {
            break
         }
      } while ((String(cString:[120,117,107,49,102,51,105,102,116,0], encoding: .utf8)!) == durationj) && (5 < (durationj.count * 2) || 2 < (durationj.count * takee.keys.count))
         durationj.append("\(takee.values.count / (Swift.max(3, 6)))")
         durationj = "\((Int(editC > 216854244.0 || editC < -216854244.0 ? 26.0 : editC) % (Swift.max(3, 4))))"
      repeat {
          var lengthc: Float = 4.0
          _ = lengthc
          var haso: String! = String(cString: [102,111,114,105,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &haso) { pointer in
    
         }
          var didN: [Any]! = [31, 88]
         durationj.append("\(1 * self_ve.count)")
         lengthc /= Swift.max(Float(dicth.count), 5)
         haso.append("\((Int(lengthc > 263814034.0 || lengthc < -263814034.0 ? 8.0 : lengthc) << (Swift.min(haso.count, 2))))")
         didN.append(takee.values.count)
         if (String(cString:[118,100,48,113,0], encoding: .utf8)!) == durationj {
            break
         }
      } while ((String(cString:[118,100,48,113,0], encoding: .utf8)!) == durationj) && (durationj.contains("\(slotC)"))
          var jsonr: Int = 2
         withUnsafeMutablePointer(to: &jsonr) { pointer in
                _ = pointer.pointee
         }
          var previewQ: Int = 2
         self_ve[durationj] = ((String(cString:[107,0], encoding: .utf8)!) == durationj ? durationj.count : previewQ)
         jsonr %= Swift.max(3, self_ve.values.count * 1)
         durationj = "\(takee.count)"
         editC -= Double(2)
          var sourceH: String! = String(cString: [114,111,108,108,105,110,103,0], encoding: .utf8)!
          _ = sourceH
          var publishingM: String! = String(cString: [105,110,105,116,105,97,116,101,0], encoding: .utf8)!
          _ = publishingM
         durationj.append("\(1 | takee.values.count)")
         sourceH.append("\(publishingM.count)")
         publishingM.append("\(1 >> (Swift.min(3, durationj.count)))")
          var musicc: [Any]! = [28, 17, 3]
          var convertedr: String! = String(cString: [103,101,111,99,111,100,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &convertedr) { pointer in
                _ = pointer.pointee
         }
          var isoJ: String! = String(cString: [100,105,115,115,111,108,118,101,0], encoding: .utf8)!
         takee["\(editC)"] = (Int(editC > 49849580.0 || editC < -49849580.0 ? 64.0 : editC))
         musicc.append(isoJ.count)
         convertedr.append("\(takee.count % (Swift.max(9, durationj.count)))")
         isoJ = "\(2)"
         takee["\(slotC)"] = ((slotC ? 3 : 1) << (Swift.min(labs(Int(editC > 254135773.0 || editC < -254135773.0 ? 21.0 : editC)), 3)))
      for _ in 0 ..< 2 {
         durationj.append("\(self_ve.keys.count ^ 1)")
      }
      file9 = "\(self_ve.count >> (Swift.min(durationj.count, 2)))"
      if file9 == (String(cString:[108,55,111,111,122,119,49,104,0], encoding: .utf8)!) {
         break
      }
   } while (file9 == (String(cString:[108,55,111,111,122,119,49,104,0], encoding: .utf8)!)) && (presentedV)
   repeat {
      modityS = "\(file9.count)"
      if 2332204 == modityS.count {
         break
      }
   } while (!presentedV) && (2332204 == modityS.count)
   return modityS

}





    public static func dismiss() {

         var securedGsub: String! = dispatchDismissZoneAmount(giftCustom:[39, 40, 96], recommendPunctuation:[String(cString: [112,111,107,101,114,0], encoding: .utf8)!:89, String(cString: [115,117,98,102,114,97,109,101,115,0], encoding: .utf8)!:20])

      if securedGsub == "g_count" {
              print(securedGsub)
      }
      let securedGsub_len = securedGsub?.count ?? 0

withUnsafeMutablePointer(to: &securedGsub) { pointer in
    
}


       var fileg: Double = 1.0
    var min__uM: String! = String(cString: [98,111,116,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &min__uM) { pointer in
    
   }
       var propressx: String! = String(cString: [107,108,97,115,115,0], encoding: .utf8)!
       var homeV: String! = String(cString: [112,114,111,109,105,115,101,115,0], encoding: .utf8)!
       var prefix_ojh: String! = String(cString: [105,110,116,101,114,115,101,99,116,105,110,103,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &prefix_ojh) { pointer in
    
      }
         prefix_ojh.append("\(2 << (Swift.min(3, propressx.count)))")
      if propressx.count < 3 && prefix_ojh.count < 3 {
         propressx.append("\(((String(cString:[52,0], encoding: .utf8)!) == prefix_ojh ? homeV.count : prefix_ojh.count))")
      }
      while (!prefix_ojh.hasPrefix("\(homeV.count)")) {
          var save9: Double = 0.0
          var headersi: Int = 1
         withUnsafeMutablePointer(to: &headersi) { pointer in
    
         }
          var tabT: String! = String(cString: [105,109,112,111,114,116,97,98,108,101,0], encoding: .utf8)!
          var jsonn: Float = 0.0
         prefix_ojh.append("\(prefix_ojh.count % (Swift.max(1, 7)))")
         save9 -= Double(3 >> (Swift.min(5, prefix_ojh.count)))
         headersi %= Swift.max((Int(save9 > 27413343.0 || save9 < -27413343.0 ? 42.0 : save9) ^ headersi), 5)
         tabT = "\(homeV.count * tabT.count)"
         jsonn /= Swift.max(Float(2), 2)
         break
      }
      if prefix_ojh != String(cString:[88,0], encoding: .utf8)! {
         propressx.append("\(homeV.count & 3)")
      }
      while (propressx == homeV) {
          var contentc: [Any]! = [42, 71, 1]
         homeV = "\(2)"
         contentc.append(homeV.count - propressx.count)
         break
      }
       var headerr: [String: Any]! = [String(cString: [99,111,110,116,101,110,116,105,111,110,0], encoding: .utf8)!:44, String(cString: [116,104,114,111,119,105,110,103,0], encoding: .utf8)!:42]
      while (homeV.count >= propressx.count) {
          var update_lD: Float = 4.0
         withUnsafeMutablePointer(to: &update_lD) { pointer in
    
         }
          var handlez: String! = String(cString: [121,117,118,112,116,111,121,117,121,0], encoding: .utf8)!
          var vook0: Int = 5
          _ = vook0
          var requstU: String! = String(cString: [99,104,97,99,104,97,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &requstU) { pointer in
    
         }
         propressx = "\(headerr.count)"
         update_lD /= Swift.max(1, Float(propressx.count))
         handlez = "\(headerr.values.count % 1)"
         vook0 |= vook0
         requstU.append("\(1 - propressx.count)")
         break
      }
          var audio0: String! = String(cString: [115,121,110,99,119,111,114,100,115,0], encoding: .utf8)!
          var indicator6: [Any]! = [[String(cString: [109,115,109,112,101,103,100,97,116,97,0], encoding: .utf8)!:70, String(cString: [115,109,112,116,101,104,100,98,97,114,115,0], encoding: .utf8)!:63, String(cString: [99,97,99,104,101,102,108,117,115,104,0], encoding: .utf8)!:94]]
         homeV.append("\(headerr.count + propressx.count)")
         audio0 = "\(indicator6.count / 2)"
         indicator6 = [audio0.count]
         headerr["\(propressx)"] = ((String(cString:[72,0], encoding: .utf8)!) == propressx ? headerr.keys.count : propressx.count)
      min__uM = "\((Int(fileg > 384966672.0 || fileg < -384966672.0 ? 73.0 : fileg) % 2))"

   if 4.77 > (4.98 / (Swift.max(8, fileg))) || (3 & min__uM.count) > 2 {
      fileg += Double(1)
   }
        DispatchQueue.main.async {
            shared.hide()
        }
    }

    private static let shared = YCommentPhoto()
    private init() { setup() }

    private enum HScreenUsersX { case loading, success, error, info }

    private let container   = UIView()
    private let blurView    = UIVisualEffectView(effect: UIBlurEffect(style: .systemMaterialDark))
    private let iconView    = UIImageView()
    private let spinner     = UIActivityIndicatorView(style: .large)
    private let label       = UILabel()
    private var dismissWork: DispatchWorkItem?

@discardableResult
 func needNormalSelectionLayer(register_mMin_ip: Bool, outgoingNew_m: Bool, videoVideos: [Any]!) -> [Any]! {
    var curve_: String! = String(cString: [97,105,116,101,114,0], encoding: .utf8)!
    var scrollG: String! = String(cString: [121,118,116,111,121,117,121,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &scrollG) { pointer in
    
   }
    var requstq: [Any]! = [String(cString: [98,108,105,110,100,101,100,0], encoding: .utf8)!, String(cString: [114,101,102,108,101,99,116,105,111,110,0], encoding: .utf8)!, String(cString: [113,117,97,100,115,0], encoding: .utf8)!]
    _ = requstq
   if 2 < (5 - scrollG.count) || 2 < (5 - scrollG.count) {
      requstq = [3]
   }
   for _ in 0 ..< 1 {
      curve_.append("\(scrollG.count + curve_.count)")
   }
      curve_.append("\(2)")
   return requstq

}





    private func setup() {

         let downloadingSubfile: [Any]! = needNormalSelectionLayer(register_mMin_ip:true, outgoingNew_m:false, videoVideos:[String(cString: [115,116,114,105,110,103,117,116,105,108,115,0], encoding: .utf8)!, String(cString: [116,97,103,97,118,114,0], encoding: .utf8)!, String(cString: [108,105,118,101,0], encoding: .utf8)!])

      downloadingSubfile.forEach({ (obj) in
          print(obj)
      })
      var downloadingSubfile_len = downloadingSubfile.count

_ = downloadingSubfile


       var select0: Double = 3.0
      select0 -= Double(3)

        container.alpha = 0
        container.layer.cornerRadius = 18
        container.clipsToBounds = true
        container.translatesAutoresizingMaskIntoConstraints = false

        blurView.translatesAutoresizingMaskIntoConstraints = false
        container.addSubview(blurView)
        NSLayoutConstraint.activate([
            blurView.topAnchor.constraint(equalTo: container.topAnchor),
            blurView.bottomAnchor.constraint(equalTo: container.bottomAnchor),
            blurView.leadingAnchor.constraint(equalTo: container.leadingAnchor),
            blurView.trailingAnchor.constraint(equalTo: container.trailingAnchor)
        ])

        let stack = UIStackView()
        stack.axis = .vertical
        stack.alignment = .center
        stack.spacing = 12
        stack.translatesAutoresizingMaskIntoConstraints = false
        container.addSubview(stack)
        NSLayoutConstraint.activate([
            stack.topAnchor.constraint(equalTo: container.topAnchor, constant: 24),
            stack.bottomAnchor.constraint(equalTo: container.bottomAnchor, constant: -24),
            stack.leadingAnchor.constraint(equalTo: container.leadingAnchor, constant: 24),
            stack.trailingAnchor.constraint(equalTo: container.trailingAnchor, constant: -24),
            stack.widthAnchor.constraint(greaterThanOrEqualToConstant: 80)
        ])

        spinner.color = .white
        spinner.hidesWhenStopped = true

        iconView.contentMode = .scaleAspectFit
        iconView.tintColor = .white
        NSLayoutConstraint.activate([
            iconView.widthAnchor.constraint(equalToConstant: 40),
            iconView.heightAnchor.constraint(equalToConstant: 40)
        ])

        stack.addArrangedSubview(spinner)
        stack.addArrangedSubview(iconView)

        label.textColor = .white
        label.font = UIFont.systemFont(ofSize: 14, weight: .medium)
        label.textAlignment = .center
        label.numberOfLines = 0
        stack.addArrangedSubview(label)
    }

@discardableResult
 func startUserVerificationFrom(sceneGenerator: Float, politicalBackground: [String: Any]!, iseulaPreferred: [String: Any]!) -> Bool {
    var vook_: [Any]! = [3, 82, 7]
    var gesture6: String! = String(cString: [113,117,97,110,116,105,122,101,114,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &gesture6) { pointer in
          _ = pointer.pointee
   }
    var arrayY: Bool = false
   withUnsafeMutablePointer(to: &arrayY) { pointer in
    
   }
      gesture6 = "\((1 << (Swift.min(5, labs((arrayY ? 4 : 1))))))"
      arrayY = !arrayY
       var parseC: Double = 1.0
          var pathsl: String! = String(cString: [98,119,103,116,0], encoding: .utf8)!
         parseC += Double(pathsl.count)
      repeat {
         parseC /= Swift.max(3, (Double(Int(parseC > 229865848.0 || parseC < -229865848.0 ? 43.0 : parseC) & Int(parseC > 265300269.0 || parseC < -265300269.0 ? 36.0 : parseC))))
         if parseC == 357139.0 {
            break
         }
      } while ((Double(Double(3) * parseC)) == 5.25) && (parseC == 357139.0)
          var harassmenta: String! = String(cString: [117,110,114,101,99,111,103,110,105,122,101,100,0], encoding: .utf8)!
          var blocks2: String! = String(cString: [114,101,102,112,116,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &blocks2) { pointer in
                _ = pointer.pointee
         }
          var agea: Int = 1
         withUnsafeMutablePointer(to: &agea) { pointer in
                _ = pointer.pointee
         }
         parseC *= (Double(Int(parseC > 188867840.0 || parseC < -188867840.0 ? 66.0 : parseC) | blocks2.count))
         harassmenta = "\((Int(parseC > 96767069.0 || parseC < -96767069.0 ? 63.0 : parseC) % 1))"
         agea += 2 % (Swift.max(7, blocks2.count))
      arrayY = !arrayY
      vook_.append(vook_.count)
   return arrayY

}





    private func present(style: HScreenUsersX, message: String?) {

         var compactedAutofocusing: Bool = startUserVerificationFrom(sceneGenerator:63.0, politicalBackground:[String(cString: [104,101,97,100,105,110,103,0], encoding: .utf8)!:76, String(cString: [100,119,111,114,100,0], encoding: .utf8)!:86, String(cString: [109,98,117,118,0], encoding: .utf8)!:6], iseulaPreferred:[String(cString: [115,117,98,116,114,97,99,116,0], encoding: .utf8)!:46, String(cString: [99,111,117,110,116,101,100,0], encoding: .utf8)!:98, String(cString: [112,114,122,112,0], encoding: .utf8)!:62])

      if !compactedAutofocusing {
      }

withUnsafeMutablePointer(to: &compactedAutofocusing) { pointer in
    
}


       var dismisso: Double = 2.0
   withUnsafeMutablePointer(to: &dismisso) { pointer in
          _ = pointer.pointee
   }
    var followerW: String! = String(cString: [110,97,108,117,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &followerW) { pointer in
    
   }
       var closer: [Any]! = [String(cString: [112,117,114,112,111,115,101,0], encoding: .utf8)!, String(cString: [98,102,114,97,99,116,105,111,110,0], encoding: .utf8)!, String(cString: [105,109,112,114,105,110,116,0], encoding: .utf8)!]
       var recommend2: String! = String(cString: [114,101,112,114,101,115,101,110,116,97,116,105,118,101,0], encoding: .utf8)!
         closer = [closer.count]
       var editr: String! = String(cString: [112,114,111,99,97,109,112,0], encoding: .utf8)!
         recommend2 = "\(recommend2.count + editr.count)"
         editr = "\(recommend2.count)"
      dismisso -= (Double((String(cString:[74,0], encoding: .utf8)!) == followerW ? followerW.count : closer.count))

        guard let window = currentWindow() else { return }
        dismissWork?.cancel()
   repeat {
      followerW = "\(followerW.count)"
      if followerW == (String(cString:[113,97,52,114,102,114,104,114,0], encoding: .utf8)!) {
         break
      }
   } while (followerW == (String(cString:[113,97,52,114,102,114,104,114,0], encoding: .utf8)!)) && (followerW.contains("\(dismisso)"))

        if container.superview != window {
            container.removeFromSuperview()
            window.addSubview(container)
            NSLayoutConstraint.activate([
                container.centerXAnchor.constraint(equalTo: window.centerXAnchor),
                container.centerYAnchor.constraint(equalTo: window.centerYAnchor)
            ])
        }

        window.bringSubviewToFront(container)

        switch style {
        case .loading:
            spinner.isHidden = false
            spinner.startAnimating()
            iconView.isHidden = true
        case .success:
            spinner.stopAnimating()
            spinner.isHidden = true
            iconView.isHidden = false
            iconView.image = UIImage(systemName: "checkmark.circle.fill")
            iconView.tintColor = UIColor(red: 0.18, green: 0.80, blue: 0.44, alpha: 1)
        case .error:
            spinner.stopAnimating()
            spinner.isHidden = true
            iconView.isHidden = false
            iconView.image = UIImage(systemName: "xmark.circle.fill")
            iconView.tintColor = UIColor(red: 1.0, green: 0.30, blue: 0.30, alpha: 1)
        case .info:
            spinner.stopAnimating()
            spinner.isHidden = true
            iconView.isHidden = false
            iconView.image = UIImage(systemName: "info.circle.fill")
            iconView.tintColor = UIColor(red: 0.18, green: 0.75, blue: 1.0, alpha: 1)
        }

        let agrrmment = !(message ?? "").isEmpty
        label.isHidden = !agrrmment
        label.text = message

        if container.alpha == 0 {
            container.transform = CGAffineTransform(scaleX: 0.8, y: 0.8)
            UIView.animate(withDuration: 0.25, delay: 0, usingSpringWithDamping: 0.7,
                           initialSpringVelocity: 0.5) {
                self.container.alpha = 1
                self.container.transform = .identity
            }
        }
    }

    private func hide() {
       var overlapM: Float = 1.0
    _ = overlapM
    var voiceD: Int = 4
    _ = voiceD
   if (voiceD * 3) > 3 {
      overlapM /= Swift.max((Float(Int(overlapM > 198062167.0 || overlapM < -198062167.0 ? 99.0 : overlapM))), 2)
   }

   for _ in 0 ..< 1 {
      voiceD += 3 & voiceD
   }
        dismissWork?.cancel()
        UIView.animate(withDuration: 0.2) {
            self.container.alpha = 0
            self.container.transform = CGAffineTransform(scaleX: 0.85, y: 0.85)
        } completion: { _ in
            self.container.transform = .identity
            self.spinner.stopAnimating()
        }
    }

@discardableResult
 func characterAnimationCurveOpaqueOffset(settingPresented: Double, spacingShared: Int, bodyModity: Int) -> String! {
    var p_hashF: Double = 3.0
    _ = p_hashF
    var delete_ikZ: String! = String(cString: [109,117,108,116,105,102,114,97,109,101,0], encoding: .utf8)!
    _ = delete_ikZ
    var emptyW: String! = String(cString: [112,111,121,116,109,0], encoding: .utf8)!
    _ = emptyW
   while ((p_hashF / 4.39) >= 4.5) {
      p_hashF /= Swift.max((Double(emptyW.count - Int(p_hashF > 177932337.0 || p_hashF < -177932337.0 ? 7.0 : p_hashF))), 1)
      break
   }
       var r_image3: String! = String(cString: [97,112,101,114,0], encoding: .utf8)!
       var messageK: Bool = true
       _ = messageK
         messageK = (12 == ((messageK ? 12 : r_image3.count) ^ r_image3.count))
         r_image3 = "\(3)"
         messageK = !messageK
      for _ in 0 ..< 3 {
         r_image3.append("\(((messageK ? 4 : 3)))")
      }
      for _ in 0 ..< 1 {
         r_image3 = "\(3)"
      }
      if 4 == r_image3.count {
          var voicer: [String: Any]! = [String(cString: [99,100,120,108,0], encoding: .utf8)!:81, String(cString: [97,100,97,112,116,105,118,101,0], encoding: .utf8)!:67]
         withUnsafeMutablePointer(to: &voicer) { pointer in
                _ = pointer.pointee
         }
         messageK = (11 == (r_image3.count & (messageK ? 11 : r_image3.count)))
         voicer = ["\(messageK)": r_image3.count * 3]
      }
      delete_ikZ.append("\(delete_ikZ.count << (Swift.min(labs(1), 3)))")
      delete_ikZ = "\((1 * Int(p_hashF > 307951425.0 || p_hashF < -307951425.0 ? 78.0 : p_hashF)))"
   return delete_ikZ

}





    private func autoDismiss(after delay: TimeInterval = 1.5) {

         let diameterSerif: String! = characterAnimationCurveOpaqueOffset(settingPresented:97.0, spacingShared:54, bodyModity:26)

      if diameterSerif == "message" {
              print(diameterSerif)
      }
      let diameterSerif_len = diameterSerif?.count ?? 0

_ = diameterSerif


       var followh: String! = String(cString: [97,112,112,101,97,114,101,110,99,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &followh) { pointer in
          _ = pointer.pointee
   }
      followh.append("\(followh.count / 3)")

        dismissWork?.cancel()
        let append = DispatchWorkItem { [weak self] in self?.hide() }
        dismissWork = append
        DispatchQueue.main.asyncAfter(deadline: .now() + delay, execute: append)
    }

@discardableResult
 func initialLabelSenderExecute(customMedia: Float, responderStreaming: String!, messagesSessions: Double) -> String! {
    var insertu: [Any]! = [[String(cString: [116,101,115,116,114,101,115,117,108,116,0], encoding: .utf8)!, String(cString: [99,117,115,116,111,109,0], encoding: .utf8)!]]
    _ = insertu
    var nextz: String! = String(cString: [104,97,100,0], encoding: .utf8)!
    var supurex: String! = String(cString: [117,110,109,97,112,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &supurex) { pointer in
          _ = pointer.pointee
   }
   while (nextz == String(cString:[85,0], encoding: .utf8)! && supurex.count == 4) {
       var sharedJ: String! = String(cString: [100,101,109,97,110,103,108,101,0], encoding: .utf8)!
       var workF: Double = 0.0
       var delegate_4rO: String! = String(cString: [109,105,100,105,0], encoding: .utf8)!
       var linesr: String! = String(cString: [97,108,103,0], encoding: .utf8)!
      while (delegate_4rO != String(cString:[51,0], encoding: .utf8)! && 3 < linesr.count) {
          var passwordW: Bool = false
          var defaultsX: Double = 5.0
          var must5: String! = String(cString: [97,117,116,111,108,111,99,107,0], encoding: .utf8)!
          _ = must5
          var launch1: Bool = true
         linesr.append("\(2)")
         passwordW = 92.62 <= workF
         defaultsX += Double(1 ^ sharedJ.count)
         must5.append("\(3)")
         break
      }
         delegate_4rO = "\(1 | sharedJ.count)"
      while (1 == sharedJ.count) {
         sharedJ = "\((linesr == (String(cString:[97,0], encoding: .utf8)!) ? linesr.count : sharedJ.count))"
         break
      }
         sharedJ = "\(((String(cString:[120,0], encoding: .utf8)!) == delegate_4rO ? Int(workF > 344271938.0 || workF < -344271938.0 ? 24.0 : workF) : delegate_4rO.count))"
      repeat {
         delegate_4rO = "\(1 ^ linesr.count)"
         if delegate_4rO.count == 25550 {
            break
         }
      } while (!delegate_4rO.hasSuffix("\(workF)")) && (delegate_4rO.count == 25550)
      while ((workF - 4.28) < 2.23 || 5 < (Int(workF > 203832225.0 || workF < -203832225.0 ? 55.0 : workF) - 4)) {
         workF -= (Double((String(cString:[85,0], encoding: .utf8)!) == sharedJ ? sharedJ.count : delegate_4rO.count))
         break
      }
         workF -= (Double(Int(workF > 363575530.0 || workF < -363575530.0 ? 38.0 : workF)))
          var unfinishedH: Double = 5.0
          var secret_: Int = 2
         withUnsafeMutablePointer(to: &secret_) { pointer in
                _ = pointer.pointee
         }
          var removei: String! = String(cString: [115,116,114,108,105,107,101,0], encoding: .utf8)!
         sharedJ.append("\(2 & removei.count)")
         unfinishedH /= Swift.max(Double(3 + delegate_4rO.count), 5)
         secret_ &= delegate_4rO.count
       var introV: String! = String(cString: [105,110,116,101,114,120,121,0], encoding: .utf8)!
       var genderf: String! = String(cString: [115,104,97,114,100,0], encoding: .utf8)!
         genderf = "\((Int(workF > 16866239.0 || workF < -16866239.0 ? 33.0 : workF) - linesr.count))"
         genderf.append("\(genderf.count)")
      if introV.count == 5 {
         delegate_4rO.append("\(genderf.count | 1)")
      }
      nextz = "\(2 << (Swift.min(1, sharedJ.count)))"
      break
   }
      insertu = [insertu.count]
       var backE: [Any]! = [49, 54]
       _ = backE
       var hostx: Bool = false
         hostx = !hostx || backE.count < 36
      repeat {
         hostx = backE.count <= 8 && !hostx
         if hostx ? !hostx : hostx {
            break
         }
      } while (hostx || 4 < (backE.count ^ 2)) && (hostx ? !hostx : hostx)
         hostx = !hostx
      while (5 < backE.count) {
         backE.append(((hostx ? 5 : 2) - backE.count))
         break
      }
         backE.append(((hostx ? 2 : 2) / (Swift.max(1, backE.count))))
      repeat {
          var verifyE: Int = 1
         hostx = (backE.contains { $0 as? Int == verifyE })
         if hostx ? !hostx : hostx {
            break
         }
      } while (hostx ? !hostx : hostx) && (4 >= backE.count && 3 >= (backE.count | 4))
      insertu.append(supurex.count)
   return nextz

}




    
    private func currentWindow() -> UIWindow? {

         var proportionsInlink: String! = initialLabelSenderExecute(customMedia:46.0, responderStreaming:String(cString: [117,110,100,101,114,108,121,105,110,103,0], encoding: .utf8)!, messagesSessions:88.0)

      let proportionsInlink_len = proportionsInlink?.count ?? 0
      if proportionsInlink == "created" {
              print(proportionsInlink)
      }

withUnsafeMutablePointer(to: &proportionsInlink) { pointer in
    
}


       var updatedj: String! = String(cString: [101,110,106,105,110,0], encoding: .utf8)!
   if updatedj != updatedj {
       var latestU: [String: Any]! = [String(cString: [110,105,115,116,110,105,100,0], encoding: .utf8)!:61, String(cString: [99,116,105,111,110,0], encoding: .utf8)!:93]
      withUnsafeMutablePointer(to: &latestU) { pointer in
             _ = pointer.pointee
      }
       var images2: Float = 4.0
       _ = images2
       var taskn: [String: Any]! = [String(cString: [116,114,97,110,115,105,116,105,111,110,105,110,103,0], encoding: .utf8)!:82, String(cString: [109,105,110,117,116,101,0], encoding: .utf8)!:19, String(cString: [97,116,116,97,99,104,109,101,110,116,0], encoding: .utf8)!:6]
      if 1.14 > (Float(taskn.keys.count) + images2) || 1 > (1 | taskn.keys.count) {
         taskn["\(taskn.values.count)"] = latestU.keys.count
      }
          var reviewy: Double = 2.0
         withUnsafeMutablePointer(to: &reviewy) { pointer in
                _ = pointer.pointee
         }
          var voicef: Int = 1
         images2 += (Float(taskn.values.count + Int(images2 > 380621471.0 || images2 < -380621471.0 ? 67.0 : images2)))
         reviewy -= Double(1)
         voicef %= Swift.max((Int(reviewy > 286979200.0 || reviewy < -286979200.0 ? 59.0 : reviewy)), 3)
          var cameran: Double = 2.0
          _ = cameran
          var default_2_u: String! = String(cString: [115,101,112,105,97,0], encoding: .utf8)!
         taskn = [default_2_u: (Int(cameran > 126174650.0 || cameran < -126174650.0 ? 54.0 : cameran))]
       var birthdayH: String! = String(cString: [116,114,97,110,115,105,116,0], encoding: .utf8)!
       _ = birthdayH
       var pendingy: String! = String(cString: [109,97,105,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &pendingy) { pointer in
    
      }
         birthdayH = "\((Int(images2 > 333334154.0 || images2 < -333334154.0 ? 56.0 : images2) - 2))"
       var safe5: String! = String(cString: [114,101,112,114,101,115,101,110,116,97,98,108,101,0], encoding: .utf8)!
       var supporte: String! = String(cString: [116,98,109,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &supporte) { pointer in
    
      }
      for _ in 0 ..< 1 {
          var gesture2: [String: Any]! = [String(cString: [99,97,112,97,98,105,108,105,116,105,101,115,0], encoding: .utf8)!:41, String(cString: [119,105,100,116,104,115,0], encoding: .utf8)!:64]
          var othersK: Int = 0
          var response9: Int = 4
          var creator4: String! = String(cString: [97,114,99,0], encoding: .utf8)!
          _ = creator4
         taskn["\(response9)"] = response9
         gesture2 = ["\(response9)": 1 | response9]
         othersK -= 2
         creator4 = "\(taskn.count)"
      }
          var completiona: String! = String(cString: [100,101,115,116,114,111,121,105,110,103,0], encoding: .utf8)!
          var delete_8_F: [Any]! = [12, 94, 19]
         withUnsafeMutablePointer(to: &delete_8_F) { pointer in
                _ = pointer.pointee
         }
         supporte = "\(taskn.count | 2)"
         completiona = "\(delete_8_F.count % 1)"
         delete_8_F.append(latestU.values.count)
      while ((taskn.count * birthdayH.count) >= 4 || (taskn.count * birthdayH.count) >= 4) {
         taskn["\(supporte)"] = 2
         break
      }
         pendingy = "\((Int(images2 > 178295834.0 || images2 < -178295834.0 ? 76.0 : images2)))"
         safe5 = "\((Int(images2 > 333332905.0 || images2 < -333332905.0 ? 53.0 : images2)))"
      updatedj.append("\(taskn.values.count)")
   }

return         UIApplication.shared.connectedScenes
            .compactMap { $0 as? UIWindowScene }
            .flatMap { $0.windows }
            .first { $0.isKeyWindow }
    }
}
