
import Foundation

import UIKit
import SDWebImage

class PTHomeUsersView: UIView {
var controlShouldContent_string: String?
var cacheGeneratorLyricsStr: String!
var test_margin: Double? = 0.0




    @IBOutlet weak var tableView: UITableView!
    @IBOutlet weak var comment_textFiled: UITextField!
    @IBOutlet weak var inputContainerBottomConstraint: NSLayoutConstraint!
    
    var vook_postData: STUResponsePopup?
    var comments: [JKHome] = []
    var vook_didUpdatePost: ((STUResponsePopup) -> Void)?

@discardableResult
 func lightStatusCommonApplicationReport(bannerMost: [Any]!, womanWidth: Double) -> Int {
    var follow1: [Any]! = [8, 59]
    var prefix_q1y: String! = String(cString: [111,116,104,101,114,115,0], encoding: .utf8)!
    var receivey: Int = 1
   withUnsafeMutablePointer(to: &receivey) { pointer in
    
   }
   if !follow1.contains { $0 as? Int == receivey } {
       var created_: String! = String(cString: [117,115,101,114,115,0], encoding: .utf8)!
       var postsQ: String! = String(cString: [116,114,101,108,108,105,115,0], encoding: .utf8)!
       var parameters9: String! = String(cString: [115,119,114,101,115,97,109,112,108,101,114,101,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &parameters9) { pointer in
             _ = pointer.pointee
      }
       var encryptionm: Float = 1.0
       var tetxf: Double = 5.0
      withUnsafeMutablePointer(to: &tetxf) { pointer in
    
      }
      for _ in 0 ..< 3 {
         parameters9 = "\((Int(tetxf > 318212583.0 || tetxf < -318212583.0 ? 21.0 : tetxf)))"
      }
         postsQ.append("\(3)")
       var http8: String! = String(cString: [114,117,110,110,105,110,103,0], encoding: .utf8)!
       var tenantu: String! = String(cString: [116,104,114,101,97,100,103,114,111,117,112,0], encoding: .utf8)!
       _ = tenantu
         parameters9 = "\(created_.count + 3)"
         tetxf /= Swift.max(1, Double(1 >> (Swift.min(5, parameters9.count))))
         postsQ = "\(http8.count >> (Swift.min(tenantu.count, 5)))"
      repeat {
          var trailingW: String! = String(cString: [99,111,112,121,0], encoding: .utf8)!
          var cellB: [String: Any]! = [String(cString: [108,111,99,97,108,0], encoding: .utf8)!:26.0]
          _ = cellB
          var infoc: Int = 3
         withUnsafeMutablePointer(to: &infoc) { pointer in
                _ = pointer.pointee
         }
          var size_upS: Float = 0.0
         postsQ = "\(cellB.keys.count)"
         trailingW = "\((Int(tetxf > 143959034.0 || tetxf < -143959034.0 ? 52.0 : tetxf)))"
         infoc += (Int(tetxf > 62111837.0 || tetxf < -62111837.0 ? 9.0 : tetxf))
         size_upS *= Float(postsQ.count)
         if 3317781 == postsQ.count {
            break
         }
      } while (3317781 == postsQ.count) && (!postsQ.hasPrefix(parameters9))
      for _ in 0 ..< 2 {
          var side6: Double = 1.0
         tetxf /= Swift.max(Double(tenantu.count | 3), 1)
         side6 /= Swift.max((Double(Int(side6 > 350555949.0 || side6 < -350555949.0 ? 71.0 : side6) << (Swift.min(3, labs(1))))), 1)
      }
      while (created_.hasPrefix("\(tetxf)")) {
          var reusem: String! = String(cString: [116,97,117,0], encoding: .utf8)!
          var insetX: String! = String(cString: [100,99,116,114,101,102,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &insetX) { pointer in
    
         }
          var g_countW: [Any]! = [16, 67]
         withUnsafeMutablePointer(to: &g_countW) { pointer in
                _ = pointer.pointee
         }
          var lyricsW: Double = 3.0
          var tabt: Double = 1.0
         tetxf *= (Double(Int(lyricsW > 93248813.0 || lyricsW < -93248813.0 ? 8.0 : lyricsW) << (Swift.min(postsQ.count, 5))))
         reusem = "\(g_countW.count / (Swift.max(2, 1)))"
         insetX = "\(((String(cString:[79,0], encoding: .utf8)!) == tenantu ? created_.count : tenantu.count))"
         g_countW.append((Int(tabt > 79764441.0 || tabt < -79764441.0 ? 73.0 : tabt)))
         tabt -= Double(reusem.count)
         break
      }
         created_.append("\(created_.count >> (Swift.min(labs(2), 5)))")
          var gestureh: Int = 5
          _ = gestureh
         postsQ.append("\((Int(tetxf > 275045750.0 || tetxf < -275045750.0 ? 76.0 : tetxf) - parameters9.count))")
         gestureh += created_.count % (Swift.max(tenantu.count, 7))
      while (created_ == postsQ) {
          var pictureb: Float = 5.0
         postsQ = "\(created_.count - 1)"
         pictureb += Float(http8.count)
         break
      }
          var finishm: [String: Any]! = [String(cString: [112,97,114,97,109,103,101,110,0], encoding: .utf8)!:21, String(cString: [101,114,114,108,111,103,0], encoding: .utf8)!:77]
          var jsonr: String! = String(cString: [104,97,110,100,108,101,100,0], encoding: .utf8)!
          _ = jsonr
         parameters9 = "\((Int(encryptionm > 114565306.0 || encryptionm < -114565306.0 ? 13.0 : encryptionm) << (Swift.min(jsonr.count, 4))))"
         finishm = ["\(tetxf)": (3 * Int(encryptionm > 202691683.0 || encryptionm < -202691683.0 ? 81.0 : encryptionm))]
         tetxf -= (Double(3 | Int(encryptionm > 111784701.0 || encryptionm < -111784701.0 ? 56.0 : encryptionm)))
      while (parameters9.hasSuffix(tenantu)) {
          var emptyP: String! = String(cString: [105,100,108,101,0], encoding: .utf8)!
          var oldJ: String! = String(cString: [97,116,116,101,109,112,116,0], encoding: .utf8)!
          var icons: String! = String(cString: [105,109,112,108,101,109,101,110,116,97,116,105,111,110,0], encoding: .utf8)!
         parameters9 = "\(icons.count)"
         emptyP = "\(oldJ.count)"
         oldJ.append("\(emptyP.count)")
         break
      }
      receivey &= (Int(tetxf > 85741929.0 || tetxf < -85741929.0 ? 74.0 : tetxf) ^ 1)
   }
   repeat {
      receivey -= 1
      if 1647061 == receivey {
         break
      }
   } while (!prefix_q1y.hasPrefix("\(receivey)")) && (1647061 == receivey)
   return receivey

}






    @IBAction func dismissCommentsView(_ sender: Any) {

         let structuresShorts: Int = lightStatusCommonApplicationReport(bannerMost:[String(cString: [110,97,116,105,118,101,108,121,0], encoding: .utf8)!, String(cString: [114,101,112,108,97,99,101,109,101,110,116,115,0], encoding: .utf8)!], womanWidth:55.0)

      if structuresShorts > 25 {
             print(structuresShorts)
      }

_ = structuresShorts


       var customt: Double = 1.0
   withUnsafeMutablePointer(to: &customt) { pointer in
    
   }
    var audio0: String! = String(cString: [115,101,116,98,105,116,0], encoding: .utf8)!
      customt /= Swift.max(Double(2), 5)

   if audio0 != String(cString:[80,0], encoding: .utf8)! && audio0.count == 3 {
      audio0 = "\(2)"
   }
        endEditing(true)
        UIView.animate(withDuration: 0.32, delay: 0, options: .curveEaseOut) {
            self.transform = CGAffineTransform(translationX: 0, y: 1200)
        } completion: { _ in
            self.isHidden = true
        }
    }
    
    deinit {
        NotificationCenter.default.removeObserver(self)
    }

@discardableResult
 func shadowManOriginalMustPicture() -> Double {
    var musick: String! = String(cString: [105,110,99,111,109,112,97,116,105,98,108,101,0], encoding: .utf8)!
    _ = musick
    var loadk: [String: Any]! = [String(cString: [116,101,108,101,103,114,97,0], encoding: .utf8)!:71, String(cString: [116,111,112,105,99,115,0], encoding: .utf8)!:47, String(cString: [99,108,97,115,115,105,102,105,101,114,0], encoding: .utf8)!:69]
    var blurf: Double = 4.0
    _ = blurf
   if (musick.count * 3) >= 4 || 3 >= (musick.count + Int(blurf > 108098763.0 || blurf < -108098763.0 ? 10.0 : blurf)) {
      musick.append("\(musick.count)")
   }
   if !loadk.keys.contains("\(blurf)") {
      loadk = ["\(blurf)": musick.count]
   }
      musick.append("\((Int(blurf > 104355359.0 || blurf < -104355359.0 ? 52.0 : blurf)))")
   return blurf

}





    
    override func awakeFromNib() {

         var serverSelection: Double = shadowManOriginalMustPicture()

      print(serverSelection)

withUnsafeMutablePointer(to: &serverSelection) { pointer in
        _ = pointer.pointee
}


       var type_ru: String! = String(cString: [99,111,109,109,97,110,100,108,105,110,101,102,108,97,103,0], encoding: .utf8)!
   while (1 == type_ru.count) {
      type_ru.append("\((type_ru == (String(cString:[75,0], encoding: .utf8)!) ? type_ru.count : type_ru.count))")
      break
   }

        super.awakeFromNib()
        tableView.dataSource = self
        tableView.delegate = self
        tableView.backgroundColor = .clear
        tableView.keyboardDismissMode = .interactive
        tableView.register(UINib(nibName: "NContainerFollowCell", bundle: nil), forCellReuseIdentifier: "cell")
        comment_textFiled.delegate = self
        
        let method_ez = UITapGestureRecognizer(target: self, action: #selector(vook_dismissKeyboard))
        method_ez.cancelsTouchesInView = false
        addGestureRecognizer(method_ez)
        
        NotificationCenter.default.addObserver(self,
                                               selector: #selector(vook_keyboardWillChangeFrame(_:)),
                                               name: UIResponder.keyboardWillChangeFrameNotification,
                                               object: nil)
        NotificationCenter.default.addObserver(self,
                                               selector: #selector(vook_keyboardWillHide(_:)),
                                               name: UIResponder.keyboardWillHideNotification,
                                               object: nil)
    }

@discardableResult
 func fullDurationRecommendGeneratorPath(pickerReceive: String!, valuesField: Double, msgLike: [String: Any]!) -> Double {
    var size_wg: [String: Any]! = [String(cString: [107,101,114,110,101,114,0], encoding: .utf8)!:String(cString: [99,109,100,117,116,105,108,115,0], encoding: .utf8)!, String(cString: [115,117,98,113,117,101,114,121,0], encoding: .utf8)!:String(cString: [117,110,108,111,99,107,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &size_wg) { pointer in
          _ = pointer.pointee
   }
    var controls: String! = String(cString: [101,99,114,101,116,0], encoding: .utf8)!
    var stylew: Double = 1.0
      controls = "\(size_wg.values.count)"
   repeat {
      size_wg = ["\(size_wg.keys.count)": 1 / (Swift.max(5, size_wg.keys.count))]
      if size_wg.count == 3461255 {
         break
      }
   } while (size_wg.count == 3461255) && (stylew <= Double(size_wg.keys.count))
   if 4.73 < (stylew * 4.27) {
      controls = "\(size_wg.count << (Swift.min(labs(3), 2)))"
   }
   return stylew

}





    
    private func vook_updateKeyboardLayout(bottom: CGFloat, notification: Notification) {

         var collectiblesArtifacts: Double = fullDurationRecommendGeneratorPath(pickerReceive:String(cString: [104,111,110,101,0], encoding: .utf8)!, valuesField:10.0, msgLike:[String(cString: [107,101,109,112,102,0], encoding: .utf8)!:[String(cString: [98,105,116,115,116,114,105,110,103,0], encoding: .utf8)!:0.0]])

      print(collectiblesArtifacts)

withUnsafeMutablePointer(to: &collectiblesArtifacts) { pointer in
        _ = pointer.pointee
}


       var customs: [Any]! = [78, 96, 93]
   withUnsafeMutablePointer(to: &customs) { pointer in
          _ = pointer.pointee
   }
   repeat {
      customs = [customs.count]
      if 271366 == customs.count {
         break
      }
   } while (271366 == customs.count) && ((customs.count | 2) > 4 && (customs.count | 2) > 3)

        let text = notification.userInfo?[UIResponder.keyboardAnimationDurationUserInfoKey] as? TimeInterval ?? 0.25
        let custom = notification.userInfo?[UIResponder.keyboardAnimationCurveUserInfoKey] as? UInt ?? 7
        let url = UIView.AnimationOptions(rawValue: custom << 16)
        
        inputContainerBottomConstraint.constant = bottom
        UIView.animate(withDuration: text, delay: 0, options: url) {
            self.layoutIfNeeded()
        }
    }

    
    private func vook_scrollToLatestComment() {
       var chunkA: Double = 3.0
    _ = chunkA
    var bottomj: Double = 0.0
    _ = bottomj
   repeat {
      chunkA /= Swift.max(Double(2), 3)
      if chunkA == 4733473.0 {
         break
      }
   } while (2.79 < (bottomj - chunkA) && 5.90 < (chunkA * 2.79)) && (chunkA == 4733473.0)

        guard comments.isEmpty == false else { return }
        tableView.scrollToRow(at: IndexPath(row: 0, section: 0), at: .top, animated: true)
   while ((Double(chunkA / (Swift.max(Double(3), 5)))) >= 1.84) {
       var hintG: String! = String(cString: [100,101,99,114,121,112,116,0], encoding: .utf8)!
       var scene_iJ: String! = String(cString: [117,99,108,111,99,107,0], encoding: .utf8)!
       var childU: [String: Any]! = [String(cString: [104,117,102,102,121,117,118,101,110,99,100,115,112,0], encoding: .utf8)!:97, String(cString: [118,101,99,0], encoding: .utf8)!:91]
       var storeS: String! = String(cString: [98,101,116,116,101,114,0], encoding: .utf8)!
      if hintG == scene_iJ {
         scene_iJ = "\(hintG.count % (Swift.max(6, scene_iJ.count)))"
      }
         scene_iJ.append("\(childU.keys.count | storeS.count)")
         childU[storeS] = (storeS == (String(cString:[48,0], encoding: .utf8)!) ? storeS.count : hintG.count)
      for _ in 0 ..< 1 {
          var spinnerD: Int = 5
         withUnsafeMutablePointer(to: &spinnerD) { pointer in
    
         }
          var listb: String! = String(cString: [100,101,118,105,100,101,0], encoding: .utf8)!
         hintG.append("\(((String(cString:[106,0], encoding: .utf8)!) == storeS ? storeS.count : childU.keys.count))")
         spinnerD &= 2
         listb = "\(scene_iJ.count + 1)"
      }
          var photoK: String! = String(cString: [112,97,116,116,101,114,110,115,0], encoding: .utf8)!
          var toupiU: [Any]! = [38, 22]
          var supureh: String! = String(cString: [112,114,105,110,99,105,112,97,108,0], encoding: .utf8)!
         storeS.append("\(childU.count / (Swift.max(hintG.count, 3)))")
         photoK = "\((photoK == (String(cString:[75,0], encoding: .utf8)!) ? photoK.count : childU.values.count))"
         toupiU = [toupiU.count]
         supureh = "\(1 >> (Swift.min(4, toupiU.count)))"
      while (childU.keys.count <= hintG.count) {
         childU = ["\(childU.keys.count)": childU.count / 1]
         break
      }
      repeat {
         scene_iJ.append("\(childU.count | 1)")
         if (String(cString:[112,115,108,120,50,0], encoding: .utf8)!) == scene_iJ {
            break
         }
      } while (!scene_iJ.hasPrefix(storeS)) && ((String(cString:[112,115,108,120,50,0], encoding: .utf8)!) == scene_iJ)
      repeat {
         childU[scene_iJ] = 2 + hintG.count
         if childU.count == 3694057 {
            break
         }
      } while (childU.count == 3694057) && (scene_iJ.count <= childU.count)
      bottomj += Double(scene_iJ.count)
      break
   }
    }

@discardableResult
 func pauseMarginRecorderMoveLabel() -> UILabel! {
    var observeI: [String: Any]! = [String(cString: [98,97,99,107,105,110,103,0], encoding: .utf8)!:String(cString: [109,117,109,98,97,105,0], encoding: .utf8)!, String(cString: [103,114,97,112,104,0], encoding: .utf8)!:String(cString: [97,116,116,114,97,99,116,105,110,103,0], encoding: .utf8)!, String(cString: [119,97,105,116,101,114,0], encoding: .utf8)!:String(cString: [111,99,99,117,114,114,101,110,99,101,115,0], encoding: .utf8)!]
    var messageS: String! = String(cString: [116,111,112,105,99,97,108,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
       var filedT: String! = String(cString: [101,120,116,101,110,115,105,111,110,115,0], encoding: .utf8)!
       var m_animationf: Double = 1.0
       var editingy: Bool = false
         editingy = m_animationf < 60.25 && filedT == (String(cString:[115,0], encoding: .utf8)!)
         m_animationf -= Double(filedT.count)
         m_animationf -= Double(1)
      for _ in 0 ..< 2 {
          var insufficientk: [Any]! = [66, 70]
          _ = insufficientk
          var valueF: String! = String(cString: [114,101,112,111,114,116,115,0], encoding: .utf8)!
         editingy = 82 <= valueF.count
         insufficientk.append(((editingy ? 4 : 4) % (Swift.max(2, Int(m_animationf > 233610918.0 || m_animationf < -233610918.0 ? 51.0 : m_animationf)))))
      }
       var sharedu: String! = String(cString: [99,104,111,115,101,110,0], encoding: .utf8)!
       _ = sharedu
       var information6: String! = String(cString: [99,111,108,117,109,110,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &information6) { pointer in
             _ = pointer.pointee
      }
         editingy = m_animationf > 94.24
      while (m_animationf > 2.69) {
         m_animationf /= Swift.max(5, (Double(information6 == (String(cString:[76,0], encoding: .utf8)!) ? information6.count : (editingy ? 2 : 2))))
         break
      }
         information6 = "\(((editingy ? 3 : 1)))"
       var update_brE: String! = String(cString: [114,101,97,100,109,101,0], encoding: .utf8)!
       _ = update_brE
         sharedu.append("\(((String(cString:[56,0], encoding: .utf8)!) == sharedu ? sharedu.count : Int(m_animationf > 198886322.0 || m_animationf < -198886322.0 ? 33.0 : m_animationf)))")
         update_brE.append("\(sharedu.count)")
      messageS.append("\(observeI.keys.count)")
   }
     let pagePoint: UIButton! = UIButton(frame:CGRect.zero)
     let sharePresent: UIView! = UIView(frame:CGRect(x: 46, y: 5, width: 0, height: 0))
     var purchaseScalar: UILabel! = UILabel(frame:CGRect.zero)
    var writeIdctxOstar = UILabel()
    writeIdctxOstar.frame = CGRect(x: 239, y: 33, width: 0, height: 0)
    writeIdctxOstar.alpha = 0.0;
    writeIdctxOstar.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    writeIdctxOstar.textAlignment = .right
    writeIdctxOstar.font = UIFont.systemFont(ofSize:14)
    writeIdctxOstar.text = ""
    writeIdctxOstar.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pagePoint.frame = CGRect(x: 39, y: 168, width: 0, height: 0)
    pagePoint.alpha = 0.7;
    pagePoint.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pagePoint.setTitle("", for: .normal)
    pagePoint.setBackgroundImage(UIImage(named:String(cString: [112,111,115,116,0], encoding: .utf8)!), for: .normal)
    pagePoint.titleLabel?.font = UIFont.systemFont(ofSize:15)
    pagePoint.setImage(UIImage(named:String(cString: [101,110,97,98,108,101,95,115,0], encoding: .utf8)!), for: .normal)
    
    sharePresent.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sharePresent.alpha = 0.9
    sharePresent.frame = CGRect(x: 127, y: 126, width: 0, height: 0)
    
    purchaseScalar.frame = CGRect(x: 75, y: 195, width: 0, height: 0)
    purchaseScalar.alpha = 0.6;
    purchaseScalar.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    purchaseScalar.textAlignment = .center
    purchaseScalar.font = UIFont.systemFont(ofSize:13)
    purchaseScalar.text = ""
    purchaseScalar.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    

    
    return writeIdctxOstar

}





    
    func vook_update(postData: STUResponsePopup?) {

         var polylineNoalloc: UILabel! = pauseMarginRecorderMoveLabel()

      if polylineNoalloc != nil {
          let polylineNoalloc_tag = polylineNoalloc.tag
          self.addSubview(polylineNoalloc)
      }
      else {
          print("polylineNoalloc is nil")      }

withUnsafeMutablePointer(to: &polylineNoalloc) { pointer in
        _ = pointer.pointee
}


       var outgoing2: String! = String(cString: [115,101,99,115,0], encoding: .utf8)!
      outgoing2 = "\(outgoing2.count)"

        vook_postData = postData
        if let postData = postData {
            comments = MSPList.shared.vook_VisibleComments(vook_postData: postData)
        } else {
            comments = []
        }
        tableView.reloadData()
    }

@discardableResult
 func customFollowingStringContainer(durationRecorder: [Any]!) -> Double {
    var postsj: Bool = true
    var followersw: [Any]! = [10, 27]
    _ = followersw
   if 5 > (1 >> (Swift.min(2, followersw.count))) && 1 > followersw.count {
      followersw.append(followersw.count)
   }
      postsj = (((postsj ? 26 : followersw.count) << (Swift.min(followersw.count, 2))) <= 26)
     var voicePackage: Bool = true
     var homeBoth: Double = 87.0
    var resetCurr:Double = 0
    voicePackage = true
    resetCurr -= Double(voicePackage ? 18 : 85)
    resetCurr /= Swift.max(homeBoth, 1)

    return resetCurr

}





    
    @objc private func vook_keyboardWillChangeFrame(_ notification: Notification) {

         var stringifyAvui: Double = customFollowingStringContainer(durationRecorder:[33, 91])

      print(stringifyAvui)

withUnsafeMutablePointer(to: &stringifyAvui) { pointer in
        _ = pointer.pointee
}


       var i_animationV: Float = 2.0
   withUnsafeMutablePointer(to: &i_animationV) { pointer in
    
   }
    var imagesD: String! = String(cString: [99,109,97,107,101,0], encoding: .utf8)!
      i_animationV -= Float(imagesD.count)

        guard isHidden == false,
              let chat = notification.userInfo,
              let buffer = chat[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue else { return }
        
        let comments = convert(buffer.cgRectValue, from: nil)
      i_animationV += Float(imagesD.count)
        let other = max(0, bounds.maxY - comments.minY)
        vook_updateKeyboardLayout(bottom: other, notification: notification)
    }

@discardableResult
 func encodePointDestructiveStandard(commentsAllows: String!, productIcon: Double, startPath: Int) -> Float {
    var nameo: String! = String(cString: [114,116,112,112,114,111,116,111,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &nameo) { pointer in
          _ = pointer.pointee
   }
    var messagec: Bool = false
    var sideD: Float = 1.0
   withUnsafeMutablePointer(to: &sideD) { pointer in
          _ = pointer.pointee
   }
       var completionD: Double = 4.0
      withUnsafeMutablePointer(to: &completionD) { pointer in
    
      }
       var eulaD: Bool = true
      withUnsafeMutablePointer(to: &eulaD) { pointer in
             _ = pointer.pointee
      }
         eulaD = !eulaD
      while (eulaD && (5.92 / (Swift.max(3, completionD))) > 2.99) {
          var line0: String! = String(cString: [114,101,106,101,99,116,105,111,110,115,0], encoding: .utf8)!
          var messageE: Float = 1.0
          _ = messageE
          var o_layerQ: String! = String(cString: [97,99,99,101,115,115,0], encoding: .utf8)!
          var recordsh: Float = 5.0
         withUnsafeMutablePointer(to: &recordsh) { pointer in
                _ = pointer.pointee
         }
         eulaD = !eulaD
         line0 = "\((Int(recordsh > 237104815.0 || recordsh < -237104815.0 ? 31.0 : recordsh) & o_layerQ.count))"
         messageE /= Swift.max(2, (Float(3 | Int(completionD > 46321641.0 || completionD < -46321641.0 ? 28.0 : completionD))))
         o_layerQ.append("\((3 >> (Swift.min(2, labs(Int(recordsh > 389539200.0 || recordsh < -389539200.0 ? 34.0 : recordsh))))))")
         break
      }
         eulaD = 53.59 >= completionD
      while (3.93 > completionD) {
         completionD -= (Double(Int(completionD > 143787939.0 || completionD < -143787939.0 ? 93.0 : completionD) + (eulaD ? 3 : 2)))
         break
      }
      if eulaD {
         eulaD = !eulaD || 78.27 < completionD
      }
       var f_productst: Double = 3.0
         f_productst += (Double(Int(f_productst > 15967286.0 || f_productst < -15967286.0 ? 86.0 : f_productst) ^ 1))
      nameo.append("\((Int(sideD > 9902898.0 || sideD < -9902898.0 ? 72.0 : sideD) + 2))")
   for _ in 0 ..< 1 {
       var messages8: Bool = false
       _ = messages8
       var labelT: [Any]! = [UILabel()]
       _ = labelT
       var value8: [String: Any]! = [String(cString: [112,97,112,101,114,0], encoding: .utf8)!:73, String(cString: [97,99,99,114,117,101,0], encoding: .utf8)!:21]
       var reportl: Double = 1.0
      while (value8.values.contains { $0 as? Int == labelT.count }) {
         value8["\(messages8)"] = labelT.count
         break
      }
         reportl /= Swift.max((Double(labelT.count >> (Swift.min(1, labs((messages8 ? 3 : 2)))))), 5)
         reportl /= Swift.max(4, (Double(Int(reportl > 124662812.0 || reportl < -124662812.0 ? 69.0 : reportl) + 1)))
      while (4.3 <= (reportl / 4.51) || !messages8) {
         messages8 = ((Int(reportl > 234415267.0 || reportl < -234415267.0 ? 75.0 : reportl)) >= value8.count)
         break
      }
      for _ in 0 ..< 3 {
         labelT = [labelT.count << (Swift.min(2, value8.keys.count))]
      }
      while (4.19 < (reportl / (Swift.max(Double(value8.keys.count), 3))) && 3.78 < (reportl / 4.19)) {
         reportl -= (Double(2 & Int(reportl > 321501290.0 || reportl < -321501290.0 ? 5.0 : reportl)))
         break
      }
       var settingq: [Any]! = [59, 56, 34]
       _ = settingq
      while (Double(labelT.count) <= reportl) {
          var tenantQ: Float = 2.0
          _ = tenantQ
         reportl += Double(2 % (Swift.max(2, labelT.count)))
         tenantQ -= Float(value8.count >> (Swift.min(labs(3), 1)))
         break
      }
          var toupiN: String! = String(cString: [114,97,116,105,110,103,0], encoding: .utf8)!
          var willT: String! = String(cString: [99,105,112,104,101,114,98,121,110,105,100,0], encoding: .utf8)!
          _ = willT
         value8["\(reportl)"] = toupiN.count
         willT = "\(toupiN.count)"
          var messageT: String! = String(cString: [100,112,116,114,115,0], encoding: .utf8)!
          var enable_dV: Int = 0
         settingq.append(2)
         messageT.append("\(labelT.count)")
         enable_dV &= (messageT == (String(cString:[85,0], encoding: .utf8)!) ? messageT.count : Int(reportl > 360867344.0 || reportl < -360867344.0 ? 41.0 : reportl))
          var marginx: Float = 4.0
         reportl /= Swift.max(5, (Double(Int(marginx > 207256807.0 || marginx < -207256807.0 ? 55.0 : marginx) * 2)))
      if !value8.values.contains { $0 as? Int == labelT.count } {
         value8 = ["\(value8.count)": value8.values.count + settingq.count]
      }
      messagec = messages8
   }
   return sideD

}





    
    @objc private func vook_dismissKeyboard() {

         var locWakeup: Float = encodePointDestructiveStandard(commentsAllows:String(cString: [108,111,111,112,102,105,108,116,101,114,0], encoding: .utf8)!, productIcon:78.0, startPath:5)

      if locWakeup > 43 {
             print(locWakeup)
      }

withUnsafeMutablePointer(to: &locWakeup) { pointer in
    
}


       var controllerF: [String: Any]! = [String(cString: [115,110,100,105,111,0], encoding: .utf8)!:[2.0]]
      controllerF = ["\(controllerF.values.count)": controllerF.count]

        endEditing(true)
    }

@discardableResult
 func cancelEntityReceiveEvent(cellConverted: Double, playerTest: [Any]!) -> Double {
    var panel4: String! = String(cString: [98,105,110,0], encoding: .utf8)!
    var recommendP: String! = String(cString: [100,109,97,98,117,102,0], encoding: .utf8)!
   while (recommendP != String(cString:[122,0], encoding: .utf8)!) {
      panel4.append("\(recommendP.count >> (Swift.min(labs(1), 2)))")
      break
   }
   if panel4.count >= recommendP.count {
       var likedM: Double = 5.0
       var finishX: String! = String(cString: [108,115,98,102,117,108,108,0], encoding: .utf8)!
       _ = finishX
       var sharedx: Double = 2.0
       var l_titleb: Int = 4
       var storage2: Double = 2.0
       var collectionJ: Double = 0.0
      if 3.79 < (Double(Int(sharedx) + 2)) {
         likedM /= Swift.max(2, Double(finishX.count))
      }
      repeat {
         collectionJ -= (Double(finishX == (String(cString:[67,0], encoding: .utf8)!) ? Int(likedM > 59093063.0 || likedM < -59093063.0 ? 57.0 : likedM) : finishX.count))
         if collectionJ == 3271365.0 {
            break
         }
      } while (likedM == collectionJ) && (collectionJ == 3271365.0)
      for _ in 0 ..< 3 {
          var mapO: String! = String(cString: [108,101,101,119,97,121,0], encoding: .utf8)!
          var delete_9m4: [String: Any]! = [String(cString: [117,95,50,53,0], encoding: .utf8)!:String(cString: [107,101,99,99,97,107,0], encoding: .utf8)!, String(cString: [109,101,116,97,100,97,116,97,115,0], encoding: .utf8)!:String(cString: [99,117,114,108,121,0], encoding: .utf8)!, String(cString: [114,101,113,117,105,117,114,101,115,0], encoding: .utf8)!:String(cString: [99,100,99,105,0], encoding: .utf8)!]
          var imageviewM: String! = String(cString: [115,105,109,112,108,105,102,121,0], encoding: .utf8)!
          var wasE: String! = String(cString: [97,115,111,99,0], encoding: .utf8)!
          _ = wasE
         sharedx /= Swift.max(5, (Double(3 % (Swift.max(Int(sharedx > 226528969.0 || sharedx < -226528969.0 ? 6.0 : sharedx), 7)))))
         mapO.append("\((Int(likedM > 247546852.0 || likedM < -247546852.0 ? 8.0 : likedM) << (Swift.min(4, labs(3)))))")
         delete_9m4[finishX] = finishX.count % 2
         imageviewM = "\(3 & l_titleb)"
         wasE.append("\(1)")
      }
          var controlR: Double = 2.0
         sharedx /= Swift.max(3, (Double(Int(storage2 > 54193325.0 || storage2 < -54193325.0 ? 44.0 : storage2))))
         controlR /= Swift.max(1, (Double(1 - Int(storage2 > 54304770.0 || storage2 < -54304770.0 ? 64.0 : storage2))))
      repeat {
         sharedx /= Swift.max(3, (Double(Int(storage2 > 360734372.0 || storage2 < -360734372.0 ? 92.0 : storage2))))
         if sharedx == 2310136.0 {
            break
         }
      } while ((finishX.count % 4) < 3) && (sharedx == 2310136.0)
          var overlayB: Bool = false
          var codeW: Float = 4.0
         finishX.append("\((Int(storage2 > 26625623.0 || storage2 < -26625623.0 ? 20.0 : storage2) * (overlayB ? 3 : 4)))")
         codeW -= (Float(2 % (Swift.max(Int(codeW > 1751176.0 || codeW < -1751176.0 ? 45.0 : codeW), 4))))
      repeat {
          var scalarw: String! = String(cString: [115,112,114,105,116,101,0], encoding: .utf8)!
          var reportedr: [Any]! = [44, 80, 87]
         withUnsafeMutablePointer(to: &reportedr) { pointer in
                _ = pointer.pointee
         }
          var openq: Float = 5.0
          var gifth: Double = 1.0
          var constraintsb: Int = 1
         collectionJ /= Swift.max(2, Double(scalarw.count & 1))
         reportedr.append((Int(sharedx > 266631299.0 || sharedx < -266631299.0 ? 45.0 : sharedx)))
         openq += (Float(scalarw.count + Int(storage2 > 361237649.0 || storage2 < -361237649.0 ? 53.0 : storage2)))
         gifth -= Double(reportedr.count)
         constraintsb /= Swift.max(1, 2 + scalarw.count)
         if collectionJ == 531555.0 {
            break
         }
      } while ((collectionJ - storage2) <= 1.46) && (collectionJ == 531555.0)
         sharedx /= Swift.max(1, (Double((String(cString:[98,0], encoding: .utf8)!) == finishX ? Int(sharedx > 88875078.0 || sharedx < -88875078.0 ? 46.0 : sharedx) : finishX.count)))
      while (collectionJ == sharedx) {
          var scalar7: String! = String(cString: [99,108,105,112,112,105,110,103,110,111,100,101,0], encoding: .utf8)!
          var nameP: Float = 0.0
         collectionJ -= (Double(1 ^ Int(sharedx > 371130065.0 || sharedx < -371130065.0 ? 73.0 : sharedx)))
         scalar7.append("\(3)")
         nameP -= (Float(Int(sharedx > 291480266.0 || sharedx < -291480266.0 ? 87.0 : sharedx)))
         break
      }
         sharedx -= Double(2)
         sharedx += (Double(Int(collectionJ > 185105262.0 || collectionJ < -185105262.0 ? 13.0 : collectionJ) >> (Swift.min(1, labs(2)))))
      panel4.append("\(2)")
   }
     let blackVisible: Bool = true
    var essentialViolation:Double = 0
    essentialViolation /= Double(blackVisible ? 54 : 17)

    return essentialViolation

}





    
    @objc private func vook_keyboardWillHide(_ notification: Notification) {

         var initencChomp: Double = cancelEntityReceiveEvent(cellConverted:75.0, playerTest:[[UILabel(frame:CGRect.zero)]])

      print(initencChomp)

withUnsafeMutablePointer(to: &initencChomp) { pointer in
        _ = pointer.pointee
}


       var previewO: String! = String(cString: [112,111,105,110,116,99,98,98,0], encoding: .utf8)!
    _ = previewO
    var setting0: [Any]! = [String(cString: [99,111,112,121,98,97,99,107,0], encoding: .utf8)!, String(cString: [99,111,110,115,117,109,112,116,105,111,110,0], encoding: .utf8)!, String(cString: [107,101,121,103,101,110,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &setting0) { pointer in
    
   }
      previewO.append("\(setting0.count)")
   if (previewO.count >> (Swift.min(labs(5), 5))) > 1 && 1 > (previewO.count >> (Swift.min(labs(5), 2))) {
       var vooko: String! = String(cString: [97,115,99,101,110,100,101,114,0], encoding: .utf8)!
       var tap3: [Any]! = [String(cString: [103,117,116,116,101,114,0], encoding: .utf8)!, String(cString: [105,115,111,112,101,110,113,117,111,116,101,0], encoding: .utf8)!]
       var source0: String! = String(cString: [118,97,114,105,97,98,108,101,0], encoding: .utf8)!
       var loginE: String! = String(cString: [102,105,108,108,105,110,0], encoding: .utf8)!
       var self_i8: Bool = true
         loginE.append("\(vooko.count - 2)")
      while (self_i8) {
          var agrrmmentc: Float = 0.0
         withUnsafeMutablePointer(to: &agrrmmentc) { pointer in
                _ = pointer.pointee
         }
          var eulax: Float = 5.0
         withUnsafeMutablePointer(to: &eulax) { pointer in
    
         }
         source0.append("\(1 | source0.count)")
         agrrmmentc /= Swift.max(1, Float(source0.count / 1))
         eulax += Float(source0.count)
         break
      }
      if (4 & source0.count) < 1 || 2 < (tap3.count & 4) {
          var nextC: Double = 4.0
          var fallback_: Bool = false
         withUnsafeMutablePointer(to: &fallback_) { pointer in
                _ = pointer.pointee
         }
          var sessionC: String! = String(cString: [116,112,105,100,0], encoding: .utf8)!
          _ = sessionC
         tap3 = [vooko.count]
         nextC -= (Double(vooko == (String(cString:[116,0], encoding: .utf8)!) ? vooko.count : (fallback_ ? 3 : 3)))
         sessionC = "\((1 | Int(nextC > 209917320.0 || nextC < -209917320.0 ? 64.0 : nextC)))"
      }
         tap3 = [tap3.count - 3]
      repeat {
          var min_zkD: [Any]! = [21, 1]
          var closev: String! = String(cString: [114,101,97,115,115,109,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &closev) { pointer in
    
         }
          var r_centerD: String! = String(cString: [108,111,119,98,100,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &r_centerD) { pointer in
    
         }
         source0 = "\(source0.count)"
         min_zkD = [closev.count >> (Swift.min(labs(1), 4))]
         closev.append("\(closev.count ^ r_centerD.count)")
         r_centerD = "\(r_centerD.count)"
         if (String(cString:[114,54,49,105,122,121,0], encoding: .utf8)!) == source0 {
            break
         }
      } while ((String(cString:[114,54,49,105,122,121,0], encoding: .utf8)!) == source0) && ((2 % (Swift.max(6, tap3.count))) > 4)
         source0 = "\(3)"
       var supureU: Int = 0
       var harassmentZ: Int = 3
       _ = harassmentZ
       var message4: Int = 2
         tap3 = [((String(cString:[74,0], encoding: .utf8)!) == vooko ? tap3.count : vooko.count)]
         harassmentZ |= 1 << (Swift.min(5, loginE.count))
          var workW: [Any]! = [95, 91, 81]
          _ = workW
          var secretu: String! = String(cString: [97,108,112,104,97,110,117,109,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &secretu) { pointer in
    
         }
          var sideD: Int = 5
         loginE = "\(tap3.count / (Swift.max(secretu.count, 5)))"
         workW = [loginE.count + 1]
         sideD -= 2 ^ loginE.count
       var others4: String! = String(cString: [115,112,108,97,115,104,0], encoding: .utf8)!
       var loginD: String! = String(cString: [112,105,112,0], encoding: .utf8)!
      if source0.hasPrefix(loginE) {
         loginE = "\(others4.count)"
      }
         vooko.append("\((source0 == (String(cString:[76,0], encoding: .utf8)!) ? source0.count : harassmentZ))")
      repeat {
         loginD.append("\(message4)")
         if loginD == (String(cString:[54,122,118,55,48,122,104,102,0], encoding: .utf8)!) {
            break
         }
      } while (loginD.contains("\(harassmentZ)")) && (loginD == (String(cString:[54,122,118,55,48,122,104,102,0], encoding: .utf8)!))
         supureU %= Swift.max(5, supureU % (Swift.max(loginD.count, 6)))
      previewO = "\(source0.count % 3)"
   }

       var dismissU: String! = String(cString: [116,114,105,97,110,103,108,101,0], encoding: .utf8)!
       var strY: Double = 4.0
         dismissU.append("\(3)")
          var profileV: Double = 5.0
         strY += (Double(Int(strY > 99964300.0 || strY < -99964300.0 ? 62.0 : strY) - dismissU.count))
         profileV /= Swift.max(3, Double(dismissU.count >> (Swift.min(labs(1), 5))))
          var sendH: Int = 1
         dismissU.append("\(2)")
         sendH <<= Swift.min(5, labs((sendH & Int(strY > 336971118.0 || strY < -336971118.0 ? 76.0 : strY))))
      for _ in 0 ..< 3 {
         dismissU = "\(2)"
      }
      while (3.87 >= (strY / (Swift.max(5.30, 9))) || 5.30 >= (strY / (Swift.max(8, Double(dismissU.count))))) {
         strY -= Double(2)
         break
      }
      for _ in 0 ..< 2 {
          var customK: String! = String(cString: [115,112,101,99,105,97,108,0], encoding: .utf8)!
          _ = customK
          var bottomS: Bool = true
          var delete_oA: String! = String(cString: [115,116,97,103,101,0], encoding: .utf8)!
         dismissU = "\((1 ^ Int(strY > 280221585.0 || strY < -280221585.0 ? 10.0 : strY)))"
         customK = "\(3)"
         bottomS = delete_oA.hasSuffix("\(bottomS)")
         delete_oA.append("\((delete_oA.count ^ (bottomS ? 4 : 5)))")
      }
      setting0 = [((String(cString:[120,0], encoding: .utf8)!) == dismissU ? setting0.count : dismissU.count)]
        vook_updateKeyboardLayout(bottom: 0, notification: notification)
    }

@discardableResult
 func pushInformationUnknownVelocityReportVerification() -> Double {
    var listj: Double = 3.0
    var hint0: String! = String(cString: [99,117,114,116,97,105,110,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &hint0) { pointer in
          _ = pointer.pointee
   }
    var responderP: Double = 1.0
       var bar1: String! = String(cString: [102,105,108,101,104,101,97,100,101,114,0], encoding: .utf8)!
      while (bar1.hasSuffix(bar1)) {
         bar1.append("\((bar1 == (String(cString:[67,0], encoding: .utf8)!) ? bar1.count : bar1.count))")
         break
      }
      while (bar1 != String(cString:[82,0], encoding: .utf8)!) {
         bar1.append("\((bar1 == (String(cString:[81,0], encoding: .utf8)!) ? bar1.count : bar1.count))")
         break
      }
         bar1.append("\((bar1 == (String(cString:[120,0], encoding: .utf8)!) ? bar1.count : bar1.count))")
      hint0 = "\(1 + bar1.count)"
      responderP /= Swift.max(2, Double(1))
   repeat {
      listj -= (Double(Int(listj > 82243950.0 || listj < -82243950.0 ? 26.0 : listj)))
      if 4753177.0 == listj {
         break
      }
   } while ((2.9 * listj) == 1.68 && (2.9 + listj) == 4.90) && (4753177.0 == listj)
   return listj

}





    
    private func vook_sendComment() {

         var unalignedArith: Double = pushInformationUnknownVelocityReportVerification()

      if unalignedArith >= 6 {
             print(unalignedArith)
      }

withUnsafeMutablePointer(to: &unalignedArith) { pointer in
    
}


       var encryptionY: Int = 4
    _ = encryptionY
   repeat {
       var alerta: Float = 1.0
       _ = alerta
       var birthdayw: Double = 5.0
         alerta -= Float(3)
          var coverw: Float = 1.0
         withUnsafeMutablePointer(to: &coverw) { pointer in
                _ = pointer.pointee
         }
          var cancelA: Double = 0.0
         withUnsafeMutablePointer(to: &cancelA) { pointer in
                _ = pointer.pointee
         }
         birthdayw /= Swift.max(1, (Double(Int(cancelA > 48884852.0 || cancelA < -48884852.0 ? 75.0 : cancelA))))
         coverw += (Float(Int(birthdayw > 322385217.0 || birthdayw < -322385217.0 ? 93.0 : birthdayw)))
      while (2.72 <= (birthdayw / 5.36)) {
          var encryptiond: [Any]! = [41.0]
          var controllerE: Float = 5.0
         birthdayw /= Swift.max(3, (Double(Int(controllerE > 140544334.0 || controllerE < -140544334.0 ? 85.0 : controllerE) * 2)))
         encryptiond.append((Int(birthdayw > 1650022.0 || birthdayw < -1650022.0 ? 16.0 : birthdayw) | 1))
         break
      }
          var mapp: String! = String(cString: [114,101,108,97,116,105,118,101,0], encoding: .utf8)!
          var womanL: [String: Any]! = [String(cString: [99,111,110,115,117,109,101,100,0], encoding: .utf8)!:86, String(cString: [119,97,115,0], encoding: .utf8)!:50]
         alerta += Float(3)
         mapp.append("\(womanL.values.count ^ mapp.count)")
         womanL["\(birthdayw)"] = 1
      repeat {
          var customb: Float = 3.0
          var p_viewi: Int = 5
          var overlayT: Float = 4.0
          var body_: String! = String(cString: [99,111,108,111,110,0], encoding: .utf8)!
          var packageM: String! = String(cString: [110,111,100,111,119,110,0], encoding: .utf8)!
         alerta += Float(3 | packageM.count)
         customb += (Float(Int(overlayT > 299372142.0 || overlayT < -299372142.0 ? 70.0 : overlayT) - 1))
         p_viewi ^= packageM.count
         overlayT -= Float(body_.count)
         body_ = "\((Int(customb > 208930690.0 || customb < -208930690.0 ? 13.0 : customb)))"
         if alerta == 3716670.0 {
            break
         }
      } while (4.75 < (alerta * 5.73) && (alerta - 5.73) < 3.41) && (alerta == 3716670.0)
      repeat {
         birthdayw /= Swift.max(5, (Double(Int(alerta > 143349434.0 || alerta < -143349434.0 ? 53.0 : alerta))))
         if 3784446.0 == birthdayw {
            break
         }
      } while (3784446.0 == birthdayw) && ((alerta / 2.4) <= 4.85 && (birthdayw / (Swift.max(2.4, 8))) <= 2.39)
      encryptionY |= (Int(alerta > 160332002.0 || alerta < -160332002.0 ? 75.0 : alerta))
      if encryptionY == 4759197 {
         break
      }
   } while ((3 ^ encryptionY) > 5 || (encryptionY ^ encryptionY) > 3) && (encryptionY == 4759197)

        let recording = comment_textFiled.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        guard recording.isEmpty == false else {
            YCommentPhoto.showInfo("Please enter content")
            return
        }
        guard let postId = vook_postData?.postId else { return }
        
        comment_textFiled.resignFirstResponder()
        MSPList.shared.vook_AddComment(vook_postId: postId, vook_content: recording) { [weak self] success in
            guard let self = self else { return }
            if success == false {
                YCommentPhoto.showError("Comment failed")
                return
            }
            if let postData = INRegisterContainer.shared.vook_postInfo(vook_postId: postId) {
                self.vook_postData = postData
                self.comments = MSPList.shared.vook_VisibleComments(vook_postData: postData)
                self.vook_didUpdatePost?(postData)
            }
            self.comment_textFiled.text = ""
            self.tableView.reloadData()
            self.vook_scrollToLatestComment()
        }
    }

    
    @IBAction func sendVideoCommentClick(_ sender: Any) {
       var confirmr: Double = 5.0
    var stringc: [Any]! = [4, 8]
    _ = stringc
      stringc.append(stringc.count)
   repeat {
      confirmr -= (Double(Int(confirmr > 179768499.0 || confirmr < -179768499.0 ? 32.0 : confirmr)))
      if confirmr == 3309881.0 {
         break
      }
   } while (confirmr == 3309881.0) && (!stringc.contains { $0 as? Double == confirmr })

        vook_sendComment()
    }
    
}

extension PTHomeUsersView: UITableViewDataSource, UITableViewDelegate {

@discardableResult
 func grantFollowingDirection(iconField: Double, responseUpdated: Double, rowChange: String!) -> String! {
    var isoy: Float = 5.0
    var intrinsicc: Bool = false
    var lyricsC: String! = String(cString: [117,110,105,116,0], encoding: .utf8)!
      lyricsC.append("\(((intrinsicc ? 4 : 4) / (Swift.max(9, Int(isoy > 62235305.0 || isoy < -62235305.0 ? 24.0 : isoy)))))")
       var max_uM: String! = String(cString: [106,99,104,117,102,102,0], encoding: .utf8)!
       var sourceU: [String: Any]! = [String(cString: [99,105,110,101,109,97,0], encoding: .utf8)!:18, String(cString: [116,102,117,101,108,0], encoding: .utf8)!:53]
       _ = sourceU
       var eulaq: String! = String(cString: [99,111,110,110,101,99,116,120,0], encoding: .utf8)!
          var start6: String! = String(cString: [120,102,111,114,109,101,100,0], encoding: .utf8)!
          var enable_iq: Int = 3
         eulaq.append("\(enable_iq)")
         start6 = "\(sourceU.count)"
       var numberM: Double = 4.0
       var infoL: Double = 5.0
      withUnsafeMutablePointer(to: &infoL) { pointer in
             _ = pointer.pointee
      }
      repeat {
         max_uM.append("\((Int(numberM > 304258231.0 || numberM < -304258231.0 ? 28.0 : numberM)))")
         if max_uM == (String(cString:[107,108,109,109,0], encoding: .utf8)!) {
            break
         }
      } while (max_uM == (String(cString:[107,108,109,109,0], encoding: .utf8)!)) && (eulaq != String(cString:[97,0], encoding: .utf8)!)
      for _ in 0 ..< 1 {
         sourceU = ["\(infoL)": (eulaq.count % (Swift.max(1, Int(infoL > 341721666.0 || infoL < -341721666.0 ? 79.0 : infoL))))]
      }
      for _ in 0 ..< 2 {
         max_uM = "\((max_uM == (String(cString:[52,0], encoding: .utf8)!) ? max_uM.count : sourceU.values.count))"
      }
      while (eulaq == String(cString:[80,0], encoding: .utf8)! && max_uM == String(cString:[83,0], encoding: .utf8)!) {
         max_uM.append("\(eulaq.count)")
         break
      }
       var pointe: Double = 5.0
       var hot9: Double = 4.0
       var g_viewK: [Any]! = [95, 94, 44]
       var revisiont: [Any]! = [5, 72, 63]
      if 3 >= g_viewK.count {
         hot9 -= Double(2 ^ max_uM.count)
      }
         pointe -= Double(eulaq.count)
         revisiont = [((String(cString:[122,0], encoding: .utf8)!) == eulaq ? Int(numberM > 277568094.0 || numberM < -277568094.0 ? 78.0 : numberM) : eulaq.count)]
      lyricsC = "\(((intrinsicc ? 2 : 3) ^ Int(isoy > 291924775.0 || isoy < -291924775.0 ? 69.0 : isoy)))"
   return lyricsC

}





    
    @objc private func vook_commentMoreClick(_ sender: UIButton) {

         var chanCudascale: String! = grantFollowingDirection(iconField:22.0, responseUpdated:29.0, rowChange:String(cString: [105,110,116,114,97,120,98,108,111,99,107,0], encoding: .utf8)!)

      if chanCudascale == "requst" {
              print(chanCudascale)
      }
      let chanCudascale_len = chanCudascale?.count ?? 0

withUnsafeMutablePointer(to: &chanCudascale) { pointer in
    
}


       var tabT: [String: Any]! = [String(cString: [109,117,115,120,0], encoding: .utf8)!:58, String(cString: [97,119,97,107,101,0], encoding: .utf8)!:72, String(cString: [109,101,115,115,97,103,101,115,0], encoding: .utf8)!:63]
    var local_xku: [Any]! = [String(cString: [117,110,114,101,97,100,0], encoding: .utf8)!, String(cString: [101,120,116,101,110,100,101,101,0], encoding: .utf8)!, String(cString: [114,101,115,101,116,0], encoding: .utf8)!]
      local_xku = [local_xku.count]

        guard sender.tag < comments.count else { return }
        let picture = sender.tag
      tabT = ["\(tabT.count)": tabT.keys.count + tabT.count]
        endEditing(true)
        let lines = UIAlertController(title: nil, message: nil, preferredStyle: .actionSheet)
        lines.addAction(UIAlertAction(title: "举报", style: .destructive) { [weak self] _ in
            guard let self = self, picture < self.comments.count else { return }
            let selected = self.comments[picture]
            MSPList.shared.vook_ReportComment(selected)
            self.comments.remove(at: picture)
            self.tableView.deleteRows(at: [IndexPath(row: picture, section: 0)], with: .automatic)
            UIViewController.topMost()?.showMusicAlert(title: "Hint", message: "The report was successful")
        })
        lines.addAction(UIAlertAction(title: "取消", style: .default))
        if let popover = lines.popoverPresentationController {
            popover.sourceView = sender
            popover.sourceRect = sender.bounds
        }
        UIViewController.topMost()?.present(lines, animated: true)
    }

@discardableResult
 func hitDataAlphaImageScrollView(shouldWindow_kq: String!, sharedLiked: Int, birthdayTotal: Bool) -> UIScrollView! {
    var signP: String! = String(cString: [116,105,110,121,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &signP) { pointer in
    
   }
    var passwordL: [String: Any]! = [String(cString: [109,97,116,114,105,120,102,0], encoding: .utf8)!:false]
    _ = passwordL
      passwordL = ["\(passwordL.count)": signP.count | passwordL.values.count]
      passwordL = ["\(passwordL.values.count)": passwordL.count >> (Swift.min(labs(3), 4))]
     let calendarIndex: UIImageView! = UIImageView()
     var updatedParameters: Bool = false
    var averageiirdVprintfUefa = UIScrollView()
    averageiirdVprintfUefa.frame = CGRect(x: 145, y: 178, width: 0, height: 0)
    averageiirdVprintfUefa.alpha = 1.0;
    averageiirdVprintfUefa.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    averageiirdVprintfUefa.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    averageiirdVprintfUefa.alwaysBounceVertical = false
    averageiirdVprintfUefa.alwaysBounceHorizontal = true
    averageiirdVprintfUefa.showsVerticalScrollIndicator = false
    averageiirdVprintfUefa.showsHorizontalScrollIndicator = false
    averageiirdVprintfUefa.delegate = nil
    calendarIndex.alpha = 0.3;
    calendarIndex.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    calendarIndex.frame = CGRect(x: 249, y: 103, width: 0, height: 0)
    calendarIndex.image = UIImage(named:String(cString: [99,111,118,101,114,0], encoding: .utf8)!)
    calendarIndex.contentMode = .scaleAspectFit
    calendarIndex.animationRepeatCount = 3
    
    averageiirdVprintfUefa.addSubview(calendarIndex)

    
    return averageiirdVprintfUefa

}





    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {

         let precompRetrieve: UIScrollView! = hitDataAlphaImageScrollView(shouldWindow_kq:String(cString: [115,117,109,97,114,121,0], encoding: .utf8)!, sharedLiked:5, birthdayTotal:true)

      if precompRetrieve != nil {
          self.addSubview(precompRetrieve)
          let precompRetrieve_tag = precompRetrieve.tag
      }

_ = precompRetrieve


       var currentx: String! = String(cString: [111,114,105,103,105,110,115,0], encoding: .utf8)!
      currentx.append("\(currentx.count)")

        return comments.count
    }

    
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
       var field7: Bool = false
    _ = field7
       var codeV: Double = 0.0
       _ = codeV
          var videosx: String! = String(cString: [112,97,103,101,111,117,116,0], encoding: .utf8)!
          var itemK: String! = String(cString: [119,114,105,116,105,110,103,0], encoding: .utf8)!
          var recommendq: Bool = true
          _ = recommendq
         codeV += Double(1 * videosx.count)
         itemK.append("\(3 * videosx.count)")
         codeV -= (Double(Int(codeV > 294940595.0 || codeV < -294940595.0 ? 40.0 : codeV)))
       var unfinished2: [Any]! = [1, 72, 8]
       var giftb: [Any]! = [63, 62]
       _ = giftb
         unfinished2 = [(giftb.count << (Swift.min(5, labs(Int(codeV > 71817704.0 || codeV < -71817704.0 ? 85.0 : codeV)))))]
         giftb = [(1 | Int(codeV > 94518373.0 || codeV < -94518373.0 ? 68.0 : codeV))]
      field7 = field7 || codeV > 85.62

        let arrayCell = tableView.dequeueReusableCell(withIdentifier: "cell", for: indexPath) as! NContainerFollowCell
        arrayCell.backgroundColor = .clear
        arrayCell.selectionStyle = .none
        let navigationS = comments[indexPath.row]
        arrayCell.comment_content_label.text = navigationS.content
        arrayCell.comment_tap.tag = indexPath.row
        arrayCell.comment_tap.removeTarget(nil, action: nil, for: .touchUpInside)
        arrayCell.comment_tap.addTarget(self, action: #selector(vook_commentMoreClick(_:)), for: .touchUpInside)
        if let user = INRegisterContainer.shared.vook_userInfo(vook_userId: navigationS.fromUserId) {
            arrayCell.comment_user_name_label.text = user.nickname
            arrayCell.comment_header_imageView.sd_setImage(with: URL(string: "\(MSPList.shared.vook_fileUrl)\(user.avatarUrl)"),
                                                      placeholderImage: UIImage(named: "fromBothLogout"))
        } else {
            arrayCell.comment_user_name_label.text = "---"
            arrayCell.comment_header_imageView.image = UIImage(named: "fromBothLogout")
        }
        
        return arrayCell
    }
}

extension PTHomeUsersView: UITextFieldDelegate {

@discardableResult
 func targetSensitivityTimer(sharedTotal: Double, oldMutual: String!) -> Bool {
    var successj: String! = String(cString: [118,97,99,97,110,116,0], encoding: .utf8)!
    var signX: [Any]! = [true]
    var backgroundG: Bool = true
   while (1 == signX.count) {
      signX = [signX.count % (Swift.max(9, successj.count))]
      break
   }
   while (successj.count == 3) {
      signX.append(2 & signX.count)
      break
   }
   if backgroundG {
      backgroundG = !backgroundG
   }
   return backgroundG

}





    
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {

         var memalignDetermine: Bool = targetSensitivityTimer(sharedTotal:20.0, oldMutual:String(cString: [114,101,118,101,114,115,101,0], encoding: .utf8)!)

      if !memalignDetermine {
          print("ok")
      }

withUnsafeMutablePointer(to: &memalignDetermine) { pointer in
    
}


       var multiq: Float = 5.0
      multiq += (Float(Int(multiq > 16167950.0 || multiq < -16167950.0 ? 23.0 : multiq)))

        vook_sendComment()
        return true
    }
}
