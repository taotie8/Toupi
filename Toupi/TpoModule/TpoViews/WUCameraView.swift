
import Foundation


import UIKit

class WUCameraView: UIView {
var tablePathsBlocks_dictionary: [String: Any]!
var itemMargin: Float? = 0.0
private var followsScreenScrollString: String?




    @IBOutlet weak var diamonds_insufficient_view: UIView!

@discardableResult
 func insertUserAdjustment(startYear: Double) -> String! {
    var defaultsA: [Any]! = [58, 57]
    var supurei: String! = String(cString: [121,111,110,108,121,120,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &supurei) { pointer in
          _ = pointer.pointee
   }
    var clientr: String! = String(cString: [114,103,101,110,0], encoding: .utf8)!
   if !supurei.hasPrefix("\(defaultsA.count)") {
      supurei = "\(defaultsA.count / (Swift.max(3, 1)))"
   }
   if supurei != clientr {
       var namel: [String: Any]! = [String(cString: [114,101,112,97,114,101,100,0], encoding: .utf8)!:20, String(cString: [111,110,115,101,116,0], encoding: .utf8)!:26, String(cString: [115,117,98,115,116,114,105,110,103,0], encoding: .utf8)!:1]
       var receiveX: Float = 0.0
      while (namel["\(receiveX)"] == nil) {
          var shareY: String! = String(cString: [100,105,109,101,110,115,105,116,111,110,115,0], encoding: .utf8)!
          var timerE: [String: Any]! = [String(cString: [111,117,116,113,0], encoding: .utf8)!:62, String(cString: [114,116,106,112,101,103,0], encoding: .utf8)!:56, String(cString: [100,97,114,107,0], encoding: .utf8)!:45]
          var root4: [String: Any]! = [String(cString: [119,104,105,116,101,108,105,115,116,0], encoding: .utf8)!:String(cString: [109,111,100,109,0], encoding: .utf8)!, String(cString: [116,95,55,52,95,104,109,104,100,0], encoding: .utf8)!:String(cString: [98,97,110,0], encoding: .utf8)!, String(cString: [108,111,110,103,98,105,103,0], encoding: .utf8)!:String(cString: [101,111,109,101,116,114,121,0], encoding: .utf8)!]
          _ = root4
          var backgrounda: String! = String(cString: [116,104,117,109,98,110,97,105,108,115,0], encoding: .utf8)!
          var pickerH: String! = String(cString: [105,109,105,113,0], encoding: .utf8)!
         receiveX /= Swift.max(2, Float(1))
         shareY = "\(((String(cString:[121,0], encoding: .utf8)!) == pickerH ? root4.keys.count : pickerH.count))"
         timerE = ["\(timerE.keys.count)": pickerH.count]
         root4 = [shareY: (shareY.count - Int(receiveX > 8686415.0 || receiveX < -8686415.0 ? 96.0 : receiveX))]
         backgrounda = "\(root4.count)"
         break
      }
      repeat {
          var paths1: String! = String(cString: [109,101,109,109,103,114,0], encoding: .utf8)!
          var normalizedN: String! = String(cString: [98,115,102,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &normalizedN) { pointer in
                _ = pointer.pointee
         }
          var lyricsX: [Any]! = [51, 7, 6]
         withUnsafeMutablePointer(to: &lyricsX) { pointer in
                _ = pointer.pointee
         }
         receiveX += Float(2 / (Swift.max(10, normalizedN.count)))
         paths1 = "\(((String(cString:[83,0], encoding: .utf8)!) == normalizedN ? lyricsX.count : normalizedN.count))"
         lyricsX.append(namel.count)
         if receiveX == 81081.0 {
            break
         }
      } while (1 >= (namel.values.count << (Swift.min(labs(3), 1))) || 3 >= (namel.values.count - Int(receiveX > 350556227.0 || receiveX < -350556227.0 ? 55.0 : receiveX))) && (receiveX == 81081.0)
         receiveX -= (Float(Int(receiveX > 352029822.0 || receiveX < -352029822.0 ? 66.0 : receiveX) / (Swift.max(namel.keys.count, 10))))
          var safei: Float = 1.0
         namel["\(safei)"] = (Int(safei > 361026879.0 || safei < -361026879.0 ? 75.0 : safei) | 1)
      if namel["\(receiveX)"] == nil {
          var responseP: Double = 2.0
          var configurationS: String! = String(cString: [100,105,103,101,115,116,98,121,110,97,109,101,0], encoding: .utf8)!
          var size_4cS: Double = 4.0
          var s_center6: Double = 1.0
          var followerC: Float = 3.0
         namel[configurationS] = (2 + Int(receiveX > 85755899.0 || receiveX < -85755899.0 ? 35.0 : receiveX))
         responseP /= Swift.max((Double((String(cString:[109,0], encoding: .utf8)!) == configurationS ? Int(responseP > 64271544.0 || responseP < -64271544.0 ? 33.0 : responseP) : configurationS.count)), 4)
         size_4cS += (Double(Int(responseP > 268323364.0 || responseP < -268323364.0 ? 71.0 : responseP)))
         s_center6 /= Swift.max(4, (Double(Int(s_center6 > 56016701.0 || s_center6 < -56016701.0 ? 2.0 : s_center6))))
         followerC /= Swift.max(2, (Float(Int(s_center6 > 276668176.0 || s_center6 < -276668176.0 ? 49.0 : s_center6))))
      }
      repeat {
         receiveX -= (Float(2 << (Swift.min(2, labs(Int(receiveX > 58523207.0 || receiveX < -58523207.0 ? 33.0 : receiveX))))))
         if 3491221.0 == receiveX {
            break
         }
      } while (3491221.0 == receiveX) && (2.32 <= (Float(namel.keys.count) - receiveX) && 3 <= (namel.keys.count - Int(receiveX > 126938176.0 || receiveX < -126938176.0 ? 5.0 : receiveX)))
      clientr = "\((supurei == (String(cString:[65,0], encoding: .utf8)!) ? supurei.count : namel.keys.count))"
   }
   if supurei.count < clientr.count {
      clientr.append("\(3 - supurei.count)")
   }
   return supurei

}





    
    override func awakeFromNib() {

         var foldButter: String! = insertUserAdjustment(startYear:31.0)

      print(foldButter)
      let foldButter_len = foldButter?.count ?? 0

withUnsafeMutablePointer(to: &foldButter) { pointer in
    
}


       var settingG: [String: Any]! = [String(cString: [114,101,113,117,105,114,101,109,101,110,116,0], encoding: .utf8)!:45, String(cString: [98,105,110,104,101,120,0], encoding: .utf8)!:88]
      settingG["\(settingG.count)"] = settingG.values.count - settingG.values.count

        super.awakeFromNib()
        self.addSubview(diamonds_insufficient_view)
        diamonds_insufficient_view.alpha = 0
        diamonds_insufficient_view.frame = self.bounds
    }

    
    @IBAction func diamondsInsufficientViewCancelAndConfirm(_ sender: UIButton) {
       var currento: Float = 5.0
      currento -= (Float(2 - Int(currento > 127378321.0 || currento < -127378321.0 ? 29.0 : currento)))

        diamonds_insufficient_view.alpha = 0
        if sender.tag == 1001 {
            
        }
        else {
            
        }
    }

@discardableResult
 func followOpenKeyboardDate(modityProduct: Double) -> Double {
    var labeln: String! = String(cString: [112,114,101,100,105,99,116,111,114,0], encoding: .utf8)!
    var intro5: Double = 1.0
       var navigationX: Int = 1
       _ = navigationX
       var startC: Double = 1.0
      withUnsafeMutablePointer(to: &startC) { pointer in
             _ = pointer.pointee
      }
      while (Int(startC) == navigationX) {
          var sourceL: [String: Any]! = [String(cString: [100,111,109,97,105,110,115,0], encoding: .utf8)!:String(cString: [103,101,116,98,105,116,0], encoding: .utf8)!, String(cString: [99,104,97,115,101,0], encoding: .utf8)!:String(cString: [97,117,116,111,100,101,116,101,99,116,111,114,0], encoding: .utf8)!, String(cString: [101,114,97,115,101,0], encoding: .utf8)!:String(cString: [105,110,105,116,105,97,108,105,122,101,114,115,0], encoding: .utf8)!]
          var imagesj: Bool = false
         navigationX |= (1 | Int(startC > 89028065.0 || startC < -89028065.0 ? 14.0 : startC))
         sourceL = ["\(imagesj)": (3 >> (Swift.min(labs(Int(startC > 14469064.0 || startC < -14469064.0 ? 29.0 : startC)), 1)))]
         imagesj = 10.75 <= startC
         break
      }
      while ((3 | navigationX) >= 4 || 3 >= (navigationX * Int(startC > 375183845.0 || startC < -375183845.0 ? 13.0 : startC))) {
         startC /= Swift.max(Double(navigationX & 2), 5)
         break
      }
      repeat {
          var identifierK: String! = String(cString: [115,99,97,108,97,114,0], encoding: .utf8)!
         navigationX |= (Int(startC > 350095730.0 || startC < -350095730.0 ? 99.0 : startC) | identifierK.count)
         if navigationX == 2898137 {
            break
         }
      } while (startC < Double(navigationX)) && (navigationX == 2898137)
          var prefix_fmo: Double = 0.0
          var followsJ: String! = String(cString: [117,110,100,101,114,101,115,116,105,109,97,116,101,100,0], encoding: .utf8)!
          var index_: [String: Any]! = [String(cString: [119,101,98,112,97,103,101,0], encoding: .utf8)!:[String(cString: [99,111,110,110,101,99,116,105,110,103,0], encoding: .utf8)!:String(cString: [102,97,99,101,0], encoding: .utf8)!, String(cString: [115,97,109,101,0], encoding: .utf8)!:String(cString: [101,110,97,98,108,101,0], encoding: .utf8)!, String(cString: [117,114,108,115,0], encoding: .utf8)!:String(cString: [99,108,101,97,110,117,112,0], encoding: .utf8)!]]
         withUnsafeMutablePointer(to: &index_) { pointer in
    
         }
         startC /= Swift.max(1, Double(navigationX / 2))
         prefix_fmo -= Double(followsJ.count)
         followsJ.append("\(followsJ.count)")
         index_["\(prefix_fmo)"] = (Int(prefix_fmo > 139430570.0 || prefix_fmo < -139430570.0 ? 71.0 : prefix_fmo) * index_.values.count)
      for _ in 0 ..< 3 {
         navigationX ^= (navigationX >> (Swift.min(labs(Int(startC > 143146227.0 || startC < -143146227.0 ? 68.0 : startC)), 3)))
      }
      while (4 > (Int(startC > 378035460.0 || startC < -378035460.0 ? 93.0 : startC) + navigationX) && (Double(navigationX) + startC) > 5.94) {
         startC -= Double(2)
         break
      }
      labeln = "\(navigationX)"
   repeat {
      intro5 -= (Double(labeln.count / (Swift.max(4, Int(intro5 > 255677675.0 || intro5 < -255677675.0 ? 91.0 : intro5)))))
      if intro5 == 2950234.0 {
         break
      }
   } while (2 == (labeln.count ^ 5) || 4 == (labeln.count >> (Swift.min(labs(5), 1)))) && (intro5 == 2950234.0)
     var enterFiled: Int = 71
     let firstYear: Float = 58.0
    var resizerHearts:Double = 0

    resizerHearts -= Double(enterFiled)
    resizerHearts += Double(firstYear)

    return resizerHearts

}





    
    func dismissPublishPopupView() {

         let slippageInterceptor: Double = followOpenKeyboardDate(modityProduct:69.0)

      if slippageInterceptor < 3 {
             print(slippageInterceptor)
      }

_ = slippageInterceptor


       var listl: String! = String(cString: [117,115,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &listl) { pointer in
    
   }
   while (listl.count < 1) {
      listl = "\(listl.count & listl.count)"
      break
   }

        UIView.animate(withDuration: 0.2, animations: {
            self.alpha = 0
        }) { _ in
            self.removeFromSuperview()
        }
    }

@discardableResult
 func rootAlbumBlue(todayWaveform: Double, nicknameAuto_l: Float) -> String! {
    var listj: [String: Any]! = [String(cString: [102,105,110,97,108,0], encoding: .utf8)!:String(cString: [101,118,101,114,0], encoding: .utf8)!, String(cString: [112,114,101,118,111,117,115,0], encoding: .utf8)!:String(cString: [114,111,108,108,98,97,99,107,0], encoding: .utf8)!, String(cString: [122,109,98,118,0], encoding: .utf8)!:String(cString: [111,102,102,108,105,110,101,0], encoding: .utf8)!]
    _ = listj
    var self_6q9: Float = 0.0
    var editQ: String! = String(cString: [97,118,115,115,0], encoding: .utf8)!
       var policyH: String! = String(cString: [114,111,102,105,108,101,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &policyH) { pointer in
             _ = pointer.pointee
      }
          var status0: Double = 2.0
          var filef: Int = 2
          var presentY: Float = 0.0
         policyH.append("\((filef << (Swift.min(labs(Int(status0 > 36064627.0 || status0 < -36064627.0 ? 37.0 : status0)), 4))))")
         presentY -= Float(policyH.count)
         policyH = "\(policyH.count << (Swift.min(policyH.count, 1)))"
      for _ in 0 ..< 3 {
         policyH.append("\(policyH.count)")
      }
      editQ.append("\(((String(cString:[68,0], encoding: .utf8)!) == policyH ? policyH.count : editQ.count))")
       var voicej: [String: Any]! = [String(cString: [111,98,106,110,105,100,0], encoding: .utf8)!:90, String(cString: [99,111,100,101,99,114,97,119,0], encoding: .utf8)!:92, String(cString: [111,110,101,108,105,110,101,0], encoding: .utf8)!:58]
       var startD: String! = String(cString: [115,116,114,105,115,116,97,114,116,0], encoding: .utf8)!
       var isdeletew: Int = 2
          var createl: String! = String(cString: [118,100,112,97,117,0], encoding: .utf8)!
          _ = createl
         voicej = ["\(voicej.values.count)": 1]
         createl = "\(startD.count)"
          var eulab: Int = 1
          var l_layerd: Float = 1.0
          _ = l_layerd
          var genderv: Float = 0.0
         isdeletew |= (2 + Int(genderv > 363571087.0 || genderv < -363571087.0 ? 65.0 : genderv))
         eulab -= eulab % (Swift.max(2, 2))
         l_layerd /= Swift.max((Float(Int(genderv > 53285484.0 || genderv < -53285484.0 ? 3.0 : genderv) | 1)), 4)
      for _ in 0 ..< 3 {
         startD.append("\(voicej.count + 3)")
      }
      if 3 > (2 ^ voicej.keys.count) {
         startD.append("\(2)")
      }
          var pathsg: Bool = true
          var cameraH: String! = String(cString: [104,119,99,111,110,116,101,120,116,0], encoding: .utf8)!
          var followersv: [String: Any]! = [String(cString: [116,120,104,97,115,104,0], encoding: .utf8)!:53, String(cString: [101,120,99,105,116,97,116,105,111,110,0], encoding: .utf8)!:7, String(cString: [114,101,103,105,115,116,101,114,0], encoding: .utf8)!:32]
         voicej = ["\(voicej.count)": 1 + voicej.keys.count]
         pathsg = (String(cString:[53,0], encoding: .utf8)!) == startD
         cameraH.append("\(((pathsg ? 5 : 5) | voicej.keys.count))")
         followersv = ["\(isdeletew)": isdeletew % (Swift.max(1, 2))]
       var loada: String! = String(cString: [99,118,105,100,0], encoding: .utf8)!
       _ = loada
      if voicej.keys.count <= 3 {
         startD = "\(((String(cString:[116,0], encoding: .utf8)!) == loada ? isdeletew : loada.count))"
      }
       var sessionsG: [Any]! = [71, 49]
       var postsO: [Any]! = [93, 66]
      repeat {
          var requstc: Bool = false
          var save9: Double = 3.0
          var decoderX: [Any]! = [57, 25, 21]
          _ = decoderX
          var lengthW: Double = 4.0
          var decoderk: String! = String(cString: [99,108,103,101,116,0], encoding: .utf8)!
         isdeletew |= (Int(save9 > 192804475.0 || save9 < -192804475.0 ? 65.0 : save9))
         requstc = sessionsG.count == 36
         decoderX = [isdeletew >> (Swift.min(1, labs(2)))]
         lengthW -= (Double(sessionsG.count & Int(save9 > 97380234.0 || save9 < -97380234.0 ? 63.0 : save9)))
         decoderk.append("\(3 << (Swift.min(1, startD.count)))")
         if 1498895 == isdeletew {
            break
         }
      } while (4 <= (loada.count * 3) || (isdeletew * 3) <= 4) && (1498895 == isdeletew)
         postsO = [1]
      listj["\(listj.keys.count)"] = voicej.count
   repeat {
      self_6q9 += Float(listj.values.count & 3)
      if self_6q9 == 1002503.0 {
         break
      }
   } while (2 == listj.values.count) && (self_6q9 == 1002503.0)
   return editQ

}





    
    @IBAction func publishPopupViewPhotoAndVideoClick(_ sender: UIButton) {

         let haveComparef: String! = rootAlbumBlue(todayWaveform:12.0, nicknameAuto_l:9.0)

      print(haveComparef)
      let haveComparef_len = haveComparef?.count ?? 0

_ = haveComparef


       var reals: String! = String(cString: [109,115,118,115,100,101,112,101,110,100,0], encoding: .utf8)!
    _ = reals
   repeat {
      reals = "\(reals.count)"
      if 480026 == reals.count {
         break
      }
   } while (reals.hasSuffix(reals)) && (480026 == reals.count)

        
        if sender.tag == 1001 {
            dismissPublishPopupView()
            guard let nav = currentNavigationController() else { return }
            let tetxController = MTextResponseController()
            tetxController.post_video_photo_type = true
            tetxController.hidesBottomBarWhenPushed = true
            nav.pushViewController(tetxController, animated: true)
        }
        else if sender.tag == 1002 {
            dismissPublishPopupView()
            guard let nav = currentNavigationController() else { return }
            let tetxController = MTextResponseController()
            tetxController.post_video_photo_type = false
            tetxController.hidesBottomBarWhenPushed = true
            nav.pushViewController(tetxController, animated: true)
        }
        else {
            dismissPublishPopupView()
        }
    }
    
}

extension UIView {

@discardableResult
static func leftAllDispatchAppearScreenMiddle(alertCode: Bool, hasOthers: Int) -> String! {
    var viewsz: Float = 5.0
    var updating6: Double = 4.0
    var videosx: String! = String(cString: [115,112,108,97,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &videosx) { pointer in
          _ = pointer.pointee
   }
      viewsz += (Float(Int(viewsz > 321863338.0 || viewsz < -321863338.0 ? 38.0 : viewsz) & Int(updating6 > 211891624.0 || updating6 < -211891624.0 ? 21.0 : updating6)))
   while (4.34 > (4.77 / (Swift.max(7, updating6))) && (4.77 * viewsz) > 1.75) {
      updating6 /= Swift.max(4, (Double((String(cString:[68,0], encoding: .utf8)!) == videosx ? Int(viewsz > 42554028.0 || viewsz < -42554028.0 ? 81.0 : viewsz) : videosx.count)))
      break
   }
   if 5 > (2 >> (Swift.min(3, videosx.count))) {
      updating6 += (Double(Int(viewsz > 333268841.0 || viewsz < -333268841.0 ? 42.0 : viewsz)))
   }
   return videosx

}





    
    private static func topNavigation(from vc: UIViewController) -> UINavigationController? {

         var filteredJpeglsdec: String! = leftAllDispatchAppearScreenMiddle(alertCode:true, hasOthers:4)

      print(filteredJpeglsdec)
      let filteredJpeglsdec_len = filteredJpeglsdec?.count ?? 0

withUnsafeMutablePointer(to: &filteredJpeglsdec) { pointer in
        _ = pointer.pointee
}


       var indicatort: Double = 2.0
    var trailingf: Bool = true
      trailingf = !trailingf

      indicatort -= (Double(Int(indicatort > 2268475.0 || indicatort < -2268475.0 ? 75.0 : indicatort) / (Swift.max(8, Int(indicatort > 239274359.0 || indicatort < -239274359.0 ? 21.0 : indicatort)))))
        if let presented = vc.presentedViewController {
            return topNavigation(from: presented)
        }
        if let nav = vc as? UINavigationController { return nav }
        for child in vc.children where child.view.window != nil {
            if let nav = topNavigation(from: child) { return nav }
        }
        return nil
    }

@discardableResult
 func setupMaterialAgeNick(supureScalar: Double, configurationDetails: String!, encryptedComments: [Any]!) -> String! {
    var bothD: String! = String(cString: [116,105,108,105,110,103,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &bothD) { pointer in
    
   }
    var totalM: Double = 0.0
    var backh: String! = String(cString: [99,111,108,108,101,99,116,105,111,110,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &backh) { pointer in
    
   }
   for _ in 0 ..< 3 {
      totalM /= Swift.max(4, (Double(Int(totalM > 326698066.0 || totalM < -326698066.0 ? 73.0 : totalM) * 1)))
   }
       var messagesq: Double = 5.0
         messagesq += (Double(Int(messagesq > 247015115.0 || messagesq < -247015115.0 ? 85.0 : messagesq) | Int(messagesq > 308760371.0 || messagesq < -308760371.0 ? 61.0 : messagesq)))
      if 3.30 > messagesq {
         messagesq /= Swift.max((Double(Int(messagesq > 289044916.0 || messagesq < -289044916.0 ? 66.0 : messagesq))), 2)
      }
         messagesq += (Double(1 >> (Swift.min(labs(Int(messagesq > 349780173.0 || messagesq < -349780173.0 ? 37.0 : messagesq)), 5))))
      totalM /= Swift.max((Double((String(cString:[113,0], encoding: .utf8)!) == bothD ? bothD.count : Int(totalM > 87034530.0 || totalM < -87034530.0 ? 49.0 : totalM))), 2)
   repeat {
      totalM -= Double(backh.count)
      if totalM == 52137.0 {
         break
      }
   } while (totalM == 52137.0) && (bothD.count < 3)
   return bothD

}





    
    func currentNavigationController() -> UINavigationController? {

         let stringbufferIndentation: String! = setupMaterialAgeNick(supureScalar:12.0, configurationDetails:String(cString: [103,101,116,112,105,120,0], encoding: .utf8)!, encryptedComments:[88, 93])

      let stringbufferIndentation_len = stringbufferIndentation?.count ?? 0
      if stringbufferIndentation == "setup" {
              print(stringbufferIndentation)
      }

_ = stringbufferIndentation


       var calendarj: String! = String(cString: [112,117,98,107,101,121,104,97,115,104,0], encoding: .utf8)!
   repeat {
       var targetb: Int = 3
       var commentsv: Double = 2.0
      withUnsafeMutablePointer(to: &commentsv) { pointer in
             _ = pointer.pointee
      }
       var confirme: [Any]! = [15, 81, 56]
       var taskU: String! = String(cString: [105,115,115,117,101,115,0], encoding: .utf8)!
          var headersu: String! = String(cString: [100,110,120,104,100,100,97,116,97,0], encoding: .utf8)!
         commentsv += Double(2)
         headersu = "\(2 & headersu.count)"
       var punctuationS: Double = 4.0
      withUnsafeMutablePointer(to: &punctuationS) { pointer in
    
      }
       var pornographyK: Double = 0.0
      withUnsafeMutablePointer(to: &pornographyK) { pointer in
    
      }
      repeat {
          var cameraE: String! = String(cString: [108,115,112,115,0], encoding: .utf8)!
          _ = cameraE
          var constraintw: String! = String(cString: [111,117,116,108,105,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &constraintw) { pointer in
    
         }
          var sourcem: String! = String(cString: [115,116,114,111,107,101,100,0], encoding: .utf8)!
         pornographyK /= Swift.max(5, Double(constraintw.count >> (Swift.min(labs(3), 4))))
         cameraE.append("\(constraintw.count >> (Swift.min(labs(1), 2)))")
         sourcem.append("\(1 * cameraE.count)")
         if pornographyK == 2257514.0 {
            break
         }
      } while (pornographyK == 2257514.0) && ((pornographyK * 2.6) < 4.68 && 3 < (3 ^ confirme.count))
          var inserta: Double = 1.0
          var str6: String! = String(cString: [108,105,98,114,97,114,105,101,115,0], encoding: .utf8)!
          _ = str6
          var screen4: Double = 2.0
         taskU.append("\(confirme.count)")
         inserta -= (Double(Int(commentsv > 157880069.0 || commentsv < -157880069.0 ? 4.0 : commentsv) - str6.count))
         str6 = "\((Int(screen4 > 93227107.0 || screen4 < -93227107.0 ? 58.0 : screen4) - 2))"
         screen4 += Double(2)
      repeat {
         commentsv -= Double(3)
         if 2072629.0 == commentsv {
            break
         }
      } while (2072629.0 == commentsv) && ((commentsv * pornographyK) >= 4.48 || 2.5 >= (pornographyK * 4.48))
         commentsv -= Double(targetb)
          var photox: [Any]! = [54, 51, 34]
          var albumm: [String: Any]! = [String(cString: [112,114,111,98,115,0], encoding: .utf8)!:String(cString: [115,116,105,112,112,101,100,0], encoding: .utf8)!, String(cString: [115,116,114,102,117,110,99,0], encoding: .utf8)!:String(cString: [115,105,103,101,120,112,0], encoding: .utf8)!, String(cString: [97,99,116,117,97,108,0], encoding: .utf8)!:String(cString: [115,116,114,117,99,116,115,0], encoding: .utf8)!]
         commentsv -= Double(1)
         photox.append(targetb)
         albumm["\(targetb)"] = 3
      if confirme.contains { $0 as? Double == commentsv } {
         commentsv += (Double(Int(commentsv > 22103511.0 || commentsv < -22103511.0 ? 89.0 : commentsv)))
      }
       var setupv: String! = String(cString: [117,110,99,111,110,102,105,103,117,114,101,0], encoding: .utf8)!
       var photoC: String! = String(cString: [97,97,99,100,101,99,0], encoding: .utf8)!
      while (confirme.count < targetb) {
         confirme.append(setupv.count / 3)
         break
      }
      repeat {
          var charactersw: Int = 2
          var beginI: String! = String(cString: [105,102,97,99,116,111,114,0], encoding: .utf8)!
         confirme = [confirme.count]
         charactersw >>= Swift.min(5, labs(3))
         beginI = "\((confirme.count | Int(commentsv > 67386331.0 || commentsv < -67386331.0 ? 98.0 : commentsv)))"
         if 1470512 == confirme.count {
            break
         }
      } while ((photoC.count << (Swift.min(5, confirme.count))) >= 2) && (1470512 == confirme.count)
      for _ in 0 ..< 2 {
         confirme = [3]
      }
         punctuationS -= (Double(Int(commentsv > 106926138.0 || commentsv < -106926138.0 ? 29.0 : commentsv)))
      calendarj.append("\(confirme.count * targetb)")
      if (String(cString:[119,103,52,0], encoding: .utf8)!) == calendarj {
         break
      }
   } while ((String(cString:[119,103,52,0], encoding: .utf8)!) == calendarj) && (!calendarj.contains(calendarj))


        var number: UIResponder? = self
        while let r = number {
            if let nav = r as? UINavigationController { return nav }
            if let vc = r as? UIViewController, let nav = vc.navigationController { return nav }
            number = r.next
        }
        
        guard let window = UIApplication.shared.connectedScenes
            .compactMap({ ($0 as? UIWindowScene)?.keyWindow })
            .first,
              let navij = window.rootViewController else { return nil }
        return UIView.topNavigation(from: navij)
    }
}
