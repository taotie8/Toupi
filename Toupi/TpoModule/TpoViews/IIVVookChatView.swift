
import Foundation

import UIKit

class IIVVookChatView: UIView {
var k_view: Double? = 0.0
private var recordMin_5bString: String!
private var videoSize: Float? = 0.0
private var type_u6: Double? = 0.0




    var userId: String = ""
    @IBOutlet weak var contentPanel: UIView!

@discardableResult
 func musicWhenIntoField(buttonNav: String!, codeClean: String!, linesUrls: String!) -> Float {
    var wasG: Bool = true
    var lyricsn: Double = 0.0
    var todayF: Float = 4.0
    _ = todayF
       var encryptedF: [Any]! = [86, 67, 72]
       var n_products1: Int = 0
       _ = n_products1
       var collectionj: String! = String(cString: [98,105,100,105,114,0], encoding: .utf8)!
       _ = collectionj
         n_products1 += n_products1
       var singleB: Double = 1.0
      withUnsafeMutablePointer(to: &singleB) { pointer in
             _ = pointer.pointee
      }
          var reportA: String! = String(cString: [115,117,98,99,101,108,0], encoding: .utf8)!
          var screenP: Int = 0
         collectionj = "\(1)"
         reportA = "\(n_products1)"
         screenP %= Swift.max(3, collectionj.count)
      repeat {
          var profielnameO: String! = String(cString: [122,111,111,109,0], encoding: .utf8)!
          var updatesa: String! = String(cString: [98,108,105,116,0], encoding: .utf8)!
          _ = updatesa
          var didY: String! = String(cString: [118,97,97,99,97,108,99,117,108,97,116,105,111,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &didY) { pointer in
    
         }
         singleB += Double(2)
         profielnameO = "\(collectionj.count)"
         updatesa = "\(profielnameO.count * encryptedF.count)"
         didY = "\(encryptedF.count)"
         if singleB == 4765424.0 {
            break
         }
      } while (singleB == 4765424.0) && ((Double(collectionj.count) * singleB) >= 3.91 || (collectionj.count & 5) >= 4)
         singleB += Double(1)
      for _ in 0 ..< 2 {
          var modityk: [String: Any]! = [String(cString: [104,101,116,97,0], encoding: .utf8)!:44, String(cString: [114,101,115,105,100,117,97,108,115,0], encoding: .utf8)!:30, String(cString: [101,120,104,97,117,115,116,101,100,0], encoding: .utf8)!:69]
          var observerQ: Double = 1.0
          var resultG: Int = 3
          var gender6: String! = String(cString: [97,117,116,111,100,101,108,101,116,101,0], encoding: .utf8)!
          var sessiong: [Any]! = [23, 39]
         collectionj.append("\(gender6.count & resultG)")
         modityk["\(singleB)"] = sessiong.count
         observerQ -= (Double(Int(singleB > 154863587.0 || singleB < -154863587.0 ? 29.0 : singleB)))
         sessiong = [n_products1]
      }
       var launcha: Bool = true
      withUnsafeMutablePointer(to: &launcha) { pointer in
             _ = pointer.pointee
      }
       var sharedX: Bool = true
          var recorderR: Double = 3.0
          _ = recorderR
          var childh: Bool = true
          _ = childh
         encryptedF.append((n_products1 / (Swift.max(7, (childh ? 1 : 4)))))
         recorderR += Double(2 - encryptedF.count)
         launcha = (n_products1 / (Swift.max(encryptedF.count, 8))) >= 12
         sharedX = encryptedF.count < 49
      todayF /= Swift.max((Float((wasG ? 4 : 5) | 1)), 1)
   for _ in 0 ..< 3 {
      todayF /= Swift.max(5, (Float(Int(todayF > 271614432.0 || todayF < -271614432.0 ? 64.0 : todayF) | (wasG ? 1 : 2))))
   }
   repeat {
      wasG = !wasG
      if wasG ? !wasG : wasG {
         break
      }
   } while (wasG ? !wasG : wasG) && ((lyricsn - 1.79) < 1.72)
   return todayF

}





    
    override func awakeFromNib() {

         var alpineCinepak: Float = musicWhenIntoField(buttonNav:String(cString: [100,101,102,105,110,101,100,0], encoding: .utf8)!, codeClean:String(cString: [112,101,114,102,0], encoding: .utf8)!, linesUrls:String(cString: [105,109,112,108,105,101,115,0], encoding: .utf8)!)

      if alpineCinepak <= 58 {
             print(alpineCinepak)
      }

withUnsafeMutablePointer(to: &alpineCinepak) { pointer in
        _ = pointer.pointee
}


       var nick7: String! = String(cString: [99,111,111,114,100,105,110,97,116,101,115,0], encoding: .utf8)!
      nick7.append("\(2)")

        super.awakeFromNib()
        
    }

@discardableResult
 func textStringPublishTrackNoneScrollView(urlsBackground: String!, playFollowing: Int, coinsMusic: [String: Any]!) -> UIScrollView! {
    var preview0: String! = String(cString: [97,117,116,111,99,97,112,105,116,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
    _ = preview0
    var homej: Double = 3.0
    _ = homej
      homej /= Swift.max(3, (Double(Int(homej > 343736162.0 || homej < -343736162.0 ? 97.0 : homej))))
   repeat {
      homej += (Double(1 * Int(homej > 305410931.0 || homej < -305410931.0 ? 54.0 : homej)))
      if homej == 1819772.0 {
         break
      }
   } while ((Int(homej > 246478779.0 || homej < -246478779.0 ? 84.0 : homej)) < preview0.count) && (homej == 1819772.0)
     let consumeClick: String! = String(cString: [115,101,97,114,99,104,97,98,108,101,0], encoding: .utf8)!
     let controllersControl: UILabel! = UILabel(frame:CGRect(x: 291, y: 18, width: 0, height: 0))
     var fallbackVideo: Double = 79.0
    var cpuflagsSlowdownMutual:UIScrollView! = UIScrollView()
    controllersControl.alpha = 0.0;
    controllersControl.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    controllersControl.frame = CGRect(x: 75, y: 268, width: 0, height: 0)
    controllersControl.text = ""
    controllersControl.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    controllersControl.textAlignment = .center
    controllersControl.font = UIFont.systemFont(ofSize:20)
    
    cpuflagsSlowdownMutual.addSubview(controllersControl)
    cpuflagsSlowdownMutual.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    cpuflagsSlowdownMutual.alwaysBounceVertical = true
    cpuflagsSlowdownMutual.alwaysBounceHorizontal = false
    cpuflagsSlowdownMutual.showsVerticalScrollIndicator = true
    cpuflagsSlowdownMutual.showsHorizontalScrollIndicator = false
    cpuflagsSlowdownMutual.delegate = nil
    cpuflagsSlowdownMutual.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    cpuflagsSlowdownMutual.alpha = 0.2
    cpuflagsSlowdownMutual.frame = CGRect(x: 300, y: 21, width: 0, height: 0)

    
    return cpuflagsSlowdownMutual

}




    
    

    
    
    func dismissBothUsersMoreView() {

         var swfhashFreeaddrinfo: UIScrollView! = textStringPublishTrackNoneScrollView(urlsBackground:String(cString: [114,101,115,116,97,107,101,0], encoding: .utf8)!, playFollowing:7, coinsMusic:[String(cString: [112,115,121,109,111,100,101,108,0], encoding: .utf8)!:String(cString: [118,105,115,98,108,101,0], encoding: .utf8)!, String(cString: [99,115,115,0], encoding: .utf8)!:String(cString: [100,105,115,99,97,114,100,101,100,0], encoding: .utf8)!, String(cString: [104,101,120,98,105,110,0], encoding: .utf8)!:String(cString: [116,101,115,115,101,108,108,97,116,101,0], encoding: .utf8)!])

      if swfhashFreeaddrinfo != nil {
          let swfhashFreeaddrinfo_tag = swfhashFreeaddrinfo.tag
          self.addSubview(swfhashFreeaddrinfo)
      }

withUnsafeMutablePointer(to: &swfhashFreeaddrinfo) { pointer in
    
}


       var settingsN: String! = String(cString: [115,116,97,99,107,101,100,0], encoding: .utf8)!
    var photoj: Bool = true
      settingsN = "\(((photoj ? 1 : 4) + 2))"

       var tabO: String! = String(cString: [105,115,99,111,118,101,114,0], encoding: .utf8)!
       var collectioni: String! = String(cString: [121,117,118,116,101,115,116,0], encoding: .utf8)!
       var bottom4: Bool = false
      withUnsafeMutablePointer(to: &bottom4) { pointer in
    
      }
         tabO = "\((tabO == (String(cString:[98,0], encoding: .utf8)!) ? collectioni.count : tabO.count))"
          var responseC: String! = String(cString: [97,99,99,114,117,101,100,0], encoding: .utf8)!
          var photob: String! = String(cString: [105,110,115,116,97,108,108,115,0], encoding: .utf8)!
         collectioni = "\(tabO.count ^ responseC.count)"
         photob = "\(collectioni.count)"
      repeat {
         tabO.append("\(3)")
         if 2574625 == tabO.count {
            break
         }
      } while (2574625 == tabO.count) && (tabO.count > collectioni.count)
         tabO = "\(tabO.count)"
      repeat {
          var enterT: [Any]! = [0, 45, 44]
         withUnsafeMutablePointer(to: &enterT) { pointer in
    
         }
          var configd: String! = String(cString: [100,105,115,112,108,97,99,101,109,101,110,116,0], encoding: .utf8)!
          _ = configd
          var setupy: Int = 4
          _ = setupy
          var realO: String! = String(cString: [103,108,105,116,99,104,0], encoding: .utf8)!
          var converted2: String! = String(cString: [108,111,97,100,101,100,0], encoding: .utf8)!
         bottom4 = 27 < collectioni.count && 27 < realO.count
         enterT = [3]
         configd = "\((enterT.count | (bottom4 ? 5 : 2)))"
         setupy ^= converted2.count
         converted2 = "\(tabO.count)"
         if bottom4 ? !bottom4 : bottom4 {
            break
         }
      } while (bottom4 ? !bottom4 : bottom4) && (!collectioni.hasSuffix("\(bottom4)"))
      while (4 < tabO.count) {
         tabO = "\(((String(cString:[77,0], encoding: .utf8)!) == collectioni ? collectioni.count : (bottom4 ? 2 : 4)))"
         break
      }
          var delete_pK: Float = 3.0
          var preferred6: String! = String(cString: [112,117,98,108,105,115,104,101,114,115,0], encoding: .utf8)!
         collectioni.append("\(((bottom4 ? 5 : 5) << (Swift.min(tabO.count, 4))))")
         delete_pK -= (Float(Int(delete_pK > 343569797.0 || delete_pK < -343569797.0 ? 23.0 : delete_pK) * 2))
         preferred6 = "\((2 % (Swift.max(1, Int(delete_pK > 237476380.0 || delete_pK < -237476380.0 ? 65.0 : delete_pK)))))"
      while (4 > tabO.count) {
         collectioni = "\(((bottom4 ? 4 : 1)))"
         break
      }
      for _ in 0 ..< 2 {
         bottom4 = bottom4 || collectioni.count <= 91
      }
      photoj = collectioni.count <= 90
        let filed = contentPanel ?? subviews.first ?? self
        let target = filed.bounds.height
        
        UIView.animate(withDuration: 0.25, delay: 0, options: .curveEaseIn, animations: {
            self.alpha = 0
            filed.transform = CGAffineTransform(translationX: 0, y: target)
        }) { _ in
            self.removeFromSuperview()
        }
    }

@discardableResult
 func runInformationCameraContainerSlashPanelScrollView(targetClick: String!, countPending: Float) -> UIScrollView! {
    var videosr: String! = String(cString: [101,110,118,105,114,111,110,109,101,110,116,0], encoding: .utf8)!
    var dict0: Float = 2.0
   withUnsafeMutablePointer(to: &dict0) { pointer in
          _ = pointer.pointee
   }
       var styleA: String! = String(cString: [115,112,97,99,105,110,103,0], encoding: .utf8)!
         styleA = "\(styleA.count * styleA.count)"
          var bottomf: Int = 2
         withUnsafeMutablePointer(to: &bottomf) { pointer in
    
         }
         styleA.append("\(styleA.count)")
         bottomf &= 2
         styleA.append("\(styleA.count)")
      dict0 /= Swift.max((Float(styleA == (String(cString:[114,0], encoding: .utf8)!) ? Int(dict0 > 17159271.0 || dict0 < -17159271.0 ? 16.0 : dict0) : styleA.count)), 2)
   if 5.98 >= dict0 {
      dict0 /= Swift.max((Float(Int(dict0 > 286377097.0 || dict0 < -286377097.0 ? 57.0 : dict0) & 2)), 4)
   }
      videosr.append("\(videosr.count)")
     var choosePackages: Bool = true
     let startFollower: Double = 56.0
     var mediaLikes: Double = 93.0
    var flowOmmonHandshake:UIScrollView! = UIScrollView()
    flowOmmonHandshake.delegate = nil
    flowOmmonHandshake.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    flowOmmonHandshake.alwaysBounceVertical = true
    flowOmmonHandshake.alwaysBounceHorizontal = true
    flowOmmonHandshake.showsVerticalScrollIndicator = false
    flowOmmonHandshake.showsHorizontalScrollIndicator = true
    flowOmmonHandshake.alpha = 0.0;
    flowOmmonHandshake.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    flowOmmonHandshake.frame = CGRect(x: 84, y: 236, width: 0, height: 0)

    
    return flowOmmonHandshake

}





    
    
    override func touchesEnded(_ touches: Set<UITouch>, with event: UIEvent?) {

         let significanceTack: UIScrollView! = runInformationCameraContainerSlashPanelScrollView(targetClick:String(cString: [115,116,105,99,107,101,114,0], encoding: .utf8)!, countPending:65.0)

      if significanceTack != nil {
          let significanceTack_tag = significanceTack.tag
          self.addSubview(significanceTack)
      }

_ = significanceTack


       var local_zeG: String! = String(cString: [119,104,105,116,101,108,105,115,116,101,100,0], encoding: .utf8)!
   repeat {
      local_zeG = "\(local_zeG.count)"
      if 4789138 == local_zeG.count {
         break
      }
   } while (4789138 == local_zeG.count) && (2 < local_zeG.count)

        super.touchesEnded(touches, with: event)
        guard let p = touches.first?.location(in: self) else { return }
        let filed = contentPanel ?? subviews.first ?? self
        if !filed.frame.contains(p) {
            dismissBothUsersMoreView()
        }
    }

@discardableResult
 func recommendMoreRoundListSection(keyboardLines: String!, description_7Buffer: Float, buttonsResponder: Bool) -> Int {
    var blurO: String! = String(cString: [113,100,101,108,116,97,0], encoding: .utf8)!
    var tableC: Float = 2.0
    var mutualu: Int = 4
   for _ in 0 ..< 2 {
      mutualu ^= 3 % (Swift.max(10, blurO.count))
   }
      mutualu /= Swift.max(5, mutualu ^ 1)
       var pathJ: Bool = true
       var big2: Int = 2
      for _ in 0 ..< 2 {
         big2 -= ((pathJ ? 1 : 5) | big2)
      }
          var datac: String! = String(cString: [116,114,97,110,115,105,116,105,111,110,0], encoding: .utf8)!
          _ = datac
          var videosP: [String: Any]! = [String(cString: [101,120,114,100,115,112,0], encoding: .utf8)!:92, String(cString: [99,115,104,97,114,112,0], encoding: .utf8)!:53]
          var rechW: String! = String(cString: [110,101,109,111,110,105,99,0], encoding: .utf8)!
          _ = rechW
         big2 /= Swift.max(3, datac.count / 3)
         videosP[datac] = (datac == (String(cString:[121,0], encoding: .utf8)!) ? videosP.values.count : datac.count)
         rechW.append("\(datac.count)")
         big2 += big2 * 2
      while (4 == big2 || (big2 & 4) == 3) {
         pathJ = pathJ && 68 == big2
         break
      }
         pathJ = 73 < big2 || !pathJ
          var background6: [Any]! = [String(cString: [105,109,109,101,100,105,97,116,101,0], encoding: .utf8)!, String(cString: [111,117,116,103,111,105,110,103,0], encoding: .utf8)!, String(cString: [102,109,116,99,111,110,118,101,114,116,0], encoding: .utf8)!]
         pathJ = (background6.contains { $0 as? Int == big2 })
      tableC /= Swift.max(5, Float(big2))
   return mutualu

}





    
    
    @IBAction func reportAndBlockCancelAction(_ sender: UIButton) {

         let systemdependentMemorybuffer: Int = recommendMoreRoundListSection(keyboardLines:String(cString: [106,97,105,108,98,114,101,97,107,0], encoding: .utf8)!, description_7Buffer:5.0, buttonsResponder:false)

      if systemdependentMemorybuffer != 5 {
             print(systemdependentMemorybuffer)
      }

_ = systemdependentMemorybuffer


       var insufficientT: String! = String(cString: [115,119,105,116,99,104,105,110,103,0], encoding: .utf8)!
      insufficientT = "\(2 ^ insufficientT.count)"

        if sender.tag == 1001 {
            guard let nav = currentNavigationController() else { return }
            let tetxController = NModityRecommendController()
            tetxController.hidesBottomBarWhenPushed = true
            nav.pushViewController(tetxController, animated: true)
            
            self.dismissBothUsersMoreView()
        }
        else if sender.tag == 1002 {
            let modity = userId
            guard modity.isEmpty == false,
                  modity != ZMList.shared.vook_userId else {
                dismissBothUsersMoreView()
                return
            }
            
            let hotController = currentNavigationController()?.visibleViewController ?? UIViewController.topMost()
            dismissBothUsersMoreView()
            hotController?.showMusicConfirmAlert(title: "Reminder",
                                                    message: "Are you sure you can block this user",
                                                    confirmTitle: "Confirm",
                                                    cancelTitle: "Cancel",
                                                    onConfirm: { [weak hotController] in
                MSPList.shared.vook_ToggleBlockUser(vook_targetUserId: modity) { success in
                    if success {
                        DispatchQueue.main.asyncAfter(deadline: .now() + 0.15) {
                            hotController?.showMusicAlert(title: "Hint", message: "This user has been blocked") {
                                hotController?.vook_leaveBlockedUserPageIfNeeded()
                            }
                        }
                    } else {
                        UIViewController.topMost()?.showMusicAlert(title: "Hint", message: "Block failed")
                    }
                }
            }, onCancel: { [weak self] in
                self?.dismissBothUsersMoreView()
            })
        }
        else {
            self.dismissBothUsersMoreView()
        }
    }

    
    private func playShowAnimation() {
       var nextW: [Any]! = [String(cString: [98,111,117,110,99,105,110,101,115,115,0], encoding: .utf8)!, String(cString: [115,109,105,108,105,110,103,0], encoding: .utf8)!]
    _ = nextW
   while (3 <= (nextW.count << (Swift.min(nextW.count, 4)))) {
      nextW = [nextW.count << (Swift.min(1, nextW.count))]
      break
   }

        layoutIfNeeded()
        let filed = contentPanel ?? subviews.first ?? self
        let target = filed.bounds.height
        
        self.alpha = 0
        filed.transform = CGAffineTransform(translationX: 0, y: target)
        
        UIView.animate(withDuration: 0.3, delay: 0, options: .curveEaseOut) {
            self.alpha = 1
            filed.transform = .identity
        }
    }


@discardableResult
static func shadowVoicePrepareGiftFormat() -> [Any]! {
    var requestU: Double = 5.0
    var manP: String! = String(cString: [114,101,109,111,118,97,98,108,101,0], encoding: .utf8)!
    var logouto: [Any]! = [63, 85, 1]
      manP = "\(((String(cString:[81,0], encoding: .utf8)!) == manP ? logouto.count : manP.count))"
   for _ in 0 ..< 3 {
      requestU += Double(manP.count)
   }
       var window_84: Bool = true
       var genderq: String! = String(cString: [97,116,114,97,99,112,108,117,115,0], encoding: .utf8)!
         genderq.append("\(((String(cString:[107,0], encoding: .utf8)!) == genderq ? (window_84 ? 3 : 4) : genderq.count))")
      while (!window_84) {
         window_84 = genderq.count == 16 && window_84
         break
      }
         window_84 = !window_84
      if !window_84 {
         window_84 = genderq.count >= 45
      }
      while (genderq.count == 5) {
         window_84 = (((window_84 ? genderq.count : 57) - genderq.count) >= 57)
         break
      }
       var sloty: Double = 4.0
       var c_layerl: Double = 5.0
       _ = c_layerl
         sloty -= (Double(Int(c_layerl > 120274216.0 || c_layerl < -120274216.0 ? 11.0 : c_layerl)))
         c_layerl -= Double(2)
      manP = "\(2 >> (Swift.min(3, genderq.count)))"
   return logouto

}



@discardableResult
    static func showBothUsersMoreView(userId: String, in parent: UIView? = nil) -> IIVVookChatView {

         var meaningfulEdts: [Any]! = shadowVoicePrepareGiftFormat()

      let meaningfulEdts_len = meaningfulEdts.count
      meaningfulEdts.enumerated().forEach({ (index,obj) in
          if index  >=  46 {
                          print(obj)
          }
      })

withUnsafeMutablePointer(to: &meaningfulEdts) { pointer in
    
}


       var popup7: String! = String(cString: [109,105,110,116,0], encoding: .utf8)!
    var size_5rH: String! = String(cString: [101,110,117,109,118,97,108,117,101,0], encoding: .utf8)!
    _ = size_5rH
   for _ in 0 ..< 1 {
       var startL: Int = 0
      withUnsafeMutablePointer(to: &startL) { pointer in
    
      }
       var decoderO: String! = String(cString: [117,110,112,97,100,100,101,100,0], encoding: .utf8)!
      while ((startL % 4) < 5 && (decoderO.count % (Swift.max(4, 9))) < 1) {
         decoderO.append("\(2)")
         break
      }
      if 1 == (decoderO.count % (Swift.max(4, 7))) || 3 == (decoderO.count % 4) {
         startL %= Swift.max(startL, 3)
      }
          var completionr: Double = 2.0
          _ = completionr
         startL /= Swift.max(1 / (Swift.max(10, startL)), 1)
         completionr /= Swift.max(5, Double(3))
      repeat {
          var error4: String! = String(cString: [97,110,110,111,117,110,99,101,0], encoding: .utf8)!
          _ = error4
         decoderO = "\(3)"
         error4.append("\(error4.count)")
         if decoderO.count == 3712624 {
            break
         }
      } while (decoderO.count == 3712624) && (5 >= startL)
      for _ in 0 ..< 1 {
         startL %= Swift.max(decoderO.count ^ startL, 4)
      }
         decoderO = "\(decoderO.count * 2)"
      popup7 = "\(startL)"
   }

       var date5: String! = String(cString: [116,104,101,105,114,0], encoding: .utf8)!
      while (date5 == String(cString:[118,0], encoding: .utf8)! && date5 != String(cString:[48,0], encoding: .utf8)!) {
          var frame_kn: Bool = true
          var toupid: [String: Any]! = [String(cString: [98,105,110,100,0], encoding: .utf8)!:25, String(cString: [114,101,99,118,0], encoding: .utf8)!:35, String(cString: [99,121,99,108,105,99,114,101,102,114,101,115,104,0], encoding: .utf8)!:0]
         withUnsafeMutablePointer(to: &toupid) { pointer in
                _ = pointer.pointee
         }
         date5 = "\(toupid.keys.count)"
         frame_kn = !frame_kn
         break
      }
      while (date5 != String(cString:[115,0], encoding: .utf8)! || 3 <= date5.count) {
         date5 = "\(date5.count)"
         break
      }
      repeat {
         date5.append("\(date5.count)")
         if (String(cString:[55,117,110,109,100,0], encoding: .utf8)!) == date5 {
            break
         }
      } while (date5.count == 5 || date5 != String(cString:[118,0], encoding: .utf8)!) && ((String(cString:[55,117,110,109,100,0], encoding: .utf8)!) == date5)
      size_5rH.append("\(date5.count | size_5rH.count)")
        let productView = UINib(nibName: "IIVVookChatView", bundle: nil)
            .instantiate(withOwner: nil, options: nil)
            .first as! IIVVookChatView
        
        let controller: UIView = parent ?? UIApplication.shared.connectedScenes
            .compactMap { ($0 as? UIWindowScene)?.keyWindow }
            .first ?? UIView()
        
        productView.userId = userId
        productView.frame = controller.bounds
        productView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        controller.addSubview(productView)
        productView.playShowAnimation()
        return productView
    }
    

}

extension UIViewController {
    
    static func topMost(of base: UIViewController? = {
        UIApplication.shared.connectedScenes
            .compactMap { ($0 as? UIWindowScene)?.keyWindow }
            .first?.rootViewController
    }()) -> UIViewController? {
        if let nav = base as? UINavigationController {
            return topMost(of: nav.visibleViewController)
        }
        if let tab = base as? UITabBarController, let selected = tab.selectedViewController {
            return topMost(of: selected)
        }
        if let presented = base?.presentedViewController {
            return topMost(of: presented)
        }
        if let child = base?.children.first(where: { $0.view.window != nil }) {
            return topMost(of: child)
        }
        return base
    }

@discardableResult
 func actualVideoDisplayPathMigration(voiceHomepage: Bool) -> Double {
    var toupiC: String! = String(cString: [109,105,110,105,109,105,122,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &toupiC) { pointer in
    
   }
    var safe2: [Any]! = [String(cString: [106,115,116,114,105,110,103,0], encoding: .utf8)!, String(cString: [99,117,114,114,114,101,110,116,0], encoding: .utf8)!, String(cString: [98,101,104,97,118,105,111,117,114,0], encoding: .utf8)!]
    var protocol_yi: Double = 4.0
    _ = protocol_yi
   for _ in 0 ..< 1 {
      safe2.append((toupiC == (String(cString:[81,0], encoding: .utf8)!) ? safe2.count : toupiC.count))
   }
       var users6: Double = 1.0
       _ = users6
       var insufficientm: [Any]! = [[String(cString: [103,101,116,111,112,116,0], encoding: .utf8)!:79, String(cString: [112,108,97,110,101,115,0], encoding: .utf8)!:83]]
       var choosez: [Any]! = [52, 94]
      if 3.9 >= users6 {
         choosez.append(1 ^ insufficientm.count)
      }
      while (2.21 < (3.37 - users6)) {
         users6 += Double(choosez.count)
         break
      }
      if insufficientm.count < 4 {
         users6 -= (Double(Int(users6 > 108782608.0 || users6 < -108782608.0 ? 52.0 : users6) & 3))
      }
          var indexw: Bool = false
          var editP: Float = 2.0
         withUnsafeMutablePointer(to: &editP) { pointer in
    
         }
         choosez.append(((indexw ? 3 : 2) % (Swift.max(Int(editP > 161903710.0 || editP < -161903710.0 ? 55.0 : editP), 2))))
      repeat {
         insufficientm.append((Int(users6 > 389475512.0 || users6 < -389475512.0 ? 48.0 : users6) ^ 2))
         if 4459277 == insufficientm.count {
            break
         }
      } while ((Int(users6 > 96498206.0 || users6 < -96498206.0 ? 43.0 : users6)) > insufficientm.count) && (4459277 == insufficientm.count)
      for _ in 0 ..< 3 {
         insufficientm = [insufficientm.count]
      }
       var homepageE: Int = 4
       _ = homepageE
         choosez.append(choosez.count)
          var statusT: Int = 5
          var introw: String! = String(cString: [101,103,111,108,111,109,98,0], encoding: .utf8)!
          var oldo: Double = 4.0
         homepageE |= insufficientm.count
         statusT -= statusT
         introw.append("\((Int(oldo > 130647671.0 || oldo < -130647671.0 ? 9.0 : oldo)))")
         oldo += Double(3)
      toupiC.append("\((Int(protocol_yi > 358977081.0 || protocol_yi < -358977081.0 ? 99.0 : protocol_yi) | 2))")
   while (4 <= (toupiC.count * 3) || (3 * toupiC.count) <= 5) {
       var photoF: String! = String(cString: [108,111,97,116,0], encoding: .utf8)!
       _ = photoF
       var defaults_: Float = 2.0
      while ((photoF.count + 1) < 4) {
         photoF.append("\((Int(defaults_ > 387560087.0 || defaults_ < -387560087.0 ? 16.0 : defaults_) << (Swift.min(labs(2), 4))))")
         break
      }
          var urls3: Bool = true
          var savee: String! = String(cString: [115,97,110,105,116,105,122,101,114,0], encoding: .utf8)!
          var secretv: String! = String(cString: [115,101,116,116,101,114,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &secretv) { pointer in
                _ = pointer.pointee
         }
         photoF.append("\(savee.count | 3)")
         urls3 = 88 > photoF.count
         secretv = "\((Int(defaults_ > 3788201.0 || defaults_ < -3788201.0 ? 99.0 : defaults_)))"
         defaults_ /= Swift.max(Float(2 + photoF.count), 1)
       var streamingE: String! = String(cString: [109,111,115,116,0], encoding: .utf8)!
       var baseU: Double = 2.0
      withUnsafeMutablePointer(to: &baseU) { pointer in
    
      }
      repeat {
         photoF.append("\(streamingE.count >> (Swift.min(5, photoF.count)))")
         if photoF.count == 2407837 {
            break
         }
      } while (photoF.count == 2407837) && (3.27 > (baseU / 3.40) && (Int(baseU > 157997254.0 || baseU < -157997254.0 ? 86.0 : baseU) / (Swift.max(photoF.count, 7))) > 3)
      safe2 = [1]
      break
   }
   return protocol_yi

}





    
    
    func showMusicConfirmAlert(title: String?,
                         message: String?,
                         confirmTitle: String = "Confirm",
                         cancelTitle: String = "Cancel",
                         confirmStyle: UIAlertAction.Style = .destructive,
                         onConfirm: (() -> Void)? = nil,
                         onCancel: (() -> Void)? = nil) {

         let globaltemFdec: Double = actualVideoDisplayPathMigration(voiceHomepage:true)

      if globaltemFdec != 50 {
             print(globaltemFdec)
      }

_ = globaltemFdec


       var removeI: String! = String(cString: [99,111,115,105,103,110,97,116,111,114,121,0], encoding: .utf8)!
    _ = removeI
   while (removeI.contains(removeI)) {
       var didd: String! = String(cString: [98,105,113,117,97,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &didd) { pointer in
             _ = pointer.pointee
      }
       var actionk: Float = 5.0
       var signI: String! = String(cString: [118,111,105,112,0], encoding: .utf8)!
      while (didd.count > (Int(actionk > 135410557.0 || actionk < -135410557.0 ? 31.0 : actionk))) {
         actionk += Float(1 - didd.count)
         break
      }
          var parameters_: Double = 2.0
         withUnsafeMutablePointer(to: &parameters_) { pointer in
    
         }
          var followingb: String! = String(cString: [97,98,103,114,0], encoding: .utf8)!
          var musice: String! = String(cString: [115,117,98,112,97,99,107,101,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &musice) { pointer in
                _ = pointer.pointee
         }
         signI.append("\((3 - Int(parameters_ > 261696739.0 || parameters_ < -261696739.0 ? 74.0 : parameters_)))")
         followingb.append("\((followingb == (String(cString:[53,0], encoding: .utf8)!) ? signI.count : followingb.count))")
         musice = "\(1)"
         didd.append("\((Int(actionk > 27887184.0 || actionk < -27887184.0 ? 81.0 : actionk)))")
         signI = "\((1 & Int(actionk > 109450720.0 || actionk < -109450720.0 ? 68.0 : actionk)))"
         actionk += (Float(didd == (String(cString:[119,0], encoding: .utf8)!) ? didd.count : signI.count))
      repeat {
         actionk /= Swift.max((Float(Int(actionk > 372863075.0 || actionk < -372863075.0 ? 72.0 : actionk) % 1)), 1)
         if 4671707.0 == actionk {
            break
         }
      } while (4671707.0 == actionk) && ((actionk / (Swift.max(8, Float(signI.count)))) == 4.39)
      if didd.contains(signI) {
          var screeno: Float = 4.0
         didd = "\((Int(actionk > 79448722.0 || actionk < -79448722.0 ? 99.0 : actionk) % 1))"
         screeno += (Float(Int(screeno > 292447170.0 || screeno < -292447170.0 ? 43.0 : screeno)))
      }
         actionk -= (Float(signI == (String(cString:[101,0], encoding: .utf8)!) ? Int(actionk > 43135645.0 || actionk < -43135645.0 ? 33.0 : actionk) : signI.count))
         signI = "\((Int(actionk > 355793597.0 || actionk < -355793597.0 ? 7.0 : actionk)))"
      removeI = "\(signI.count >> (Swift.min(labs(1), 3)))"
      break
   }

        let lines = UIAlertController(title: title, message: message, preferredStyle: .alert)
        lines.addAction(UIAlertAction(title: cancelTitle, style: .cancel) { _ in
            onCancel?()
        })
        lines.addAction(UIAlertAction(title: confirmTitle, style: confirmStyle) { _ in
            onConfirm?()
        })
        present(lines, animated: true)
    }

    
    
    func showMusicAlert(title: String?,
                   message: String?,
                   buttonTitle: String = "OK",
                   onTap: (() -> Void)? = nil) {
       var layoutC: [String: Any]! = [String(cString: [115,99,116,112,99,111,110,110,0], encoding: .utf8)!:59, String(cString: [115,105,109,112,108,101,116,105,109,101,111,117,116,0], encoding: .utf8)!:26, String(cString: [118,101,114,115,105,111,110,115,0], encoding: .utf8)!:89]
    var messager: [String: Any]! = [String(cString: [99,111,114,114,117,112,116,101,100,0], encoding: .utf8)!:58.0]
   while (layoutC.count == messager.values.count) {
       var cnewsd: [Any]! = [String(cString: [98,105,110,100,101,114,0], encoding: .utf8)!]
       _ = cnewsd
       var takem: Float = 1.0
       var packageU: Double = 2.0
       var videoW: String! = String(cString: [102,105,108,116,101,114,102,0], encoding: .utf8)!
      if 5 <= (Int(takem > 178219696.0 || takem < -178219696.0 ? 82.0 : takem) + cnewsd.count) || 4.42 <= (takem + Float(cnewsd.count)) {
          var max_gwu: Double = 0.0
          var calendard: String! = String(cString: [99,111,114,111,117,116,105,110,101,0], encoding: .utf8)!
          var mustg: String! = String(cString: [116,99,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &mustg) { pointer in
    
         }
         cnewsd = [(1 | Int(packageU > 312433500.0 || packageU < -312433500.0 ? 12.0 : packageU))]
         max_gwu -= (Double(mustg == (String(cString:[77,0], encoding: .utf8)!) ? Int(takem > 325926547.0 || takem < -325926547.0 ? 16.0 : takem) : mustg.count))
         calendard = "\(videoW.count)"
      }
      while ((Int(packageU > 53202542.0 || packageU < -53202542.0 ? 55.0 : packageU) - videoW.count) == 4 && (4 - Int(packageU > 334003634.0 || packageU < -334003634.0 ? 56.0 : packageU)) == 1) {
         packageU -= Double(3)
         break
      }
      repeat {
          var chat7: Double = 4.0
          var navigation4: Double = 3.0
          _ = navigation4
          var selected9: [Any]! = [String(cString: [115,116,114,116,111,100,0], encoding: .utf8)!, String(cString: [97,112,105,115,0], encoding: .utf8)!]
         videoW.append("\((Int(takem > 40082154.0 || takem < -40082154.0 ? 78.0 : takem) >> (Swift.min(4, labs(2)))))")
         chat7 -= (Double(Int(chat7 > 61314339.0 || chat7 < -61314339.0 ? 73.0 : chat7) & 3))
         navigation4 -= Double(2)
         selected9 = [1]
         if videoW.count == 937795 {
            break
         }
      } while (cnewsd.count > videoW.count) && (videoW.count == 937795)
       var s_managery: Double = 5.0
       var violent1: Double = 1.0
      for _ in 0 ..< 3 {
         cnewsd.append(videoW.count & 1)
      }
      repeat {
         packageU /= Swift.max(1, (Double(Int(violent1 > 61874541.0 || violent1 < -61874541.0 ? 84.0 : violent1))))
         if 1209185.0 == packageU {
            break
         }
      } while (3.100 == packageU) && (1209185.0 == packageU)
          var reportedK: String! = String(cString: [105,100,99,116,99,111,108,0], encoding: .utf8)!
          var mnew_oD: String! = String(cString: [99,105,110,116,0], encoding: .utf8)!
          var directoryB: String! = String(cString: [117,110,115,97,118,101,0], encoding: .utf8)!
         takem -= Float(mnew_oD.count)
         reportedK = "\((Int(takem > 288618836.0 || takem < -288618836.0 ? 41.0 : takem) + 3))"
         directoryB = "\(2)"
      repeat {
         packageU /= Swift.max((Double((String(cString:[48,0], encoding: .utf8)!) == videoW ? Int(violent1 > 309257625.0 || violent1 < -309257625.0 ? 47.0 : violent1) : videoW.count)), 3)
         if 2627667.0 == packageU {
            break
         }
      } while (packageU <= Double(takem)) && (2627667.0 == packageU)
         s_managery += Double(3 * cnewsd.count)
      messager["\(takem)"] = (Int(takem > 158042624.0 || takem < -158042624.0 ? 15.0 : takem))
      break
   }

      layoutC = ["\(messager.values.count)": 3 << (Swift.min(3, layoutC.count))]
        let lines = UIAlertController(title: title, message: message, preferredStyle: .alert)
        lines.addAction(UIAlertAction(title: buttonTitle, style: .default) { _ in
            onTap?()
        })
        present(lines, animated: true)
    }

@discardableResult
 func originalButtonCompletionView() -> UIView! {
    var pornographyQ: String! = String(cString: [116,114,117,110,99,97,116,105,111,110,0], encoding: .utf8)!
    var client_: Double = 3.0
   for _ in 0 ..< 1 {
      client_ /= Swift.max(Double(1), 3)
   }
      client_ /= Swift.max(Double(pornographyQ.count - 3), 2)
     let dictTake: UILabel! = UILabel(frame:CGRect.zero)
     var itemsHot: UIImageView! = UIImageView(image:UIImage(named:String(cString: [99,104,97,114,116,0], encoding: .utf8)!))
     var rowMethod_le: [Any]! = [56, 44, 73]
    var reloaderNppscaleNeeded: UIView! = UIView(frame:CGRect(x: 305, y: 283, width: 0, height: 0))
    reloaderNppscaleNeeded.alpha = 0.1;
    reloaderNppscaleNeeded.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    reloaderNppscaleNeeded.frame = CGRect(x: 125, y: 85, width: 0, height: 0)
    dictTake.frame = CGRect(x: 144, y: 312, width: 0, height: 0)
    dictTake.alpha = 0.6;
    dictTake.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dictTake.textAlignment = .center
    dictTake.font = UIFont.systemFont(ofSize:20)
    dictTake.text = ""
    dictTake.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    reloaderNppscaleNeeded.addSubview(dictTake)
    itemsHot.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    itemsHot.alpha = 0.0
    itemsHot.frame = CGRect(x: 263, y: 14, width: 0, height: 0)
    itemsHot.image = UIImage(named:String(cString: [104,101,97,100,101,114,115,0], encoding: .utf8)!)
    itemsHot.contentMode = .scaleAspectFit
    itemsHot.animationRepeatCount = 1
    
    reloaderNppscaleNeeded.addSubview(itemsHot)

    
    return reloaderNppscaleNeeded

}





    
    func vook_leaveBlockedUserPageIfNeeded() {

         var candidateTint: UIView! = originalButtonCompletionView()

      if candidateTint != nil {
          let candidateTint_tag = candidateTint.tag
          self.view.addSubview(candidateTint)
      }

withUnsafeMutablePointer(to: &candidateTint) { pointer in
    
}


       var applicationa: String! = String(cString: [100,101,110,111,114,109,97,108,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      applicationa = "\(2 ^ applicationa.count)"
   }

        if self is TWEListRechController || self is CJRechController || self is HQChatController || self is TTYCommentDelegateController {
            navigationController?.popToRootViewController(animated: true)
        }
    }
}
