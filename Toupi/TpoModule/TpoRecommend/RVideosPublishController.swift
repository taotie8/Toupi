
import Foundation

import UIKit

class RVideosPublishController: JZPopupController {
private var name_margin: Float? = 0.0
private var followerHttpStr: String!
private var append_space: Float? = 0.0
var heightBlack_arr: [Any]!




    @IBOutlet weak var collectionView: UICollectionView!
    var text_list: [STUResponsePopup] = []

@discardableResult
 func horizontalActivitySupport(sceneViolent: Float, migrationBase: Float, fileRecommend: Double) -> String! {
    var containerv: Bool = true
    var base6: Bool = false
    var lyrics3: String! = String(cString: [97,108,112,110,0], encoding: .utf8)!
      lyrics3.append("\(((base6 ? 2 : 2)))")
   while (!containerv) {
       var editing7: Int = 2
       _ = editing7
       var httpi: [Any]! = [true]
       _ = httpi
         editing7 -= editing7 % (Swift.max(3, httpi.count))
      for _ in 0 ..< 2 {
          var scalarR: Float = 3.0
          _ = scalarR
          var buttonsX: String! = String(cString: [114,103,98,97,121,99,111,99,103,0], encoding: .utf8)!
         editing7 ^= editing7 - httpi.count
         scalarR -= Float(editing7 ^ 3)
         buttonsX = "\(3 % (Swift.max(7, httpi.count)))"
      }
      while (httpi.count == editing7) {
          var followsO: Double = 1.0
         withUnsafeMutablePointer(to: &followsO) { pointer in
                _ = pointer.pointee
         }
          var register_hl: String! = String(cString: [114,101,115,116,111,114,101,0], encoding: .utf8)!
          var streaming3: String! = String(cString: [97,97,115,99,0], encoding: .utf8)!
          _ = streaming3
         httpi = [streaming3.count * httpi.count]
         followsO -= Double(register_hl.count)
         register_hl.append("\(httpi.count)")
         break
      }
      while (editing7 >= httpi.count) {
          var woman_: Double = 4.0
          var postx: [Any]! = [38.0]
          var delegate_qt: Bool = true
          _ = delegate_qt
          var entityg: String! = String(cString: [99,104,101,99,107,112,97,99,107,101,116,0], encoding: .utf8)!
          _ = entityg
         httpi.append(3)
         woman_ /= Swift.max((Double(Int(woman_ > 112324417.0 || woman_ < -112324417.0 ? 7.0 : woman_) & 2)), 1)
         postx.append(1 + editing7)
         entityg = "\((Int(woman_ > 289882158.0 || woman_ < -289882158.0 ? 71.0 : woman_)))"
         break
      }
          var eulaI: String! = String(cString: [116,105,109,101,115,116,97,109,112,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &eulaI) { pointer in
                _ = pointer.pointee
         }
          var streamS: String! = String(cString: [104,97,115,0], encoding: .utf8)!
          var pictureG: Float = 1.0
         withUnsafeMutablePointer(to: &pictureG) { pointer in
    
         }
         editing7 &= eulaI.count | editing7
         streamS.append("\(2)")
         pictureG -= Float(editing7 << (Swift.min(httpi.count, 5)))
      repeat {
          var dynamic_2G: String! = String(cString: [116,112,101,108,100,115,112,0], encoding: .utf8)!
          _ = dynamic_2G
          var firstc: Double = 4.0
          var lastO: Bool = false
         httpi = [1]
         dynamic_2G = "\((Int(firstc > 145868315.0 || firstc < -145868315.0 ? 32.0 : firstc)))"
         firstc -= Double(3 % (Swift.max(9, httpi.count)))
         lastO = dynamic_2G.count >= 90
         if 1846877 == httpi.count {
            break
         }
      } while (httpi.count >= editing7) && (1846877 == httpi.count)
      base6 = (((!base6 ? 29 : httpi.count) * httpi.count) > 29)
      break
   }
   for _ in 0 ..< 3 {
      containerv = !containerv || base6
   }
   return lyrics3

}





    
    @objc private func vook_reloadVisiblePosts() {

         var updateOptimum: String! = horizontalActivitySupport(sceneViolent:87.0, migrationBase:21.0, fileRecommend:40.0)

      print(updateOptimum)
      let updateOptimum_len = updateOptimum?.count ?? 0

withUnsafeMutablePointer(to: &updateOptimum) { pointer in
        _ = pointer.pointee
}


       var with_ra: Float = 4.0
   for _ in 0 ..< 1 {
      with_ra -= (Float(Int(with_ra > 357549999.0 || with_ra < -357549999.0 ? 63.0 : with_ra)))
   }

        getCurrentTextLists()
    }
    
    deinit {
        NotificationCenter.default.removeObserver(self)
    }

    
    func setup_collectionView() {
       var containsa: [String: Any]! = [String(cString: [112,111,115,105,116,105,111,110,0], encoding: .utf8)!:78, String(cString: [114,101,109,97,112,112,105,110,103,0], encoding: .utf8)!:13]
      containsa["\(containsa.count)"] = containsa.values.count + 1

        let users = UICollectionViewFlowLayout()
        users.minimumLineSpacing = 20
        users.minimumInteritemSpacing = 20
        users.scrollDirection = .vertical
        users.sectionInset = UIEdgeInsets(top: 20, left: 17, bottom: 20, right: 17)
        users.itemSize = CGSizeMake(UIScreen.main.bounds.width - 34, 343)
        
        collectionView.collectionViewLayout = users
        collectionView.dataSource = self
        collectionView.delegate = self
        collectionView.backgroundColor = .clear
        collectionView.register(UINib(nibName: "FRequstCell", bundle: nil), forCellWithReuseIdentifier: "textPhoto")
    }

@discardableResult
 func leadingEntityConfirmButton(bothSetup: Bool) -> [Any]! {
    var code3: Float = 2.0
    var usersk: Bool = true
    _ = usersk
    var nickJ: [Any]! = [54, 65, 28]
      code3 -= (Float(Int(code3 > 34253976.0 || code3 < -34253976.0 ? 44.0 : code3) & (usersk ? 5 : 3)))
   for _ in 0 ..< 1 {
      code3 -= (Float((usersk ? 3 : 2) % (Swift.max(Int(code3 > 392653724.0 || code3 < -392653724.0 ? 75.0 : code3), 6))))
   }
      code3 += Float(2 + nickJ.count)
   return nickJ

}





    
    override func viewWillAppear(_ animated: Bool) {

         var fintPcmb: [Any]! = leadingEntityConfirmButton(bothSetup:true)

      let fintPcmb_len = fintPcmb.count
      fintPcmb.enumerated().forEach({ (index,obj) in
          if index  >=  20 {
                print(obj)
          }
      })

withUnsafeMutablePointer(to: &fintPcmb) { pointer in
    
}


       var blacksd: Double = 5.0
   while ((blacksd + 5.29) <= 5.33 && (blacksd + 5.29) <= 5.40) {
      blacksd /= Swift.max(2, Double(1))
      break
   }

        super.viewWillAppear(animated)
        navigationController?.isNavigationBarHidden = true
        getCurrentTextLists()
    }

    
    func getCurrentTextLists() {
       var purchaseE: Int = 2
    var otherl: Double = 1.0
    _ = otherl
      purchaseE >>= Swift.min(1, labs((3 ^ Int(otherl > 117919104.0 || otherl < -117919104.0 ? 84.0 : otherl))))

      otherl /= Swift.max(2, (Double(Int(otherl > 373151722.0 || otherl < -373151722.0 ? 23.0 : otherl) + purchaseE)))
        text_list = MSPList.shared.vook_VisibleTextPosts()
        collectionView.reloadData()
    }

@discardableResult
 func nowBigMediaTouchVerifyGenerator(reportedNew_ar: Double, closeReported: [Any]!) -> Double {
    var h_manager6: String! = String(cString: [98,114,101,97,107,112,111,105,110,116,0], encoding: .utf8)!
    var picker0: Bool = false
   withUnsafeMutablePointer(to: &picker0) { pointer in
    
   }
    var method_u4: Double = 2.0
      h_manager6.append("\((2 / (Swift.max(Int(method_u4 > 41072440.0 || method_u4 < -41072440.0 ? 79.0 : method_u4), 10))))")
      picker0 = h_manager6.count <= 56
       var creator0: Float = 2.0
      if creator0 > 3.38 {
         creator0 /= Swift.max(1, (Float(Int(creator0 > 292984927.0 || creator0 < -292984927.0 ? 84.0 : creator0))))
      }
      if 4.4 > (4.25 * creator0) && 4.94 > (4.25 * creator0) {
         creator0 -= (Float(Int(creator0 > 146838515.0 || creator0 < -146838515.0 ? 27.0 : creator0) << (Swift.min(4, labs(1)))))
      }
      repeat {
         creator0 -= Float(1)
         if 4053296.0 == creator0 {
            break
         }
      } while (4053296.0 == creator0) && (4.55 > (5.70 / (Swift.max(2, creator0))) || (creator0 / (Swift.max(5.70, 8))) > 4.49)
      picker0 = creator0 >= Float(method_u4)
   return method_u4

}





    override func viewDidLoad() {

         var decodingMsg: Double = nowBigMediaTouchVerifyGenerator(reportedNew_ar:74.0, closeReported:[13, 67, 58])

      if decodingMsg > 45 {
             print(decodingMsg)
      }

withUnsafeMutablePointer(to: &decodingMsg) { pointer in
    
}


       var completer: Float = 4.0
   withUnsafeMutablePointer(to: &completer) { pointer in
          _ = pointer.pointee
   }
   if completer < 3.9 {
      completer /= Swift.max((Float(Int(completer > 243648295.0 || completer < -243648295.0 ? 55.0 : completer) >> (Swift.min(3, labs(Int(completer > 331425461.0 || completer < -331425461.0 ? 93.0 : completer)))))), 3)
   }

        super.viewDidLoad()
        setup_collectionView()
        NotificationCenter.default.addObserver(self,
                                               selector: #selector(vook_reloadVisiblePosts),
                                               name: UWProfileNews.vook_refreshNotification,
                                               object: nil)
        getCurrentTextLists()
    }
}

extension RVideosPublishController: UICollectionViewDelegate, UICollectionViewDataSource, UICollectionViewDelegateFlowLayout {

@discardableResult
 func tablePriceSinceBlack(playHeader: Double, prefix_tCount: [String: Any]!) -> Bool {
    var firstW: Double = 0.0
    var vookw: String! = String(cString: [99,97,118,115,105,100,99,116,0], encoding: .utf8)!
    _ = vookw
    var spacingG: Bool = false
       var passwordw: Float = 5.0
         passwordw += (Float(Int(passwordw > 240854079.0 || passwordw < -240854079.0 ? 42.0 : passwordw) & Int(passwordw > 34110316.0 || passwordw < -34110316.0 ? 19.0 : passwordw)))
         passwordw += (Float(Int(passwordw > 271244754.0 || passwordw < -271244754.0 ? 57.0 : passwordw)))
         passwordw -= (Float(Int(passwordw > 186160490.0 || passwordw < -186160490.0 ? 90.0 : passwordw) | Int(passwordw > 170240307.0 || passwordw < -170240307.0 ? 59.0 : passwordw)))
      vookw.append("\(((spacingG ? 2 : 1) & vookw.count))")
   repeat {
      vookw = "\((vookw == (String(cString:[106,0], encoding: .utf8)!) ? vookw.count : Int(firstW > 123438239.0 || firstW < -123438239.0 ? 85.0 : firstW)))"
      if vookw == (String(cString:[122,97,121,107,0], encoding: .utf8)!) {
         break
      }
   } while (vookw == (String(cString:[122,97,121,107,0], encoding: .utf8)!)) && (!spacingG)
      firstW /= Swift.max((Double(Int(firstW > 239040299.0 || firstW < -239040299.0 ? 30.0 : firstW))), 5)
   return spacingG

}





    func collectionView(_ collectionView: UICollectionView, numberOfItemsInSection section: Int) -> Int {

         var voicemailFtvmnode: Bool = tablePriceSinceBlack(playHeader:19.0, prefix_tCount:[String(cString: [115,101,115,115,105,111,110,115,0], encoding: .utf8)!:65, String(cString: [111,114,116,104,111,103,111,110,97,108,105,122,101,0], encoding: .utf8)!:75, String(cString: [108,97,121,101,114,115,0], encoding: .utf8)!:64])

      if voicemailFtvmnode {
      }

withUnsafeMutablePointer(to: &voicemailFtvmnode) { pointer in
        _ = pointer.pointee
}


       var insetsv: Double = 0.0
    _ = insetsv
   for _ in 0 ..< 3 {
       var e_heightv: Int = 0
         e_heightv %= Swift.max(4, e_heightv % 1)
         e_heightv /= Swift.max(4, e_heightv)
      while (e_heightv > e_heightv) {
         e_heightv >>= Swift.min(labs(e_heightv), 1)
         break
      }
      insetsv /= Swift.max((Double(Int(insetsv > 353637007.0 || insetsv < -353637007.0 ? 84.0 : insetsv))), 1)
   }

        return text_list.count
    }

@discardableResult
 func runAppearSignSectionMarginParent() -> Double {
    var directory6: [String: Any]! = [String(cString: [115,117,98,112,97,116,104,115,0], encoding: .utf8)!:99, String(cString: [117,110,97,118,97,105,108,97,98,108,101,0], encoding: .utf8)!:94]
    var productq: Double = 4.0
   withUnsafeMutablePointer(to: &productq) { pointer in
          _ = pointer.pointee
   }
   while (!directory6.keys.contains("\(productq)")) {
       var filedo: String! = String(cString: [114,101,97,115,111,110,0], encoding: .utf8)!
       var receives: Double = 2.0
       var bottom9: Double = 3.0
      withUnsafeMutablePointer(to: &bottom9) { pointer in
    
      }
       var sourceA: Float = 4.0
       var recommendh: String! = String(cString: [108,101,114,116,0], encoding: .utf8)!
       var taskq: String! = String(cString: [115,105,109,112,108,101,0], encoding: .utf8)!
       var delegate_sC: String! = String(cString: [105,110,105,116,104,0], encoding: .utf8)!
       _ = delegate_sC
       var size_qI: Double = 2.0
       var tap_: Double = 0.0
         bottom9 *= Double(delegate_sC.count)
          var packageM: [Any]! = [96.0]
          var followerV: [String: Any]! = [String(cString: [99,111,110,115,105,115,116,101,110,99,121,0], encoding: .utf8)!:28, String(cString: [99,95,48,95,103,114,97,110,117,108,101,0], encoding: .utf8)!:37, String(cString: [119,97,108,108,112,97,112,101,114,115,0], encoding: .utf8)!:71]
         delegate_sC.append("\(delegate_sC.count)")
         packageM = [1 * filedo.count]
         followerV = ["\(followerV.values.count)": (Int(sourceA > 344939424.0 || sourceA < -344939424.0 ? 22.0 : sourceA))]
      for _ in 0 ..< 1 {
         tap_ /= Swift.max(2, (Double(Int(size_qI > 11691671.0 || size_qI < -11691671.0 ? 34.0 : size_qI))))
      }
       var p_playerM: String! = String(cString: [106,95,56,55,95,122,101,114,111,115,0], encoding: .utf8)!
       var clientg: String! = String(cString: [101,120,116,114,97,100,97,116,97,112,115,101,116,115,0], encoding: .utf8)!
         tap_ += (Double(Int(size_qI > 153229694.0 || size_qI < -153229694.0 ? 24.0 : size_qI)))
         receives /= Swift.max(1, (Double(recommendh == (String(cString:[83,0], encoding: .utf8)!) ? Int(tap_ > 351057065.0 || tap_ < -351057065.0 ? 46.0 : tap_) : recommendh.count)))
      if 5.79 >= receives {
          var hotx: String! = String(cString: [114,101,112,111,0], encoding: .utf8)!
          var notificationy: Bool = true
          _ = notificationy
          var recordh: Int = 3
         withUnsafeMutablePointer(to: &recordh) { pointer in
                _ = pointer.pointee
         }
          var urla: Float = 1.0
         receives += (Double(Int(bottom9 > 255773927.0 || bottom9 < -255773927.0 ? 87.0 : bottom9) / (Swift.max(3, (notificationy ? 1 : 3)))))
         hotx = "\((recommendh == (String(cString:[90,0], encoding: .utf8)!) ? Int(size_qI > 144988789.0 || size_qI < -144988789.0 ? 57.0 : size_qI) : recommendh.count))"
         recordh %= Swift.max((Int(tap_ > 83211739.0 || tap_ < -83211739.0 ? 41.0 : tap_) & (notificationy ? 5 : 1)), 2)
         urla -= Float(taskq.count * 3)
      }
       var nicknameT: Bool = false
       var packagesV: Bool = true
      for _ in 0 ..< 2 {
          var slotd: [String: Any]! = [String(cString: [104,111,116,105,122,111,110,116,108,0], encoding: .utf8)!:13, String(cString: [110,111,99,104,97,110,103,101,0], encoding: .utf8)!:72]
          var inputH: String! = String(cString: [118,97,114,121,95,114,95,55,53,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &inputH) { pointer in
                _ = pointer.pointee
         }
          var prefix_sll: String! = String(cString: [117,110,98,108,117,114,0], encoding: .utf8)!
          var horizontalg: String! = String(cString: [116,114,97,110,115,105,101,110,116,0], encoding: .utf8)!
          var audiod: Float = 4.0
         clientg = "\((p_playerM == (String(cString:[114,0], encoding: .utf8)!) ? p_playerM.count : (nicknameT ? 1 : 2)))"
         slotd = [filedo: (Int(tap_ > 153075496.0 || tap_ < -153075496.0 ? 19.0 : tap_) % 2)]
         inputH = "\((Int(sourceA > 128046892.0 || sourceA < -128046892.0 ? 6.0 : sourceA)))"
         prefix_sll.append("\((Int(receives > 120070080.0 || receives < -120070080.0 ? 5.0 : receives) - 1))")
         horizontalg.append("\(p_playerM.count)")
         audiod += Float(1)
      }
         delegate_sC.append("\((1 / (Swift.max(7, Int(receives > 161406400.0 || receives < -161406400.0 ? 6.0 : receives)))))")
      repeat {
          var trailing8: String! = String(cString: [102,111,99,117,115,0], encoding: .utf8)!
          var numberC: String! = String(cString: [116,95,57,52,95,115,101,101,0], encoding: .utf8)!
          var detailsh: String! = String(cString: [104,95,56,54,95,110,111,100,101,115,101,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &detailsh) { pointer in
    
         }
          var messagez: [Any]! = [String(cString: [100,105,115,99,111,110,110,101,99,116,101,100,0], encoding: .utf8)!, String(cString: [105,105,114,102,105,108,116,101,114,0], encoding: .utf8)!, String(cString: [97,110,97,108,121,122,105,110,103,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &messagez) { pointer in
    
         }
         delegate_sC = "\((Int(tap_ > 96877360.0 || tap_ < -96877360.0 ? 32.0 : tap_) % 2))"
         trailing8.append("\(numberC.count)")
         numberC.append("\((filedo == (String(cString:[118,0], encoding: .utf8)!) ? filedo.count : Int(receives > 292265482.0 || receives < -292265482.0 ? 70.0 : receives)))")
         detailsh.append("\(2)")
         messagez.append((Int(size_qI > 197958023.0 || size_qI < -197958023.0 ? 6.0 : size_qI)))
         if delegate_sC.count == 210041 {
            break
         }
      } while (2 > taskq.count) && (delegate_sC.count == 210041)
          var packages2: String! = String(cString: [114,101,97,100,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &packages2) { pointer in
    
         }
         sourceA += (Float((packagesV ? 5 : 2)))
         packages2.append("\((Int(tap_ > 352372393.0 || tap_ < -352372393.0 ? 33.0 : tap_) ^ 2))")
         p_playerM = "\((Int(tap_ > 266146260.0 || tap_ < -266146260.0 ? 11.0 : tap_) ^ recommendh.count))"
      directory6[filedo] = (Int(sourceA > 56129351.0 || sourceA < -56129351.0 ? 39.0 : sourceA) << (Swift.min(filedo.count, 2)))
      break
   }
     let contentBytes: Double = 51.0
     let centerStr: Bool = true
    var pixletTablec:Double = 0
    pixletTablec *= Double(contentBytes)
    pixletTablec -= Double(centerStr ? 41 : 38)

    return pixletTablec

}





    
    func collectionView(_ collectionView: UICollectionView, cellForItemAt indexPath: IndexPath) -> UICollectionViewCell {

         var dividendI_37: Double = runAppearSignSectionMarginParent()

      print(dividendI_37)

withUnsafeMutablePointer(to: &dividendI_37) { pointer in
        _ = pointer.pointee
}


       var delete_bjR: String! = String(cString: [101,109,111,106,105,115,0], encoding: .utf8)!
      delete_bjR = "\(delete_bjR.count)"

        let arrayCell = collectionView.dequeueReusableCell(withReuseIdentifier: "textPhoto", for: indexPath) as! FRequstCell
        arrayCell.backgroundColor = .clear
        arrayCell.updateTextCellViews(data: text_list[indexPath.item])
        arrayCell.photo_text_user_center_tap.tag = 1000 + indexPath.row
        arrayCell.photo_text_like_tap.tag = 2000 + indexPath.row
        arrayCell.photo_text_user_follow_tap.tag = 3000 + indexPath.row
        arrayCell.photo_text_right_tap.tag = 4000 + indexPath.row
        arrayCell.photo_text_user_center_tap.addTarget(self, action: #selector(enterUserHomePageController(_ :)), for: .touchUpInside)
        arrayCell.photo_text_like_tap.addTarget(self, action: #selector(enterUserHomePageController(_ :)), for: .touchUpInside)
        arrayCell.photo_text_user_follow_tap.addTarget(self, action: #selector(enterUserHomePageController(_ :)), for: .touchUpInside)
        arrayCell.photo_text_right_tap.addTarget(self, action: #selector(enterUserHomePageController(_ :)), for: .touchUpInside)
        
        return arrayCell
    }

    
    @objc func enterUserHomePageController(_ sender: UIButton) {
       var tests: [String: Any]! = [String(cString: [109,97,105,110,104,101,97,100,101,114,0], encoding: .utf8)!:17, String(cString: [114,101,115,104,117,102,102,108,101,0], encoding: .utf8)!:13]
    var znewsN: Float = 2.0
   withUnsafeMutablePointer(to: &znewsN) { pointer in
    
   }
       var likesP: String! = String(cString: [103,101,110,101,114,97,116,105,111,110,0], encoding: .utf8)!
         likesP = "\(((String(cString:[118,0], encoding: .utf8)!) == likesP ? likesP.count : likesP.count))"
         likesP.append("\((likesP == (String(cString:[95,0], encoding: .utf8)!) ? likesP.count : likesP.count))")
      while (likesP.count < likesP.count) {
         likesP.append("\(likesP.count / (Swift.max(1, 10)))")
         break
      }
      znewsN -= Float(tests.count)

   repeat {
       var rightj: String! = String(cString: [102,114,97,109,101,98,117,102,102,101,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &rightj) { pointer in
    
      }
       var chatA: String! = String(cString: [114,101,103,105,115,116,101,114,102,100,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &chatA) { pointer in
             _ = pointer.pointee
      }
       var labelF: Float = 0.0
      withUnsafeMutablePointer(to: &labelF) { pointer in
             _ = pointer.pointee
      }
      while (4 < (Int(labelF > 85159558.0 || labelF < -85159558.0 ? 55.0 : labelF) * rightj.count) || 4 < (rightj.count * Int(labelF > 254600420.0 || labelF < -254600420.0 ? 81.0 : labelF))) {
         labelF -= (Float(Int(labelF > 229051861.0 || labelF < -229051861.0 ? 36.0 : labelF) | chatA.count))
         break
      }
       var main_l3: String! = String(cString: [105,110,116,101,114,114,117,112,116,105,98,108,101,0], encoding: .utf8)!
       var settingsD: String! = String(cString: [109,117,116,97,98,108,101,0], encoding: .utf8)!
      while (rightj == String(cString:[89,0], encoding: .utf8)!) {
         chatA.append("\((Int(labelF > 304070863.0 || labelF < -304070863.0 ? 35.0 : labelF) << (Swift.min(labs(3), 3))))")
         break
      }
         rightj.append("\((Int(labelF > 255491354.0 || labelF < -255491354.0 ? 13.0 : labelF)))")
         rightj = "\(1 % (Swift.max(7, rightj.count)))"
         settingsD.append("\(main_l3.count * chatA.count)")
         rightj = "\(3)"
       var viewsu: [Any]! = [String(cString: [115,101,118,101,110,0], encoding: .utf8)!, String(cString: [97,117,116,104,107,101,121,0], encoding: .utf8)!, String(cString: [118,111,105,99,101,109,97,105,108,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &viewsu) { pointer in
             _ = pointer.pointee
      }
       var adjustd: [Any]! = [String(cString: [108,117,116,114,103,98,0], encoding: .utf8)!, String(cString: [112,114,101,105,118,111,117,115,0], encoding: .utf8)!]
          var strQ: [String: Any]! = [String(cString: [108,111,111,112,98,97,99,107,0], encoding: .utf8)!:39, String(cString: [102,105,116,0], encoding: .utf8)!:93]
         withUnsafeMutablePointer(to: &strQ) { pointer in
    
         }
          var controller0: Int = 3
          var reportedc: String! = String(cString: [114,101,100,0], encoding: .utf8)!
         viewsu.append(controller0)
         strQ = ["\(controller0)": chatA.count]
         reportedc = "\(adjustd.count)"
      znewsN /= Swift.max(4, (Float(2 << (Swift.min(5, labs(Int(labelF > 302770201.0 || labelF < -302770201.0 ? 34.0 : labelF)))))))
      if znewsN == 1708325.0 {
         break
      }
   } while (znewsN == 1708325.0) && (tests.values.contains { $0 as? Float == znewsN })
        let othersItem = sender.tag
        
        if othersItem >= 4000 { 
            let did = othersItem - 4000
            let constraints = MSPList.shared.vook_VisibleTextPosts()
            guard did < constraints.count else { return }
            let navigationS = constraints[did]
            IIVVookChatView.showBothUsersMoreView(userId: navigationS.publishUserId)
        }
        else if othersItem >= 3000 { 
            let did = othersItem - 3000
            let constraints = MSPList.shared.vook_VisibleTextPosts()
            guard did < constraints.count else { return }
            let navigationS = constraints[did]
            MSPList.shared.vook_ToggleFollowUser(vook_targetUserId: navigationS.publishUserId) { [weak self] _ in
                self?.collectionView.reloadData()
            }
            collectionView.reloadData()
        }
        else if othersItem >= 2000 { 
            let did = othersItem - 2000
            let constraints = MSPList.shared.vook_VisibleTextPosts()
            guard did < constraints.count else { return }
            let navigationS = constraints[did]
            MSPList.shared.vook_ToggleLikePost(vook_postId: navigationS.postId) { [weak self] _ in
                self?.collectionView.reloadData()
            }
            collectionView.reloadData()
        }
        else if othersItem >= 1000 {
            let did = othersItem - 1000
            let constraints = MSPList.shared.vook_VisibleTextPosts()
            guard did < constraints.count else { return }
            let navigationS = constraints[did]
            let insetController = TWEListRechController()
            insetController.vook_userId = navigationS.publishUserId
            insetController.hidesBottomBarWhenPushed = true
            self.navigationController?.pushViewController(insetController, animated: true)
        }
    }

@discardableResult
 func customShareBehavior(generatorHost: String!) -> [Any]! {
    var willP: Double = 2.0
   withUnsafeMutablePointer(to: &willP) { pointer in
          _ = pointer.pointee
   }
    var streamd: Double = 3.0
    var enable_3s: [Any]! = [[90, 64, 23]]
    _ = enable_3s
       var clickq: Double = 2.0
       var more2: [Any]! = [46, 33]
       _ = more2
       var others2: Float = 4.0
      while ((more2.count + 3) < 1 && 5.31 < (others2 - 2.38)) {
         others2 += (Float(Int(clickq > 245262613.0 || clickq < -245262613.0 ? 81.0 : clickq)))
         break
      }
      if more2.count >= 1 {
         more2 = [1]
      }
          var body6: String! = String(cString: [102,116,118,112,108,97,115,116,110,111,100,101,0], encoding: .utf8)!
          var startR: String! = String(cString: [114,101,105,115,115,117,101,0], encoding: .utf8)!
          var user7: String! = String(cString: [111,102,102,105,99,105,97,108,0], encoding: .utf8)!
         more2 = [(startR == (String(cString:[113,0], encoding: .utf8)!) ? startR.count : body6.count)]
         user7.append("\(startR.count & body6.count)")
         more2.append((1 ^ Int(others2 > 381078538.0 || others2 < -381078538.0 ? 63.0 : others2)))
      while (4.22 >= (clickq + 1.57)) {
         clickq -= (Double(Int(clickq > 92979521.0 || clickq < -92979521.0 ? 14.0 : clickq)))
         break
      }
         clickq *= Double(1 & more2.count)
         more2 = [(Int(others2 > 49194540.0 || others2 < -49194540.0 ? 28.0 : others2))]
         clickq /= Swift.max(2, (Double(3 ^ Int(clickq > 248461750.0 || clickq < -248461750.0 ? 50.0 : clickq))))
      for _ in 0 ..< 2 {
          var configuration_: Double = 1.0
          _ = configuration_
         clickq += Double(2 * more2.count)
         configuration_ -= Double(2)
      }
      streamd += (Double(Int(willP > 148915421.0 || willP < -148915421.0 ? 89.0 : willP) / 2))
      willP /= Swift.max(4, (Double(enable_3s.count << (Swift.min(1, labs(Int(streamd > 198229006.0 || streamd < -198229006.0 ? 67.0 : streamd)))))))
   return enable_3s

}





    
    func collectionView(_ collectionView: UICollectionView, didSelectItemAt indexPath: IndexPath) {

         let placemarksIndentation: [Any]! = customShareBehavior(generatorHost:String(cString: [104,109,97,99,108,105,115,116,0], encoding: .utf8)!)

      let placemarksIndentation_len = placemarksIndentation.count
      placemarksIndentation.forEach({ (obj) in
          print(obj)
      })

_ = placemarksIndentation


       var avatarx: Int = 3
       var h_heightd: Int = 0
       var icon6: [Any]! = [82, 99, 52]
      withUnsafeMutablePointer(to: &icon6) { pointer in
    
      }
       var custom9: String! = String(cString: [110,111,116,97,116,105,111,110,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &custom9) { pointer in
             _ = pointer.pointee
      }
         icon6 = [2]
      if (icon6.count & 2) > 2 && 2 > (custom9.count & icon6.count) {
         custom9 = "\(custom9.count)"
      }
       var blocksz: [String: Any]! = [String(cString: [119,112,116,104,114,101,97,100,115,0], encoding: .utf8)!:60, String(cString: [110,105,99,101,0], encoding: .utf8)!:89, String(cString: [110,105,107,111,110,0], encoding: .utf8)!:83]
         icon6 = [(custom9 == (String(cString:[103,0], encoding: .utf8)!) ? custom9.count : h_heightd)]
      while (!custom9.hasPrefix("\(icon6.count)")) {
         custom9.append("\(1 << (Swift.min(2, custom9.count)))")
         break
      }
         blocksz["\(custom9)"] = 3
      repeat {
         h_heightd -= icon6.count
         if h_heightd == 2905226 {
            break
         }
      } while (5 <= (icon6.count << (Swift.min(4, labs(h_heightd))))) && (h_heightd == 2905226)
          var model6: [Any]! = [58, 69]
         withUnsafeMutablePointer(to: &model6) { pointer in
    
         }
         h_heightd &= 3 - h_heightd
         model6.append(3 ^ blocksz.values.count)
      while ((blocksz.values.count * icon6.count) < 1 && 1 < (icon6.count * 1)) {
         blocksz = ["\(h_heightd)": 2 - custom9.count]
         break
      }
      avatarx ^= icon6.count - custom9.count

        let headerController = HQChatController()
        headerController.vook_postData = text_list[indexPath.item]
        navigationController?.pushViewController(headerController, animated: true)
    }
}
