
import Foundation

import UIKit
import AVFoundation

class MTextResponseController: JZPopupController {
private var launchPopoverContainerStr: String!
private var bothValueParametersList: [Any]?
var sourceSpace: Float? = 0.0
private var rightBlackCreateArray: [Any]?




    var post_video_photo_type: Bool = false
    @IBOutlet var takePhoto_Video_View: UIView!
    @IBOutlet weak var post_dynamic_content_text: UITextField!
    @IBOutlet weak var publish_dynamic_cover_image: UIImageView!
    @IBOutlet weak var publish_dynamic_delete_button: UIButton!
    @IBOutlet weak var take_photo_button: UIButton!
    
    private var vook_selectedMediaURL: URL?
    private var vook_selectedCoverImage: UIImage?
    private var vook_selectedMediaType: String?
    private var vook_isPublishing = false
    private weak var vook_reviewOverlayView: UIView?

@discardableResult
 func popActivityMusicOpaqueBannerImageView(mustCenter: String!, sendLike: Bool, grantController: Double) -> UIImageView! {
    var nicknameJ: Bool = false
   withUnsafeMutablePointer(to: &nicknameJ) { pointer in
    
   }
    var scene_aN: Double = 5.0
      scene_aN -= (Double((nicknameJ ? 3 : 5) >> (Swift.min(labs(Int(scene_aN > 226076470.0 || scene_aN < -226076470.0 ? 66.0 : scene_aN)), 1))))
      scene_aN -= (Double((nicknameJ ? 1 : 1) >> (Swift.min(labs(Int(scene_aN > 325044819.0 || scene_aN < -325044819.0 ? 60.0 : scene_aN)), 4))))
     var mapBlack: UIView! = UIView(frame:CGRect.zero)
     var mediaUpdate_4v: UIImageView! = UIImageView(image:UIImage(named:String(cString: [97,117,116,111,102,105,120,0], encoding: .utf8)!), highlightedImage:UIImage(named:String(cString: [118,97,108,105,100,97,116,111,114,0], encoding: .utf8)!))
     let musicTitle: String! = String(cString: [100,101,99,111,100,101,102,114,97,109,101,0], encoding: .utf8)!
    var dcmpAppendable = UIImageView(frame:CGRect(x: 211, y: 324, width: 0, height: 0))
    dcmpAppendable.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dcmpAppendable.alpha = 0.0
    dcmpAppendable.frame = CGRect(x: 20, y: 70, width: 0, height: 0)
    dcmpAppendable.animationRepeatCount = 8
    dcmpAppendable.image = UIImage(named:String(cString: [98,108,117,114,0], encoding: .utf8)!)
    dcmpAppendable.contentMode = .scaleAspectFit
    mapBlack.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    mapBlack.alpha = 0.5
    mapBlack.frame = CGRect(x: 289, y: 105, width: 0, height: 0)
    
    mediaUpdate_4v.frame = CGRect(x: 174, y: 11, width: 0, height: 0)
    mediaUpdate_4v.alpha = 0.7;
    mediaUpdate_4v.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    mediaUpdate_4v.animationRepeatCount = 8
    mediaUpdate_4v.image = UIImage(named:String(cString: [99,101,108,108,0], encoding: .utf8)!)
    mediaUpdate_4v.contentMode = .scaleAspectFit
    

    
    return dcmpAppendable

}





    
    private func vook_openCamera() {

         var sublayoutsAppears: UIImageView! = popActivityMusicOpaqueBannerImageView(mustCenter:String(cString: [117,112,99,97,108,108,0], encoding: .utf8)!, sendLike:true, grantController:5.0)

      if sublayoutsAppears != nil {
          let sublayoutsAppears_tag = sublayoutsAppears.tag
          self.view.addSubview(sublayoutsAppears)
      }

withUnsafeMutablePointer(to: &sublayoutsAppears) { pointer in
        _ = pointer.pointee
}


       var backT: Double = 2.0
   withUnsafeMutablePointer(to: &backT) { pointer in
    
   }
   for _ in 0 ..< 2 {
      backT /= Swift.max(3, (Double(Int(backT > 306031078.0 || backT < -306031078.0 ? 95.0 : backT) << (Swift.min(1, labs(Int(backT > 346652346.0 || backT < -346652346.0 ? 80.0 : backT)))))))
   }

        guard UIImagePickerController.isSourceTypeAvailable(.camera) else {
            YCommentPhoto.showInfo("Camera unavailable")
            return
        }
        
        let picker = UIImagePickerController()
        picker.sourceType = .camera
        picker.delegate = self
        picker.allowsEditing = false
        if post_video_photo_type {
            picker.mediaTypes = ["public.image"]
            picker.cameraCaptureMode = .photo
        } else {
            picker.mediaTypes = ["public.movie"]
            picker.cameraCaptureMode = .video
            picker.videoQuality = .typeMedium
        }
        present(picker, animated: true)
    }

@discardableResult
 func lastRowRemoveEndpointGestureScrollView(relationUpdates: [Any]!) -> UIScrollView! {
    var clickW: [Any]! = [String(cString: [99,101,114,116,115,0], encoding: .utf8)!, String(cString: [97,107,97,114,111,115,0], encoding: .utf8)!, String(cString: [97,105,109,100,0], encoding: .utf8)!]
    _ = clickW
    var spinners: [String: Any]! = [String(cString: [102,97,108,108,111,102,102,0], encoding: .utf8)!:40, String(cString: [109,101,116,101,114,0], encoding: .utf8)!:95, String(cString: [103,114,97,112,104,113,108,0], encoding: .utf8)!:87]
   repeat {
       var z_playerQ: String! = String(cString: [114,101,115,105,122,105,110,103,0], encoding: .utf8)!
       var streamingi: [String: Any]! = [String(cString: [115,121,109,98,111,108,115,0], encoding: .utf8)!:60, String(cString: [101,120,104,97,117,115,116,0], encoding: .utf8)!:80, String(cString: [110,111,116,99,104,0], encoding: .utf8)!:54]
       var permissionC: [Any]! = [22, 6]
       var reply9: String! = String(cString: [111,114,105,103,110,97,108,0], encoding: .utf8)!
          var updatesD: [Any]! = [14, 95]
          var requestn: [String: Any]! = [String(cString: [99,114,108,115,0], encoding: .utf8)!:36, String(cString: [115,117,112,112,114,101,115,115,105,111,110,0], encoding: .utf8)!:77]
         withUnsafeMutablePointer(to: &requestn) { pointer in
    
         }
          var streamv: Double = 3.0
         reply9 = "\(2 + requestn.keys.count)"
         updatesD = [3]
         streamv /= Swift.max(Double(streamingi.count), 5)
         streamingi[reply9] = 3
         streamingi[reply9] = 1 + permissionC.count
      while (2 > (streamingi.values.count ^ 4) && (4 ^ streamingi.values.count) > 1) {
         permissionC = [3]
         break
      }
      if (permissionC.count | z_playerQ.count) == 1 && (z_playerQ.count | 1) == 4 {
         permissionC = [2]
      }
         streamingi = ["\(streamingi.count)": streamingi.keys.count]
         permissionC.append(reply9.count)
      if permissionC.count >= reply9.count {
         permissionC = [reply9.count]
      }
         permissionC = [streamingi.keys.count % (Swift.max(2, permissionC.count))]
          var more7: Double = 4.0
         reply9.append("\(streamingi.keys.count / 2)")
         more7 /= Swift.max(5, (Double(Int(more7 > 191155978.0 || more7 < -191155978.0 ? 26.0 : more7) & reply9.count)))
      for _ in 0 ..< 2 {
          var black2: [Any]! = [67, 81]
          _ = black2
         streamingi["\(black2.count)"] = 3 / (Swift.max(10, black2.count))
      }
          var errorH: String! = String(cString: [100,113,117,97,110,116,0], encoding: .utf8)!
          var pickeru: String! = String(cString: [115,117,99,104,0], encoding: .utf8)!
         permissionC = [streamingi.values.count]
         errorH.append("\(pickeru.count >> (Swift.min(labs(3), 1)))")
         pickeru = "\(errorH.count | 2)"
      spinners = ["\(streamingi.values.count)": 2]
      if 3339263 == spinners.count {
         break
      }
   } while (spinners.keys.contains("\(clickW.count)")) && (3339263 == spinners.count)
   while (spinners["\(clickW.count)"] != nil) {
       var photoy: Int = 5
       var tenant9: Double = 0.0
       var blackD: String! = String(cString: [112,114,101,102,101,116,99,104,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &blackD) { pointer in
    
      }
         tenant9 /= Swift.max(4, (Double(Int(tenant9 > 35014012.0 || tenant9 < -35014012.0 ? 92.0 : tenant9) ^ 1)))
         photoy ^= ((String(cString:[110,0], encoding: .utf8)!) == blackD ? blackD.count : photoy)
         blackD = "\((2 * Int(tenant9 > 30787466.0 || tenant9 < -30787466.0 ? 30.0 : tenant9)))"
          var singlef: Bool = false
         withUnsafeMutablePointer(to: &singlef) { pointer in
    
         }
         blackD = "\(1)"
         singlef = tenant9 >= 19.28
       var previewN: [Any]! = [98.0]
      if 5 > blackD.count {
         blackD = "\(3)"
      }
       var passwordA: String! = String(cString: [98,97,116,99,104,101,100,0], encoding: .utf8)!
       var resultB: String! = String(cString: [110,110,109,111,100,0], encoding: .utf8)!
       var isdeleteW: String! = String(cString: [112,95,55,50,95,105,110,118,105,116,97,116,105,111,110,115,0], encoding: .utf8)!
          var closeX: String! = String(cString: [117,118,104,111,114,105,122,111,110,116,97,108,0], encoding: .utf8)!
          var iconn: String! = String(cString: [114,101,99,111,109,112,117,116,101,0], encoding: .utf8)!
          var source5: String! = String(cString: [99,121,99,108,101,115,0], encoding: .utf8)!
         passwordA.append("\(source5.count % (Swift.max(1, 8)))")
         closeX.append("\(((String(cString:[104,0], encoding: .utf8)!) == iconn ? iconn.count : photoy))")
         previewN = [2 << (Swift.min(4, passwordA.count))]
         resultB.append("\(resultB.count ^ passwordA.count)")
         isdeleteW.append("\(2)")
      clickW = [photoy]
      break
   }
     var replyHost: UIButton! = UIButton()
     var responseBottom: Float = 21.0
     var bannerOverlap: Float = 28.0
     let keyAlert: [Any]! = [13, 80, 36]
    var unconsumedBoundaries = UIScrollView()
    unconsumedBoundaries.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unconsumedBoundaries.alpha = 0.0
    unconsumedBoundaries.frame = CGRect(x: 33, y: 238, width: 0, height: 0)
    unconsumedBoundaries.showsVerticalScrollIndicator = true
    unconsumedBoundaries.showsHorizontalScrollIndicator = false
    unconsumedBoundaries.delegate = nil
    unconsumedBoundaries.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    unconsumedBoundaries.alwaysBounceVertical = true
    unconsumedBoundaries.alwaysBounceHorizontal = true
    replyHost.frame = CGRect(x: 42, y: 104, width: 0, height: 0)
    replyHost.alpha = 0.6;
    replyHost.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    replyHost.titleLabel?.font = UIFont.systemFont(ofSize:12)
    replyHost.setImage(UIImage(named:String(cString: [114,101,112,111,114,116,101,100,0], encoding: .utf8)!), for: .normal)
    replyHost.setTitle("", for: .normal)
    replyHost.setBackgroundImage(UIImage(named:String(cString: [110,111,114,109,97,108,105,122,101,100,0], encoding: .utf8)!), for: .normal)
    
    unconsumedBoundaries.addSubview(replyHost)

    
    return unconsumedBoundaries

}





    
    @IBAction func publihsPostClick(_ sender: Any) {

         let hlsencUsable: UIScrollView! = lastRowRemoveEndpointGestureScrollView(relationUpdates:[12, 98])

      if hlsencUsable != nil {
          self.view.addSubview(hlsencUsable)
          let hlsencUsable_tag = hlsencUsable.tag
      }
      else {
          print("hlsencUsable is nil")      }

_ = hlsencUsable


       var propressz: [Any]! = [93, 84, 81]
   repeat {
       var hostI: Float = 5.0
      for _ in 0 ..< 2 {
          var adjustC: Int = 2
          var optionsO: Double = 5.0
          var headersG: Double = 3.0
         withUnsafeMutablePointer(to: &headersG) { pointer in
                _ = pointer.pointee
         }
          var storeR: Double = 1.0
         hostI += Float(3)
         adjustC -= (2 + Int(storeR > 90511151.0 || storeR < -90511151.0 ? 31.0 : storeR))
         optionsO -= (Double(3 % (Swift.max(6, Int(hostI > 16801296.0 || hostI < -16801296.0 ? 21.0 : hostI)))))
         headersG += (Double(Int(optionsO > 379432214.0 || optionsO < -379432214.0 ? 77.0 : optionsO)))
         storeR /= Swift.max(2, (Double(Int(optionsO > 353650493.0 || optionsO < -353650493.0 ? 90.0 : optionsO))))
      }
      for _ in 0 ..< 1 {
         hostI -= (Float(Int(hostI > 106945042.0 || hostI < -106945042.0 ? 58.0 : hostI) << (Swift.min(4, labs(Int(hostI > 381743408.0 || hostI < -381743408.0 ? 38.0 : hostI))))))
      }
      if 2.24 < (5.6 + hostI) && 1.78 < (5.6 + hostI) {
         hostI += (Float(Int(hostI > 158073991.0 || hostI < -158073991.0 ? 100.0 : hostI)))
      }
      propressz.append(propressz.count)
      if propressz.count == 1999032 {
         break
      }
   } while (propressz.count <= 4) && (propressz.count == 1999032)

        vook_publishPost()
    }

@discardableResult
 func appendResetEntityButton() -> UIButton! {
    var description_wjp: String! = String(cString: [99,108,105,112,98,111,97,114,100,0], encoding: .utf8)!
    var sanitizedn: [Any]! = [String(cString: [112,114,111,108,111,110,103,0], encoding: .utf8)!, String(cString: [108,111,99,97,116,105,111,110,0], encoding: .utf8)!]
      description_wjp = "\(2 >> (Swift.min(4, sanitizedn.count)))"
      description_wjp = "\(description_wjp.count * sanitizedn.count)"
     let description_qCount: UIButton! = UIButton(frame:CGRect.zero)
     var animationEnable_gq: Float = 94.0
     var selectHomepage: UIButton! = UIButton(frame:CGRect(x: 148, y: 351, width: 0, height: 0))
     let infoNotification: Double = 58.0
    var ustomServicesNvdec = UIButton(frame:CGRect(x: 291, y: 38, width: 0, height: 0))
    description_qCount.frame = CGRect(x: 184, y: 0, width: 0, height: 0)
    description_qCount.alpha = 0.0;
    description_qCount.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    description_qCount.setTitle("", for: .normal)
    description_qCount.setBackgroundImage(UIImage(named:String(cString: [112,114,111,102,105,108,101,0], encoding: .utf8)!), for: .normal)
    description_qCount.titleLabel?.font = UIFont.systemFont(ofSize:20)
    description_qCount.setImage(UIImage(named:String(cString: [99,117,114,118,101,0], encoding: .utf8)!), for: .normal)
    
    selectHomepage.frame = CGRect(x: 138, y: 247, width: 0, height: 0)
    selectHomepage.alpha = 0.9;
    selectHomepage.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    selectHomepage.setBackgroundImage(UIImage(named:String(cString: [100,101,115,99,114,105,112,116,105,111,110,95,98,119,0], encoding: .utf8)!), for: .normal)
    selectHomepage.titleLabel?.font = UIFont.systemFont(ofSize:19)
    selectHomepage.setImage(UIImage(named:String(cString: [101,110,99,114,121,112,116,101,100,0], encoding: .utf8)!), for: .normal)
    selectHomepage.setTitle("", for: .normal)
    
    ustomServicesNvdec.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    ustomServicesNvdec.alpha = 0.5
    ustomServicesNvdec.frame = CGRect(x: 7, y: 318, width: 0, height: 0)

    
    return ustomServicesNvdec

}





    
    @objc private func vook_reviewAlertOKClick() {

         var rlpInitialize: UIButton! = appendResetEntityButton()

      if rlpInitialize != nil {
          let rlpInitialize_tag = rlpInitialize.tag
          self.view.addSubview(rlpInitialize)
      }

withUnsafeMutablePointer(to: &rlpInitialize) { pointer in
        _ = pointer.pointee
}


       var insufficientD: String! = String(cString: [114,111,116,114,0], encoding: .utf8)!
      insufficientD = "\(1 << (Swift.min(1, insufficientD.count)))"

        guard let overlay = vook_reviewOverlayView else {
            navigationController?.popViewController(animated: true)
            return
        }
        
        vook_isPublishing = false
        UIView.animate(withDuration: 0.18, animations: {
            overlay.alpha = 0
        }) { [weak self] _ in
            overlay.removeFromSuperview()
            self?.navigationController?.popViewController(animated: true)
        }
    }

    
    private func vook_clearSelectedMedia() {
       var pointp: String! = String(cString: [97,117,116,111,99,111,114,114,101,99,116,105,111,110,0], encoding: .utf8)!
    _ = pointp
      pointp = "\(pointp.count)"

        publish_dynamic_cover_image.image = UIImage(named: "profileAgeDetails")
        publish_dynamic_delete_button.isHidden = true
        vook_selectedCoverImage = nil
        vook_selectedMediaURL = nil
        vook_selectedMediaType = nil
    }

    
    private func vook_hideMediaPickerSheet() {
       var delete_6j: Double = 3.0
   for _ in 0 ..< 1 {
       var needz: String! = String(cString: [99,111,114,100,0], encoding: .utf8)!
       _ = needz
       var configurationu: Double = 1.0
      withUnsafeMutablePointer(to: &configurationu) { pointer in
             _ = pointer.pointee
      }
       var normalized5: String! = String(cString: [118,112,111,105,110,116,0], encoding: .utf8)!
          var t_centera: String! = String(cString: [116,114,97,110,115,112,97,114,101,110,99,121,0], encoding: .utf8)!
          var grantT: String! = String(cString: [99,117,116,111,102,102,0], encoding: .utf8)!
         needz.append("\(((String(cString:[104,0], encoding: .utf8)!) == needz ? needz.count : Int(configurationu > 142957983.0 || configurationu < -142957983.0 ? 77.0 : configurationu)))")
         t_centera.append("\((2 | Int(configurationu > 178159059.0 || configurationu < -178159059.0 ? 83.0 : configurationu)))")
         grantT.append("\(3)")
         normalized5 = "\((needz == (String(cString:[80,0], encoding: .utf8)!) ? Int(configurationu > 328810874.0 || configurationu < -328810874.0 ? 49.0 : configurationu) : needz.count))"
      if configurationu < 5.59 {
         normalized5 = "\(normalized5.count & 2)"
      }
      repeat {
          var delegate_dQ: String! = String(cString: [111,112,101,114,97,116,111,114,115,0], encoding: .utf8)!
          var identifier1: [String: Any]! = [String(cString: [100,105,118,105,100,111,114,0], encoding: .utf8)!:true]
          var scrollh: [Any]! = [false]
          var gendern: Double = 2.0
         normalized5 = "\((Int(configurationu > 208546152.0 || configurationu < -208546152.0 ? 90.0 : configurationu) & Int(gendern > 208047254.0 || gendern < -208047254.0 ? 23.0 : gendern)))"
         delegate_dQ = "\(identifier1.keys.count << (Swift.min(labs(3), 3)))"
         identifier1["\(gendern)"] = (Int(gendern > 229638596.0 || gendern < -229638596.0 ? 31.0 : gendern) ^ 3)
         scrollh = [needz.count << (Swift.min(2, delegate_dQ.count))]
         if (String(cString:[51,116,104,106,106,101,0], encoding: .utf8)!) == normalized5 {
            break
         }
      } while (4.44 >= (configurationu / (Swift.max(2.93, 3)))) && ((String(cString:[51,116,104,106,106,101,0], encoding: .utf8)!) == normalized5)
      repeat {
          var nicknameP: String! = String(cString: [112,114,111,0], encoding: .utf8)!
         configurationu -= Double(needz.count)
         nicknameP = "\((Int(configurationu > 119951265.0 || configurationu < -119951265.0 ? 95.0 : configurationu) & needz.count))"
         if 1018493.0 == configurationu {
            break
         }
      } while (needz.count <= 2) && (1018493.0 == configurationu)
      if 4 <= (normalized5.count >> (Swift.min(labs(1), 4))) {
         normalized5.append("\(needz.count & 2)")
      }
          var constraint4: Bool = true
          var yearL: [String: Any]! = [String(cString: [102,117,110,99,0], encoding: .utf8)!:58, String(cString: [109,115,103,115,109,100,101,99,0], encoding: .utf8)!:40, String(cString: [100,101,116,101,99,116,105,110,103,0], encoding: .utf8)!:82]
         configurationu /= Swift.max(1, Double(3))
         constraint4 = (((constraint4 ? normalized5.count : 71) + normalized5.count) == 71)
         yearL["\(constraint4)"] = (3 - Int(configurationu > 209554944.0 || configurationu < -209554944.0 ? 10.0 : configurationu))
       var overlayX: String! = String(cString: [98,105,103,103,101,115,116,0], encoding: .utf8)!
       var updatesl: Float = 3.0
       var backx: Float = 4.0
         overlayX = "\((needz == (String(cString:[50,0], encoding: .utf8)!) ? overlayX.count : needz.count))"
         updatesl /= Swift.max((Float((String(cString:[74,0], encoding: .utf8)!) == normalized5 ? normalized5.count : Int(configurationu > 103462985.0 || configurationu < -103462985.0 ? 31.0 : configurationu))), 1)
         backx -= Float(1)
      delete_6j -= (Double(Int(delete_6j > 321224664.0 || delete_6j < -321224664.0 ? 82.0 : delete_6j)))
   }

        UIView.animate(withDuration: 0.32) {
            self.takePhoto_Video_View.frame.origin.y = 1333
        }
    }

@discardableResult
 func shouldDomainFollowerAnyScrollView() -> UIScrollView! {
    var revisionW: String! = String(cString: [115,116,100,101,114,114,0], encoding: .utf8)!
    var backq: String! = String(cString: [98,114,111,119,110,0], encoding: .utf8)!
      revisionW = "\(3)"
      revisionW = "\(2)"
      backq = "\(backq.count)"
     let rightRevision: UIImageView! = UIImageView(image:UIImage(named:String(cString: [98,97,115,107,101,116,98,97,108,108,0], encoding: .utf8)!), highlightedImage:UIImage(named:String(cString: [100,101,99,111,109,112,114,101,115,115,101,100,0], encoding: .utf8)!))
     var fromDuration: Double = 10.0
     var testRequst: Double = 63.0
    var slopSlippage:UIScrollView! = UIScrollView()
    slopSlippage.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    slopSlippage.alpha = 0.6
    slopSlippage.frame = CGRect(x: 137, y: 46, width: 0, height: 0)
    slopSlippage.alwaysBounceHorizontal = false
    slopSlippage.showsVerticalScrollIndicator = true
    slopSlippage.showsHorizontalScrollIndicator = false
    slopSlippage.delegate = nil
    slopSlippage.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    slopSlippage.alwaysBounceVertical = false
    rightRevision.alpha = 0.3;
    rightRevision.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    rightRevision.frame = CGRect(x: 50, y: 110, width: 0, height: 0)
    rightRevision.animationRepeatCount = 6
    rightRevision.image = UIImage(named:String(cString: [100,97,116,101,0], encoding: .utf8)!)
    rightRevision.contentMode = .scaleAspectFit
    
    slopSlippage.addSubview(rightRevision)

    
    return slopSlippage

}





    
    private func vook_publishPost() {

         let notifiesPrecomp: UIScrollView! = shouldDomainFollowerAnyScrollView()

      if notifiesPrecomp != nil {
          let notifiesPrecomp_tag = notifiesPrecomp.tag
          self.view.addSubview(notifiesPrecomp)
      }

_ = notifiesPrecomp


       var baro: String! = String(cString: [113,115,118,100,101,105,110,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &baro) { pointer in
    
   }
   repeat {
      baro = "\(baro.count - 2)"
      if (String(cString:[52,106,116,120,55,0], encoding: .utf8)!) == baro {
         break
      }
   } while ((String(cString:[52,106,116,120,55,0], encoding: .utf8)!) == baro) && (3 == baro.count)

        guard vook_isPublishing == false else { return }
        let recording = post_dynamic_content_text.text?.trimmingCharacters(in: .whitespacesAndNewlines) ?? ""
        guard recording.isEmpty == false else {
            YCommentPhoto.showInfo("Please enter content")
            return
        }
        guard let mediaType = vook_selectedMediaType else {
            YCommentPhoto.showInfo(post_video_photo_type ? "Please add a photo" : "Please add a video")
            return
        }
        
        view.endEditing(true)
        vook_isPublishing = true
        vook_showReviewAlert()
    }

    
    private func vook_videoCoverImage(from url: URL) -> UIImage? {
       var likeX: Double = 5.0
      likeX /= Swift.max(5, (Double(Int(likeX > 380126483.0 || likeX < -380126483.0 ? 28.0 : likeX) >> (Swift.min(1, labs(Int(likeX > 71735492.0 || likeX < -71735492.0 ? 65.0 : likeX)))))))

        let setting = AVAsset(url: url)
        let list = AVAssetImageGenerator(asset: setting)
        list.appliesPreferredTrackTransform = true
        let time = CMTime(seconds: 0.1, preferredTimescale: 600)
        guard let cgImage = try? list.copyCGImage(at: time, actualTime: nil) else { return nil }
        return UIImage(cgImage: cgImage)
    }

    
    private func vook_openAlbum() {
       var convertedd: Double = 5.0
   if 4.91 <= (3.91 - convertedd) {
      convertedd += (Double(Int(convertedd > 243205433.0 || convertedd < -243205433.0 ? 47.0 : convertedd)))
   }

        guard UIImagePickerController.isSourceTypeAvailable(.photoLibrary) else {
            YCommentPhoto.showInfo("Album unavailable")
            return
        }
        
        let horizontal = UIImagePickerController()
        horizontal.sourceType = .photoLibrary
        horizontal.delegate = self
        horizontal.allowsEditing = false
        horizontal.mediaTypes = post_video_photo_type ? ["public.image"] : ["public.movie"]
        present(horizontal, animated: true)
    }

@discardableResult
 func originalOverlapRunKitBodyScrollView(rightKeys: Double) -> UIScrollView! {
    var blacksq: String! = String(cString: [103,101,111,116,97,103,115,0], encoding: .utf8)!
    var observerp: String! = String(cString: [117,110,108,105,110,107,101,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &observerp) { pointer in
    
   }
   if observerp.count <= 3 || blacksq == String(cString:[67,0], encoding: .utf8)! {
      blacksq.append("\(blacksq.count)")
   }
   if 4 >= observerp.count {
      blacksq.append("\(2 << (Swift.min(1, blacksq.count)))")
   }
     let mapHost: Double = 5.0
     let sensitivityView: Double = 8.0
     var buttonsKeys: UIImageView! = UIImageView()
    var helperRefinerMpdb:UIScrollView! = UIScrollView(frame:CGRect(x: 42, y: 395, width: 0, height: 0))
    buttonsKeys.frame = CGRect(x: 111, y: 178, width: 0, height: 0)
    buttonsKeys.alpha = 0.6;
    buttonsKeys.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    buttonsKeys.image = UIImage(named:String(cString: [112,111,112,117,112,0], encoding: .utf8)!)
    buttonsKeys.contentMode = .scaleAspectFit
    buttonsKeys.animationRepeatCount = 8
    
    helperRefinerMpdb.addSubview(buttonsKeys)
    helperRefinerMpdb.alpha = 0.0;
    helperRefinerMpdb.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    helperRefinerMpdb.frame = CGRect(x: 190, y: 260, width: 0, height: 0)
    helperRefinerMpdb.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    helperRefinerMpdb.alwaysBounceVertical = true
    helperRefinerMpdb.alwaysBounceHorizontal = false
    helperRefinerMpdb.showsVerticalScrollIndicator = true
    helperRefinerMpdb.showsHorizontalScrollIndicator = false
    helperRefinerMpdb.delegate = nil

    
    return helperRefinerMpdb

}






    @IBAction func publish_dynamic_photo_video_delete(_ sender: UIButton) {

         let optedPurpose: UIScrollView! = originalOverlapRunKitBodyScrollView(rightKeys:94.0)

      if optedPurpose != nil {
          self.view.addSubview(optedPurpose)
          let optedPurpose_tag = optedPurpose.tag
      }
      else {
          print("optedPurpose is nil")      }

_ = optedPurpose


       var client9: [String: Any]! = [String(cString: [115,112,101,99,116,114,97,0], encoding: .utf8)!:85, String(cString: [97,114,99,104,0], encoding: .utf8)!:80, String(cString: [109,106,112,101,103,0], encoding: .utf8)!:68]
    _ = client9
      client9["\(client9.keys.count)"] = client9.count

        view.endEditing(true)
        if sender.tag == 1001 {
            UIView.animate(withDuration: 0.32) {
                self.takePhoto_Video_View.frame.origin.y = 0
            }
        }
        else {
            vook_clearSelectedMedia()
        }
    }

    
    override func viewDidLoad() {
       var description_0kU: Double = 3.0
   withUnsafeMutablePointer(to: &description_0kU) { pointer in
    
   }
      description_0kU += (Double(Int(description_0kU > 311076803.0 || description_0kU < -311076803.0 ? 24.0 : description_0kU) / 2))

        super.viewDidLoad()
        post_dynamic_content_text.delegate = self
        post_dynamic_content_text.returnKeyType = .done
        
        let method_ez = UITapGestureRecognizer(target: self, action: #selector(vook_dismissKeyboard))
        method_ez.cancelsTouchesInView = false
        view.addGestureRecognizer(method_ez)
        
        if post_video_photo_type == true {
            navigationItem.title = "Post Photo"
        }else {
            navigationItem.title = "Post Video"
            take_photo_button.setImage(UIImage(named: "keysSelected"), for: .normal)
        }
        publish_dynamic_delete_button.isHidden = true
        view.addSubview(takePhoto_Video_View)
        takePhoto_Video_View.frame = view.bounds
        takePhoto_Video_View.frame.origin.y = 1333
    }

@discardableResult
 func navigationBigCustomEventKeyboardButton() -> UIButton! {
    var delegate_7iO: Int = 2
    var editb: Int = 4
   for _ in 0 ..< 2 {
      delegate_7iO &= editb ^ 3
   }
   repeat {
      editb >>= Swift.min(labs(delegate_7iO ^ editb), 4)
      if editb == 3746056 {
         break
      }
   } while (3 > delegate_7iO) && (editb == 3746056)
     let otherChange: Double = 92.0
     let dynamic_tdStream: UIButton! = UIButton(frame:CGRect.zero)
     var photoUpdates: Float = 1.0
    var pframeSelector = UIButton()
    pframeSelector.setBackgroundImage(UIImage(named:String(cString: [105,110,116,95,111,57,0], encoding: .utf8)!), for: .normal)
    pframeSelector.titleLabel?.font = UIFont.systemFont(ofSize:12)
    pframeSelector.setImage(UIImage(named:String(cString: [116,97,107,101,0], encoding: .utf8)!), for: .normal)
    pframeSelector.setTitle("", for: .normal)
    pframeSelector.frame = CGRect(x: 49, y: 166, width: 0, height: 0)
    pframeSelector.alpha = 1.0;
    pframeSelector.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dynamic_tdStream.alpha = 0.7;
    dynamic_tdStream.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    dynamic_tdStream.frame = CGRect(x: 140, y: 301, width: 0, height: 0)
    dynamic_tdStream.setImage(UIImage(named:String(cString: [100,101,108,101,116,101,95,103,120,0], encoding: .utf8)!), for: .normal)
    dynamic_tdStream.setTitle("", for: .normal)
    dynamic_tdStream.setBackgroundImage(UIImage(named:String(cString: [98,108,117,114,0], encoding: .utf8)!), for: .normal)
    dynamic_tdStream.titleLabel?.font = UIFont.systemFont(ofSize:18)
    

    
    return pframeSelector

}





    
    @IBAction func publishTakePhotoANdVideoCancelClick(_ sender: UIButton) {

         let bilinMenus: UIButton! = navigationBigCustomEventKeyboardButton()

      if bilinMenus != nil {
          let bilinMenus_tag = bilinMenus.tag
          self.view.addSubview(bilinMenus)
      }
      else {
          print("bilinMenus is nil")      }

_ = bilinMenus


       var controllerU: String! = String(cString: [105,109,112,111,114,116,115,0], encoding: .utf8)!
    _ = controllerU
      controllerU = "\(controllerU.count / (Swift.max(3, 2)))"

        view.endEditing(true)
        UIView.animate(withDuration: 0.32) {
            self.takePhoto_Video_View.frame.origin.y = 1333
        }
        if sender.tag == 1001 {
            vook_openCamera()
        }
        else if sender.tag == 1002 {
            vook_openAlbum()
        }
        else {
            vook_hideMediaPickerSheet()
        }
    }

@discardableResult
 func photoInformationPoliticalInstanceStore(normalizedSanitized: Bool, reuseHost: Double, loadingScene: String!) -> String! {
    var music8: String! = String(cString: [97,99,99,101,108,101,114,97,116,105,111,110,0], encoding: .utf8)!
    _ = music8
    var endpointX: String! = String(cString: [100,101,99,111,114,97,116,101,0], encoding: .utf8)!
    var safeU: String! = String(cString: [100,105,115,97,98,108,101,0], encoding: .utf8)!
      endpointX.append("\(1)")
   while (safeU == music8) {
       var sendL: String! = String(cString: [107,118,111,0], encoding: .utf8)!
       _ = sendL
       var imagesH: [Any]! = [8.0]
       var most_: String! = String(cString: [110,111,97,115,109,0], encoding: .utf8)!
       _ = most_
       var emailC: Double = 3.0
       _ = emailC
         sendL = "\((3 ^ Int(emailC > 226490297.0 || emailC < -226490297.0 ? 32.0 : emailC)))"
       var videoQ: Double = 4.0
       var ageU: Bool = true
      withUnsafeMutablePointer(to: &ageU) { pointer in
             _ = pointer.pointee
      }
         sendL = "\(most_.count & 1)"
         emailC -= Double(most_.count | 2)
         imagesH = [((ageU ? 1 : 2) / (Swift.max(8, Int(videoQ > 103561831.0 || videoQ < -103561831.0 ? 81.0 : videoQ))))]
         videoQ -= (Double((ageU ? 4 : 2) / (Swift.max(8, Int(emailC > 33327277.0 || emailC < -33327277.0 ? 46.0 : emailC)))))
      if imagesH.count > 5 {
         imagesH.append(most_.count + 3)
      }
         most_.append("\(sendL.count)")
          var isdeleteo: Int = 4
          var videoF: Int = 0
         imagesH.append((Int(videoQ > 331059747.0 || videoQ < -331059747.0 ? 50.0 : videoQ) * (ageU ? 3 : 4)))
         isdeleteo ^= (Int(emailC > 310673306.0 || emailC < -310673306.0 ? 83.0 : emailC))
         videoF |= isdeleteo
          var enter6: Float = 1.0
          _ = enter6
         emailC -= (Double(Int(enter6 > 257113831.0 || enter6 < -257113831.0 ? 58.0 : enter6)))
         emailC -= Double(1)
      music8 = "\(most_.count)"
      break
   }
   if safeU.hasPrefix("\(endpointX.count)") {
      safeU.append("\(music8.count >> (Swift.min(labs(1), 5)))")
   }
   return music8

}





    
    @objc private func vook_dismissKeyboard() {

         var clgetMcore: String! = photoInformationPoliticalInstanceStore(normalizedSanitized:true, reuseHost:88.0, loadingScene:String(cString: [114,101,109,111,118,101,100,0], encoding: .utf8)!)

      if clgetMcore == "status" {
              print(clgetMcore)
      }
      let clgetMcore_len = clgetMcore?.count ?? 0

withUnsafeMutablePointer(to: &clgetMcore) { pointer in
    
}


       var musicG: Bool = true
   withUnsafeMutablePointer(to: &musicG) { pointer in
    
   }
    var removeL: Float = 2.0
   withUnsafeMutablePointer(to: &removeL) { pointer in
    
   }
      musicG = removeL <= 93.76
      removeL += (Float(1 >> (Swift.min(labs(Int(removeL > 134146584.0 || removeL < -134146584.0 ? 63.0 : removeL)), 4))))

        view.endEditing(true)
    }

@discardableResult
 func photoAvailableRecommendDurationAnyMaterialLabel(normalizedShared: Double, albumKeyboard: [String: Any]!) -> UILabel! {
    var albumm: Bool = false
    var type_ejh: Float = 2.0
    _ = type_ejh
   repeat {
      albumm = !albumm
      if albumm ? !albumm : albumm {
         break
      }
   } while (albumm ? !albumm : albumm) && (2.34 >= (type_ejh + 4.96) && albumm)
   while (albumm) {
       var type_5A: Double = 4.0
      if (Double(type_5A - Double(2))) >= 2.51 {
          var othersb: Double = 1.0
          var eulac: String! = String(cString: [111,105,100,0], encoding: .utf8)!
         type_5A += (Double(Int(othersb > 81422234.0 || othersb < -81422234.0 ? 57.0 : othersb)))
         eulac.append("\((1 << (Swift.min(1, labs(Int(type_5A > 98106644.0 || type_5A < -98106644.0 ? 9.0 : type_5A))))))")
      }
       var h_view0: String! = String(cString: [99,111,110,116,97,99,116,0], encoding: .utf8)!
       _ = h_view0
      for _ in 0 ..< 3 {
         type_5A -= (Double(Int(type_5A > 386743301.0 || type_5A < -386743301.0 ? 88.0 : type_5A)))
      }
         h_view0.append("\((Int(type_5A > 357693201.0 || type_5A < -357693201.0 ? 92.0 : type_5A) & h_view0.count))")
      albumm = type_ejh == 61.58
      break
   }
     let fallbackResponse: String! = String(cString: [120,117,118,109,118,115,0], encoding: .utf8)!
     let productsHome: [Any]! = [92, 86, 38]
     var hintMsg: UIView! = UIView()
     let updatesIdentifier: String! = String(cString: [99,116,120,116,0], encoding: .utf8)!
    var mrzAnaglyphMpadsp = UILabel()
    mrzAnaglyphMpadsp.font = UIFont.systemFont(ofSize:10)
    mrzAnaglyphMpadsp.text = ""
    mrzAnaglyphMpadsp.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    mrzAnaglyphMpadsp.textAlignment = .right
    mrzAnaglyphMpadsp.frame = CGRect(x: 57, y: 212, width: 0, height: 0)
    mrzAnaglyphMpadsp.alpha = 0.1;
    mrzAnaglyphMpadsp.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    hintMsg.alpha = 0.1;
    hintMsg.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    hintMsg.frame = CGRect(x: 29, y: 184, width: 0, height: 0)
    

    
    return mrzAnaglyphMpadsp

}





    
    private func vook_showReviewAlert() {

         let callocResilience: UILabel! = photoAvailableRecommendDurationAnyMaterialLabel(normalizedShared:91.0, albumKeyboard:[String(cString: [112,114,101,115,101,110,99,101,115,0], encoding: .utf8)!:34, String(cString: [118,112,108,97,121,101,114,0], encoding: .utf8)!:81])

      if callocResilience != nil {
          self.view.addSubview(callocResilience)
          let callocResilience_tag = callocResilience.tag
      }
      else {
          print("callocResilience is nil")      }

_ = callocResilience


       var actionv: String! = String(cString: [116,117,114,98,117,108,101,110,99,101,0], encoding: .utf8)!
      actionv.append("\(3 ^ actionv.count)")

        guard vook_reviewOverlayView == nil else { return }
        
        let likeView: UIView
        if let navigationView = navigationController?.view {
            likeView = navigationView
        } else {
            likeView = view
        }
        let picker = UIView(frame: likeView.bounds)
        picker.autoresizingMask = [UIView.AutoresizingMask.flexibleWidth, UIView.AutoresizingMask.flexibleHeight]
        picker.backgroundColor = UIColor.black.withAlphaComponent(0.62)
        picker.alpha = 0
        
        let homeView = UIVisualEffectView(effect: UIBlurEffect(style: .dark))
        homeView.frame = picker.bounds
        homeView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        homeView.alpha = 0.35
        picker.addSubview(homeView)
        
        let spacingView = UIImageView(image: UIImage(named: "startDefaulIntro"))
        spacingView.contentMode = .scaleAspectFit
        spacingView.isUserInteractionEnabled = true
        spacingView.translatesAutoresizingMaskIntoConstraints = false
        picker.addSubview(spacingView)
        
        let buttonButton = UIButton(type: .custom)
        buttonButton.setImage(UIImage(named: "centersHomepageContains"), for: .normal)
        buttonButton.addTarget(self, action: #selector(vook_reviewAlertOKClick), for: .touchUpInside)
        buttonButton.translatesAutoresizingMaskIntoConstraints = false
        spacingView.addSubview(buttonButton)
        
        likeView.addSubview(picker)
        vook_reviewOverlayView = picker
        
        NSLayoutConstraint.activate([
            spacingView.centerXAnchor.constraint(equalTo: picker.centerXAnchor),
            spacingView.centerYAnchor.constraint(equalTo: picker.centerYAnchor, constant: -18),
            spacingView.widthAnchor.constraint(equalToConstant: 275),
            spacingView.heightAnchor.constraint(equalToConstant: 363),
            
            buttonButton.centerXAnchor.constraint(equalTo: spacingView.centerXAnchor),
            buttonButton.bottomAnchor.constraint(equalTo: spacingView.bottomAnchor, constant: -64),
            buttonButton.widthAnchor.constraint(equalToConstant: 235),
            buttonButton.heightAnchor.constraint(equalToConstant: 40)
        ])
        
        spacingView.transform = CGAffineTransform(scaleX: 0.92, y: 0.92)
        UIView.animate(withDuration: 0.22, delay: 0, options: .curveEaseOut) {
            picker.alpha = 1
            spacingView.transform = .identity
        }
    }

@discardableResult
 func writeCreatorClientClick(baseManager: Double) -> Float {
    var header3: [String: Any]! = [String(cString: [108,111,117,112,101,0], encoding: .utf8)!:13.0]
    var tasku: String! = String(cString: [102,105,108,116,101,114,101,100,0], encoding: .utf8)!
    var layout3: Float = 2.0
    _ = layout3
   repeat {
       var musicL: String! = String(cString: [103,97,117,115,115,105,97,110,105,105,114,100,0], encoding: .utf8)!
         musicL.append("\(2 * musicL.count)")
         musicL = "\(musicL.count << (Swift.min(labs(1), 4)))"
      repeat {
         musicL = "\(2 + musicL.count)"
         if (String(cString:[112,51,112,113,114,98,114,116,121,104,0], encoding: .utf8)!) == musicL {
            break
         }
      } while ((String(cString:[112,51,112,113,114,98,114,116,121,104,0], encoding: .utf8)!) == musicL) && (!musicL.hasPrefix("\(musicL.count)"))
      header3 = ["\(header3.count)": 1]
      if 2381303 == header3.count {
         break
      }
   } while (header3.count <= tasku.count) && (2381303 == header3.count)
       var tabv: Double = 2.0
       var dictt: String! = String(cString: [115,116,114,116,121,112,101,0], encoding: .utf8)!
       var observew: Double = 3.0
      repeat {
          var keyboardC: [String: Any]! = [String(cString: [100,101,110,111,109,0], encoding: .utf8)!:String(cString: [114,101,99,111,110,105,110,116,114,97,120,0], encoding: .utf8)!, String(cString: [99,117,116,111,117,116,0], encoding: .utf8)!:String(cString: [115,112,108,105,116,115,0], encoding: .utf8)!, String(cString: [104,101,118,99,112,114,101,100,0], encoding: .utf8)!:String(cString: [116,101,100,99,97,112,116,105,111,110,115,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &keyboardC) { pointer in
    
         }
          var verifyq: Int = 3
          _ = verifyq
          var pointH: String! = String(cString: [114,101,106,111,105,110,0], encoding: .utf8)!
         dictt.append("\(dictt.count / (Swift.max(10, pointH.count)))")
         keyboardC = ["\(observew)": (Int(tabv > 202260698.0 || tabv < -202260698.0 ? 73.0 : tabv) + Int(observew > 196327540.0 || observew < -196327540.0 ? 87.0 : observew))]
         verifyq |= dictt.count
         if dictt == (String(cString:[113,112,118,112,53,0], encoding: .utf8)!) {
            break
         }
      } while (5 <= dictt.count) && (dictt == (String(cString:[113,112,118,112,53,0], encoding: .utf8)!))
      for _ in 0 ..< 2 {
         tabv -= (Double(Int(tabv > 44594176.0 || tabv < -44594176.0 ? 10.0 : tabv) - 3))
      }
      while ((tabv * observew) >= 3.59 || (observew * tabv) >= 3.59) {
          var requstz: String! = String(cString: [97,99,116,105,118,101,0], encoding: .utf8)!
          _ = requstz
          var slota: [String: Any]! = [String(cString: [114,101,115,101,114,118,101,100,0], encoding: .utf8)!:String(cString: [104,111,109,101,0], encoding: .utf8)!, String(cString: [100,105,115,112,97,116,99,104,105,110,103,0], encoding: .utf8)!:String(cString: [106,115,105,109,100,99,112,117,0], encoding: .utf8)!, String(cString: [110,105,109,97,116,101,100,0], encoding: .utf8)!:String(cString: [122,102,114,101,101,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &slota) { pointer in
                _ = pointer.pointee
         }
          var blocksD: Bool = true
          var editG: Double = 5.0
          var t_centerQ: Float = 5.0
         tabv /= Swift.max(Double(requstz.count), 1)
         slota[dictt] = 3
         blocksD = 15.25 <= t_centerQ
         editG /= Swift.max(4, (Double(Int(editG > 1775168.0 || editG < -1775168.0 ? 40.0 : editG))))
         t_centerQ *= (Float(Int(tabv > 289581886.0 || tabv < -289581886.0 ? 37.0 : tabv) - 1))
         break
      }
       var rightn: String! = String(cString: [99,111,108,108,0], encoding: .utf8)!
       var model1: String! = String(cString: [113,101,120,112,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &model1) { pointer in
    
      }
      repeat {
          var horizontalX: Bool = true
         dictt = "\(((String(cString:[73,0], encoding: .utf8)!) == dictt ? dictt.count : Int(tabv > 162718973.0 || tabv < -162718973.0 ? 96.0 : tabv)))"
         horizontalX = model1.hasPrefix("\(horizontalX)")
         if (String(cString:[117,111,110,115,107,99,122,120,51,118,0], encoding: .utf8)!) == dictt {
            break
         }
      } while ((String(cString:[117,111,110,115,107,99,122,120,51,118,0], encoding: .utf8)!) == dictt) && (2.70 <= (tabv / (Swift.max(2.35, 3))) && 2 <= (2 + dictt.count))
      repeat {
         tabv /= Swift.max(Double(model1.count), 2)
         if 1943813.0 == tabv {
            break
         }
      } while (1943813.0 == tabv) && ((tabv / (Swift.max(3.86, 4))) == 4.40)
      while (dictt.count == 1) {
          var agreementz: String! = String(cString: [114,116,108,0], encoding: .utf8)!
          _ = agreementz
         dictt.append("\(2 << (Swift.min(1, model1.count)))")
         agreementz.append("\(rightn.count * 1)")
         break
      }
      repeat {
          var commenty: String! = String(cString: [97,112,112,114,111,118,97,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &commenty) { pointer in
                _ = pointer.pointee
         }
          var hasU: String! = String(cString: [106,117,103,103,108,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &hasU) { pointer in
                _ = pointer.pointee
         }
          var commentsX: Int = 1
          var firstq: [Any]! = [false]
          var charactersR: [Any]! = [false]
         rightn = "\(((String(cString:[50,0], encoding: .utf8)!) == hasU ? commenty.count : hasU.count))"
         commentsX |= (commenty == (String(cString:[108,0], encoding: .utf8)!) ? charactersR.count : commenty.count)
         firstq.append(charactersR.count << (Swift.min(rightn.count, 2)))
         if rightn == (String(cString:[55,115,118,109,49,48,119,117,0], encoding: .utf8)!) {
            break
         }
      } while (rightn == (String(cString:[55,115,118,109,49,48,119,117,0], encoding: .utf8)!)) && (rightn.hasSuffix("\(model1.count)"))
       var cleanc: String! = String(cString: [109,101,114,103,101,0], encoding: .utf8)!
         cleanc = "\(model1.count)"
      layout3 -= Float(header3.keys.count)
      layout3 *= Float(3)
   return layout3

}





    
    private func vook_updatePreview(image: UIImage, mediaURL: URL?, mediaType: String) {

         var msmpegdataUploadable: Float = writeCreatorClientClick(baseManager:4.0)

      print(msmpegdataUploadable)

withUnsafeMutablePointer(to: &msmpegdataUploadable) { pointer in
        _ = pointer.pointee
}


       var prefix_gkz: String! = String(cString: [122,101,114,111,101,115,0], encoding: .utf8)!
      prefix_gkz.append("\(prefix_gkz.count - prefix_gkz.count)")

        publish_dynamic_cover_image.image = image
        publish_dynamic_delete_button.isHidden = false
        vook_selectedCoverImage = image
        vook_selectedMediaURL = mediaURL
        vook_selectedMediaType = mediaType
    }
    
}

extension MTextResponseController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {

@discardableResult
 func otherDeadlineControlLargeImageView() -> UIImageView! {
    var infox: Bool = true
    var jsonM: String! = String(cString: [112,114,101,118,105,101,119,0], encoding: .utf8)!
   if infox && 2 == jsonM.count {
      infox = jsonM.count >= 12 && infox
   }
     var birthdayLines: UIButton! = UIButton(frame:CGRect.zero)
     var completeLast: Bool = false
     var nicknameDirectory: [String: Any]! = [String(cString: [112,97,99,107,101,100,0], encoding: .utf8)!:41, String(cString: [109,106,112,101,103,97,0], encoding: .utf8)!:82, String(cString: [115,116,97,99,107,118,105,101,119,0], encoding: .utf8)!:92]
     let followersRow: Int = 11
    var cbcrDestroyingUnpacked: UIImageView! = UIImageView()
    birthdayLines.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    birthdayLines.alpha = 0.4
    birthdayLines.frame = CGRect(x: 65, y: 70, width: 0, height: 0)
    birthdayLines.setImage(UIImage(named:String(cString: [111,116,104,101,114,115,0], encoding: .utf8)!), for: .normal)
    birthdayLines.setTitle("", for: .normal)
    birthdayLines.setBackgroundImage(UIImage(named:String(cString: [108,97,98,101,108,0], encoding: .utf8)!), for: .normal)
    birthdayLines.titleLabel?.font = UIFont.systemFont(ofSize:20)
    
    cbcrDestroyingUnpacked.frame = CGRect(x: 16, y: 97, width: 0, height: 0)
    cbcrDestroyingUnpacked.alpha = 0.3;
    cbcrDestroyingUnpacked.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    cbcrDestroyingUnpacked.animationRepeatCount = 1
    cbcrDestroyingUnpacked.image = UIImage(named:String(cString: [108,111,103,105,110,0], encoding: .utf8)!)
    cbcrDestroyingUnpacked.contentMode = .scaleAspectFit

    
    return cbcrDestroyingUnpacked

}





    
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {

         var accumExtent: UIImageView! = otherDeadlineControlLargeImageView()

      if accumExtent != nil {
          let accumExtent_tag = accumExtent.tag
          self.view.addSubview(accumExtent)
      }
      else {
          print("accumExtent is nil")      }

withUnsafeMutablePointer(to: &accumExtent) { pointer in
        _ = pointer.pointee
}


       var blurs: String! = String(cString: [100,105,102,102,101,114,101,110,99,101,115,0], encoding: .utf8)!
      blurs.append("\(2 / (Swift.max(9, blurs.count)))")

        picker.dismiss(animated: true)
    }

    
    func imagePickerController(_ picker: UIImagePickerController,
                               didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey : Any]) {
       var layouti: String! = String(cString: [100,114,97,119,97,98,108,101,115,0], encoding: .utf8)!
    var insufficient8: Double = 4.0
   while (1.18 < (5.32 - insufficient8)) {
      insufficient8 += (Double(layouti.count / (Swift.max(5, Int(insufficient8 > 67602601.0 || insufficient8 < -67602601.0 ? 83.0 : insufficient8)))))
      break
   }

   for _ in 0 ..< 2 {
       var publishing5: [String: Any]! = [String(cString: [116,101,109,112,0], encoding: .utf8)!:100, String(cString: [115,111,102,116,102,108,111,97,116,0], encoding: .utf8)!:39, String(cString: [114,97,110,100,111,109,110,101,115,115,0], encoding: .utf8)!:13]
       var scalarU: Int = 3
       var switch_rxb: Double = 3.0
       _ = switch_rxb
       var alertz: [Any]! = [72, 46]
      withUnsafeMutablePointer(to: &alertz) { pointer in
             _ = pointer.pointee
      }
          var idsq: [Any]! = [11, 31]
          _ = idsq
          var followwithP: String! = String(cString: [107,105,108,108,0], encoding: .utf8)!
          var click1: String! = String(cString: [112,97,99,107,97,103,101,115,0], encoding: .utf8)!
         alertz = [followwithP.count % (Swift.max(2, 9))]
         idsq = [followwithP.count ^ 3]
         click1.append("\(scalarU ^ 3)")
          var isdeletem: Int = 2
          _ = isdeletem
         switch_rxb -= Double(isdeletem % (Swift.max(2, publishing5.values.count)))
      repeat {
          var sharedc: String! = String(cString: [99,115,119,97,112,0], encoding: .utf8)!
          _ = sharedc
          var client4: Int = 1
          var avatarD: Float = 1.0
          var createdz: [String: Any]! = [String(cString: [100,101,106,117,100,100,101,114,0], encoding: .utf8)!:String(cString: [119,101,108,115,100,101,99,100,101,109,111,0], encoding: .utf8)!, String(cString: [110,115,117,105,114,103,98,97,0], encoding: .utf8)!:String(cString: [119,105,110,100,111,119,0], encoding: .utf8)!, String(cString: [116,114,97,110,115,112,111,110,101,100,0], encoding: .utf8)!:String(cString: [117,110,112,97,99,107,104,105,0], encoding: .utf8)!]
         alertz = [alertz.count * sharedc.count]
         client4 += (sharedc == (String(cString:[113,0], encoding: .utf8)!) ? client4 : sharedc.count)
         avatarD /= Swift.max(Float(scalarU), 2)
         createdz = ["\(avatarD)": (2 << (Swift.min(labs(Int(avatarD > 106012716.0 || avatarD < -106012716.0 ? 46.0 : avatarD)), 5)))]
         if alertz.count == 437606 {
            break
         }
      } while (alertz.count == scalarU) && (alertz.count == 437606)
         scalarU ^= (Int(switch_rxb > 36676757.0 || switch_rxb < -36676757.0 ? 94.0 : switch_rxb) / (Swift.max(9, publishing5.count)))
       var coinsB: String! = String(cString: [100,117,114,98,0], encoding: .utf8)!
       _ = coinsB
      if !publishing5.keys.contains("\(switch_rxb)") {
          var decoderb: Bool = true
          var navB: Float = 4.0
          _ = navB
         publishing5[coinsB] = alertz.count ^ 3
         decoderb = publishing5.values.count < 100 || scalarU < 100
         navB /= Swift.max(Float(2), 4)
      }
       var coins2: Double = 2.0
       var self_sn: Double = 3.0
      while (1 == (1 + scalarU) && 1 == (1 + coinsB.count)) {
         coinsB.append("\(2 | scalarU)")
         break
      }
         publishing5["\(switch_rxb)"] = 3 ^ alertz.count
         self_sn /= Swift.max(2, (Double(Int(switch_rxb > 265680315.0 || switch_rxb < -265680315.0 ? 29.0 : switch_rxb) ^ 3)))
         coinsB.append("\(coinsB.count / (Swift.max(2, 8)))")
          var settingv: Int = 3
         alertz.append(alertz.count | 3)
         settingv %= Swift.max(3, (Int(coins2 > 43883976.0 || coins2 < -43883976.0 ? 82.0 : coins2)))
      layouti = "\(scalarU * alertz.count)"
   }
        picker.dismiss(animated: true)
        
        if post_video_photo_type,
           let update_bqImage = info[.originalImage] as? UIImage {
            vook_updatePreview(image: update_bqImage, mediaURL: nil, mediaType: UHomepageLaunchq.multiImage)
            return
        }
        
        if let videoURL = info[.mediaURL] as? URL {
            let recordingk = vook_videoCoverImage(from: videoURL) ?? UIImage(named: "eulaSpinner") ?? UIImage()
            vook_updatePreview(image: recordingk, mediaURL: videoURL, mediaType: UHomepageLaunchq.singleVideo)
        }
    }
}

extension MTextResponseController: UITextFieldDelegate {

@discardableResult
 func contentLayerDataLabel(rowClose: String!) -> UILabel! {
    var agrrmments: String! = String(cString: [109,111,100,101,99,111,110,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &agrrmments) { pointer in
          _ = pointer.pointee
   }
    var time_5X: String! = String(cString: [112,114,111,118,105,100,105,110,103,0], encoding: .utf8)!
    _ = time_5X
   while (time_5X == String(cString:[100,0], encoding: .utf8)!) {
       var singlei: String! = String(cString: [116,105,109,101,119,97,105,116,0], encoding: .utf8)!
       var linei: Int = 4
       var should3: String! = String(cString: [97,117,100,0], encoding: .utf8)!
       var singlen: String! = String(cString: [115,116,97,116,101,115,0], encoding: .utf8)!
       var background4: Int = 5
      for _ in 0 ..< 3 {
         background4 -= 2 ^ linei
      }
         linei -= should3.count | 2
         should3.append("\(((String(cString:[65,0], encoding: .utf8)!) == should3 ? linei : should3.count))")
      repeat {
         should3 = "\(should3.count)"
         if should3.count == 4663485 {
            break
         }
      } while (should3.count == 4663485) && (1 <= singlen.count && 1 <= should3.count)
          var videox: String! = String(cString: [111,112,116,101,100,0], encoding: .utf8)!
          var completel: String! = String(cString: [117,116,116,101,114,97,110,99,101,0], encoding: .utf8)!
          var list7: String! = String(cString: [116,116,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &list7) { pointer in
                _ = pointer.pointee
         }
         linei -= linei & 3
         videox.append("\(singlen.count)")
         completel = "\(((String(cString:[116,0], encoding: .utf8)!) == singlei ? singlei.count : videox.count))"
         list7.append("\(completel.count)")
      repeat {
         linei -= singlen.count
         if 1748759 == linei {
            break
         }
      } while (1 < (linei % (Swift.max(3, 1)))) && (1748759 == linei)
      for _ in 0 ..< 3 {
         singlei.append("\(singlen.count)")
      }
       var timerO: String! = String(cString: [105,110,100,105,99,97,116,101,100,0], encoding: .utf8)!
      if (1 << (Swift.min(3, singlei.count))) >= 2 {
          var startH: String! = String(cString: [109,101,109,110,95,109,95,56,52,0], encoding: .utf8)!
          var cleanP: String! = String(cString: [110,115,112,97,99,101,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &cleanP) { pointer in
    
         }
          var eulab: String! = String(cString: [110,118,111,105,99,101,0], encoding: .utf8)!
         singlei = "\(singlen.count)"
         startH = "\(startH.count)"
         cleanP = "\(startH.count)"
         eulab = "\((eulab == (String(cString:[83,0], encoding: .utf8)!) ? eulab.count : cleanP.count))"
      }
      repeat {
         background4 |= timerO.count >> (Swift.min(5, labs(linei)))
         if 3599130 == background4 {
            break
         }
      } while (timerO.hasPrefix("\(background4)")) && (3599130 == background4)
      for _ in 0 ..< 1 {
         background4 /= Swift.max(1, singlen.count)
      }
          var alertF: Bool = true
         linei -= ((String(cString:[99,0], encoding: .utf8)!) == singlen ? singlen.count : singlei.count)
         alertF = (String(cString:[103,0], encoding: .utf8)!) == singlei
         timerO = "\(background4 << (Swift.min(labs(1), 2)))"
          var idsl: String! = String(cString: [116,121,112,101,115,116,114,0], encoding: .utf8)!
          var strI: [String: Any]! = [String(cString: [114,101,102,99,111,117,110,116,0], encoding: .utf8)!:15, String(cString: [117,116,102,108,101,110,0], encoding: .utf8)!:16, String(cString: [114,101,106,101,99,116,101,100,0], encoding: .utf8)!:46]
         should3 = "\(1 * idsl.count)"
         strI[timerO] = idsl.count
         background4 >>= Swift.min(1, labs(background4))
      agrrmments.append("\(should3.count | singlen.count)")
      break
   }
     var snewsError: [Any]! = [String(cString: [114,101,99,111,103,110,105,122,101,0], encoding: .utf8)!, String(cString: [116,114,117,110,99,97,116,101,100,0], encoding: .utf8)!, String(cString: [109,115,117,98,0], encoding: .utf8)!]
     var commentsChat: UILabel! = UILabel()
     let isdeleteAllowed: Float = 13.0
    var storexDestroy:UILabel! = UILabel()
    commentsChat.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    commentsChat.alpha = 0.1
    commentsChat.frame = CGRect(x: 8, y: 111, width: 0, height: 0)
    commentsChat.font = UIFont.systemFont(ofSize:20)
    commentsChat.text = ""
    commentsChat.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    commentsChat.textAlignment = .center
    
    storexDestroy.font = UIFont.systemFont(ofSize:11)
    storexDestroy.text = ""
    storexDestroy.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    storexDestroy.textAlignment = .left
    storexDestroy.alpha = 0.6;
    storexDestroy.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    storexDestroy.frame = CGRect(x: 211, y: 233, width: 0, height: 0)

    
    return storexDestroy

}





    
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {

         var logFailures: UILabel! = contentLayerDataLabel(rowClose:String(cString: [104,115,116,114,105,110,103,0], encoding: .utf8)!)

      if logFailures != nil {
          self.view.addSubview(logFailures)
          let logFailures_tag = logFailures.tag
      }
      else {
          print("logFailures is nil")      }

withUnsafeMutablePointer(to: &logFailures) { pointer in
    
}


       var screenb: Bool = false
    _ = screenb
   for _ in 0 ..< 3 {
       var scene_x8: String! = String(cString: [97,112,111,108,108,111,0], encoding: .utf8)!
      repeat {
         scene_x8.append("\(scene_x8.count >> (Swift.min(scene_x8.count, 5)))")
         if (String(cString:[100,97,49,102,52,121,51,97,0], encoding: .utf8)!) == scene_x8 {
            break
         }
      } while ((String(cString:[100,97,49,102,52,121,51,97,0], encoding: .utf8)!) == scene_x8) && (scene_x8.count > scene_x8.count)
         scene_x8 = "\(scene_x8.count & scene_x8.count)"
      while (scene_x8.count > 3) {
         scene_x8.append("\(scene_x8.count & scene_x8.count)")
         break
      }
      screenb = ((scene_x8.count & (screenb ? 56 : scene_x8.count)) >= 56)
   }

        textField.resignFirstResponder()
        return true
    }
}
