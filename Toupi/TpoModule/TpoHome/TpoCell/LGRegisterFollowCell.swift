
import Foundation


import UIKit

class LGRegisterFollowCell: UICollectionViewCell {
var advertisingScrollLength_str: String!
var client_mark: Int? = 0
private var normalizedStyleHasStr: String?




    @IBOutlet weak var photo_text_cell_imageView: UIImageView!
    @IBOutlet weak var photo_text_user_header_imageview: UIImageView!
    @IBOutlet weak var photo_text_like_tap: UIButton!
    @IBOutlet weak var photo_text_right_tap: UIButton!
    @IBOutlet weak var photo_text_user_nickName_label: UILabel!
    @IBOutlet weak var photo_text_user_center_tap: UIButton!

@discardableResult
 func systemNickMusicRateChunk(taskSelect: Int) -> Bool {
    var videosv: Bool = true
    var max_wZ: String! = String(cString: [97,95,56,55,0], encoding: .utf8)!
    var animateE: Bool = false
      max_wZ.append("\(((animateE ? 3 : 1)))")
   repeat {
      videosv = !videosv
      if videosv ? !videosv : videosv {
         break
      }
   } while (videosv ? !videosv : videosv) && (max_wZ.contains("\(videosv)"))
   if max_wZ.contains("\(animateE)") {
       var linesd: Bool = false
       var controllerss: Double = 5.0
       var animateF: Bool = false
       var agrrmmentx: Int = 2
       var bigS: [String: Any]! = [String(cString: [109,115,103,115,109,0], encoding: .utf8)!:String(cString: [100,111,119,110,0], encoding: .utf8)!]
         agrrmmentx ^= ((linesd ? 2 : 3) & Int(controllerss > 176831116.0 || controllerss < -176831116.0 ? 35.0 : controllerss))
      while (!linesd) {
         linesd = 5 <= bigS.count
         break
      }
      repeat {
         animateF = 17.57 < controllerss || !animateF
         if animateF ? !animateF : animateF {
            break
         }
      } while (controllerss >= 2.29) && (animateF ? !animateF : animateF)
         linesd = controllerss <= 81.66 || animateF
         controllerss /= Swift.max((Double(agrrmmentx * (animateF ? 1 : 1))), 5)
         bigS = ["\(bigS.keys.count)": (Int(controllerss > 120765750.0 || controllerss < -120765750.0 ? 61.0 : controllerss) - 2)]
         linesd = 85.3 <= controllerss
      repeat {
         controllerss *= (Double(Int(controllerss > 178657733.0 || controllerss < -178657733.0 ? 69.0 : controllerss) & (animateF ? 2 : 1)))
         if 4238346.0 == controllerss {
            break
         }
      } while (1 == (3 * agrrmmentx)) && (4238346.0 == controllerss)
          var savee: Double = 4.0
          var leadingJ: String! = String(cString: [115,105,98,108,105,110,103,115,0], encoding: .utf8)!
          _ = leadingJ
         bigS = ["\(linesd)": ((linesd ? 1 : 2))]
         savee /= Swift.max((Double((animateF ? 5 : 2) ^ Int(controllerss > 281006472.0 || controllerss < -281006472.0 ? 13.0 : controllerss))), 5)
         leadingJ = "\(agrrmmentx)"
      if bigS.values.contains { $0 as? Int == agrrmmentx } {
         bigS["\(linesd)"] = ((linesd ? 5 : 1) & Int(controllerss > 220978113.0 || controllerss < -220978113.0 ? 47.0 : controllerss))
      }
      if bigS["\(controllerss)"] == nil {
          var record5: [String: Any]! = [String(cString: [99,108,97,115,115,101,115,0], encoding: .utf8)!:9, String(cString: [112,97,97,100,0], encoding: .utf8)!:69]
          var homeD: Double = 5.0
         withUnsafeMutablePointer(to: &homeD) { pointer in
    
         }
          var vookU: String! = String(cString: [116,101,115,116,0], encoding: .utf8)!
         bigS = ["\(bigS.count)": bigS.count & vookU.count]
         record5 = [vookU: (vookU == (String(cString:[106,0], encoding: .utf8)!) ? vookU.count : agrrmmentx)]
         homeD /= Swift.max(2, Double(vookU.count))
      }
          var profilev: String! = String(cString: [112,101,114,99,101,112,116,117,97,108,0], encoding: .utf8)!
         linesd = !animateF
         profilev.append("\((Int(controllerss > 240029704.0 || controllerss < -240029704.0 ? 61.0 : controllerss)))")
      if linesd {
         linesd = agrrmmentx >= 62 && 62 >= bigS.keys.count
      }
         controllerss -= Double(agrrmmentx / 1)
          var followo: Double = 1.0
          _ = followo
         controllerss += Double(bigS.keys.count)
         followo *= (Double(agrrmmentx - (animateF ? 1 : 2)))
      animateE = agrrmmentx > 21
   }
   return videosv

}





    
    override func awakeFromNib() {

         var interestFint: Bool = systemNickMusicRateChunk(taskSelect:50)

      if !interestFint {
          print("slash")
      }

withUnsafeMutablePointer(to: &interestFint) { pointer in
        _ = pointer.pointee
}


       var lengthd: String! = String(cString: [117,110,101,100,105,116,97,98,108,101,0], encoding: .utf8)!
   if !lengthd.hasSuffix(lengthd) {
      lengthd.append("\(((String(cString:[84,0], encoding: .utf8)!) == lengthd ? lengthd.count : lengthd.count))")
   }

        super.awakeFromNib()
        photo_text_cell_imageView.layer.cornerRadius = 13
        photo_text_user_header_imageview.layer.cornerRadius = 16
        photo_text_user_header_imageview.layer.borderWidth = 1
        photo_text_user_header_imageview.layer.borderColor = UIColor.white.cgColor
    }

    
    func updateTextCellViews(data: STUResponsePopup) {
       var blurN: String! = String(cString: [100,101,109,111,100,117,108,97,116,101,0], encoding: .utf8)!
    var rightd: Float = 3.0
   withUnsafeMutablePointer(to: &rightd) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 2 {
      blurN.append("\(((String(cString:[97,0], encoding: .utf8)!) == blurN ? blurN.count : Int(rightd > 258133769.0 || rightd < -258133769.0 ? 93.0 : rightd)))")
   }

   if (rightd - rightd) <= 4.7 {
       var presentedR: [String: Any]! = [String(cString: [116,119,111,102,105,115,104,0], encoding: .utf8)!:[String(cString: [119,97,110,116,115,0], encoding: .utf8)!:52, String(cString: [100,105,118,105,100,105,110,103,0], encoding: .utf8)!:87, String(cString: [100,101,108,101,116,101,0], encoding: .utf8)!:38]]
       var msgd: String! = String(cString: [97,108,108,112,97,115,115,0], encoding: .utf8)!
       var rech1: String! = String(cString: [99,97,109,101,108,108,105,97,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &rech1) { pointer in
             _ = pointer.pointee
      }
       var bodyA: Double = 5.0
       var register_0f: Bool = false
       var cancel1: [Any]! = [14, 28, 10]
       _ = cancel1
      for _ in 0 ..< 1 {
          var pathsx: String! = String(cString: [101,118,101,110,97,118,103,0], encoding: .utf8)!
         presentedR["\(pathsx)"] = pathsx.count
      }
          var commentj: String! = String(cString: [98,105,116,101,120,97,99,116,0], encoding: .utf8)!
          _ = commentj
         msgd.append("\(((String(cString:[75,0], encoding: .utf8)!) == rech1 ? Int(bodyA > 306516278.0 || bodyA < -306516278.0 ? 18.0 : bodyA) : rech1.count))")
         commentj = "\(3 | cancel1.count)"
      while (rech1.hasPrefix("\(bodyA)")) {
         rech1.append("\(rech1.count)")
         break
      }
      for _ in 0 ..< 1 {
          var mapg: Bool = false
         withUnsafeMutablePointer(to: &mapg) { pointer in
                _ = pointer.pointee
         }
          var man1: Double = 3.0
         msgd = "\((rech1 == (String(cString:[117,0], encoding: .utf8)!) ? Int(bodyA > 174645519.0 || bodyA < -174645519.0 ? 36.0 : bodyA) : rech1.count))"
         mapg = presentedR.values.count == 99
         man1 /= Swift.max(Double(rech1.count + msgd.count), 5)
      }
      if msgd.count >= 1 {
         presentedR["\(bodyA)"] = (2 + Int(bodyA > 254918265.0 || bodyA < -254918265.0 ? 13.0 : bodyA))
      }
          var filedc: [Any]! = [47, 61, 62]
         withUnsafeMutablePointer(to: &filedc) { pointer in
                _ = pointer.pointee
         }
          var centersY: String! = String(cString: [115,113,108,105,116,101,115,101,115,115,105,111,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &centersY) { pointer in
    
         }
          var curveG: String! = String(cString: [111,112,116,105,111,110,0], encoding: .utf8)!
         register_0f = 78 <= filedc.count || msgd.count <= 78
         centersY.append("\(3 - msgd.count)")
         curveG.append("\(2 - msgd.count)")
      while (2 >= (cancel1.count ^ rech1.count) || (rech1.count ^ cancel1.count) >= 2) {
         rech1 = "\(rech1.count)"
         break
      }
          var rootG: String! = String(cString: [97,117,103,109,101,110,116,101,100,0], encoding: .utf8)!
         cancel1.append(presentedR.values.count)
         rootG = "\(presentedR.count & 1)"
      while (4 < (presentedR.values.count % 5)) {
         rech1 = "\(msgd.count)"
         break
      }
         rech1.append("\(2 - presentedR.keys.count)")
         rech1.append("\(3)")
      for _ in 0 ..< 2 {
         register_0f = Double(msgd.count) > bodyA
      }
      repeat {
         cancel1 = [msgd.count + presentedR.count]
         if 465325 == cancel1.count {
            break
         }
      } while (465325 == cancel1.count) && (1 <= rech1.count)
         register_0f = !msgd.hasSuffix("\(register_0f)")
      rightd /= Swift.max((Float(Int(bodyA > 295983387.0 || bodyA < -295983387.0 ? 84.0 : bodyA))), 2)
   }
        if let coverPath = data.vook_coverPath {
            photo_text_cell_imageView.sd_setImage(with: UManagerJ.displayURL(for: coverPath),
                                                  placeholderImage: UIImage(named: "eulaSpinner"))
        } else {
            photo_text_cell_imageView.image = UIImage(named: "eulaSpinner")
        }
        
        if let user = INRegisterContainer.shared.vook_userInfo(vook_userId: data.publishUserId) {
            photo_text_user_header_imageview.sd_setImage(with: URL(string: "\(MSPList.shared.vook_fileUrl)\(user.avatarUrl)"))
            photo_text_user_nickName_label.text = user.nickname
        }
        
        photo_text_like_tap.isSelected = INRegisterContainer.shared.vook_isLiked(vook_postId: data.postId)
    }
}
