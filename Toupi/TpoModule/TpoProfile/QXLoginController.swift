
import Foundation

import UIKit
import StoreKit

class QXLoginController: JZPopupController {
var changeSelectAllowed_str: String?
var camera_margin: Double? = 0.0
var model_offset: Float? = 0.0
var task_margin: Double? = 0.0



    @IBOutlet weak var collectionView: UICollectionView!
    @IBOutlet private weak var balanceLabel: UILabel!
    private let loadingView = UIView()
    private let loadingIndicator = UIActivityIndicatorView(style: .large)
    
    private let packages: [ELProfile] = [ 
        ELProfile(productId: "phqnswbvxeahnicm", diamonds: 800, fallbackPrice: "$0.99"),
        ELProfile(productId: "imsplntloboabdyo", diamonds: 1600, fallbackPrice: "$1.99"),
        ELProfile(productId: "gxifodyevxhgerrz", diamonds: 4900, fallbackPrice: "$4.99"),
        ELProfile(productId: "prdyvhvxcrgmvhqm", diamonds: 10300, fallbackPrice: "$9.99"),
        ELProfile(productId: "ekrphdyvyvyjyqbq", diamonds: 21600, fallbackPrice: "$19.99"),
        ELProfile(productId: "ordsjfhwjehqwebf", diamonds: 32600, fallbackPrice: "$29.99"),
        ELProfile(productId: "nshgdkuqwgqashrg", diamonds: 46600, fallbackPrice: "$39.99"),
        ELProfile(productId: "ekxqdwgdvxexcymk", diamonds: 58800, fallbackPrice: "$49.99"),
        ELProfile(productId: "guqjwjrnvtbktgcc", diamonds: 127600, fallbackPrice: "$99.99")
    ]
    
    private var productsById: [String: Product] = [:]
    private var updatesTask: Task<Void, Never>?

@discardableResult
 func decodeRowAnimateFollowingBigTableView() -> UITableView! {
    var containsa: [String: Any]! = [String(cString: [101,110,111,117,103,104,0], encoding: .utf8)!:UILabel(frame:CGRect.zero)]
    var commentsj: Float = 1.0
   withUnsafeMutablePointer(to: &commentsj) { pointer in
    
   }
      commentsj /= Swift.max((Float(Int(commentsj > 90152362.0 || commentsj < -90152362.0 ? 76.0 : commentsj) ^ containsa.count)), 2)
     let playLast: [String: Any]! = [String(cString: [108,111,110,0], encoding: .utf8)!:76, String(cString: [101,108,97,115,116,105,99,0], encoding: .utf8)!:22]
     var gnewsScene: String! = String(cString: [97,114,107,101,114,0], encoding: .utf8)!
     var willLike: Double = 23.0
    var subframesNegotiateInterrupted = UITableView()
    subframesNegotiateInterrupted.dataSource = nil
    subframesNegotiateInterrupted.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    subframesNegotiateInterrupted.delegate = nil
    subframesNegotiateInterrupted.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    subframesNegotiateInterrupted.alpha = 0.5
    subframesNegotiateInterrupted.frame = CGRect(x: 154, y: 53, width: 0, height: 0)

    
    return subframesNegotiateInterrupted

}





    
    private func handleTransactionResult(_ result: VerificationResult<Transaction>) async {

         var overAlias: UITableView! = decodeRowAnimateFollowingBigTableView()

      if overAlias != nil {
          self.view.addSubview(overAlias)
          let overAlias_tag = overAlias.tag
      }

withUnsafeMutablePointer(to: &overAlias) { pointer in
        _ = pointer.pointee
}


       var coverT: String! = String(cString: [116,116,114,105,98,117,116,101,100,0], encoding: .utf8)!
       var mediaW: String! = String(cString: [115,113,108,108,111,103,0], encoding: .utf8)!
          var homepaged: Int = 1
         withUnsafeMutablePointer(to: &homepaged) { pointer in
                _ = pointer.pointee
         }
          var isoz: Double = 2.0
         withUnsafeMutablePointer(to: &isoz) { pointer in
                _ = pointer.pointee
         }
         mediaW = "\(1)"
         homepaged %= Swift.max(homepaged & 3, 3)
         isoz /= Swift.max((Double(3 + Int(isoz > 225719124.0 || isoz < -225719124.0 ? 42.0 : isoz))), 3)
         mediaW = "\(mediaW.count)"
         mediaW = "\((mediaW == (String(cString:[69,0], encoding: .utf8)!) ? mediaW.count : mediaW.count))"
      coverT = "\(coverT.count)"

        switch result {
        case .verified(let transaction):
            await grantDiamondsIfNeeded(for: transaction)
            await transaction.finish()
            await MainActor.run {
                setLoading(false)
            }
        case .unverified:
            await MainActor.run {
                setLoading(false)
                YCommentPhoto.showInfo("Purchase verification failed")
            }
        }
    }
    
    deinit {
        updatesTask?.cancel()
    }

@discardableResult
 func iconOpaqueHeightSaveRadiusButton(centerPreview: Float, pickerSpinner: Bool) -> UIButton! {
    var scene_tr: Float = 3.0
   withUnsafeMutablePointer(to: &scene_tr) { pointer in
    
   }
    var containerW: [String: Any]! = [String(cString: [104,111,115,116,110,97,109,101,0], encoding: .utf8)!:63, String(cString: [115,116,114,105,112,115,0], encoding: .utf8)!:3, String(cString: [101,109,98,101,100,100,105,110,103,0], encoding: .utf8)!:19]
    _ = containerW
      scene_tr += Float(1)
      containerW = ["\(containerW.keys.count)": containerW.values.count]
     let coverBirthday: [Any]! = [4, 99, 34]
     let spacingCreated: String! = String(cString: [115,105,122,101,0], encoding: .utf8)!
     let harassmentUpdate_xl: String! = String(cString: [121,101,108,108,111,119,0], encoding: .utf8)!
    var ifftDemuxersMjpegdec:UIButton! = UIButton(frame:CGRect.zero)
    ifftDemuxersMjpegdec.alpha = 1.0;
    ifftDemuxersMjpegdec.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    ifftDemuxersMjpegdec.frame = CGRect(x: 81, y: 118, width: 0, height: 0)
    ifftDemuxersMjpegdec.setImage(UIImage(named:String(cString: [111,118,101,114,108,97,112,0], encoding: .utf8)!), for: .normal)
    ifftDemuxersMjpegdec.setTitle("", for: .normal)
    ifftDemuxersMjpegdec.setBackgroundImage(UIImage(named:String(cString: [105,110,116,95,114,0], encoding: .utf8)!), for: .normal)
    ifftDemuxersMjpegdec.titleLabel?.font = UIFont.systemFont(ofSize:20)

    
    return ifftDemuxersMjpegdec

}





    
    private func loadProducts() {

         var bnhexCalculated: UIButton! = iconOpaqueHeightSaveRadiusButton(centerPreview:84.0, pickerSpinner:false)

      if bnhexCalculated != nil {
          self.view.addSubview(bnhexCalculated)
          let bnhexCalculated_tag = bnhexCalculated.tag
      }
      else {
          print("bnhexCalculated is nil")      }

withUnsafeMutablePointer(to: &bnhexCalculated) { pointer in
        _ = pointer.pointee
}


       var likes9: String! = String(cString: [116,102,100,116,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &likes9) { pointer in
    
   }
   while (likes9.hasSuffix("\(likes9.count)")) {
      likes9.append("\(((String(cString:[118,0], encoding: .utf8)!) == likes9 ? likes9.count : likes9.count))")
      break
   }

        Task { [weak self] in
            guard let self = self else { return }
            do {
                let hotd = self.packages.map { $0.productId }
                let spacing = try await Product.products(for: hotd)
                let x_width = Dictionary(uniqueKeysWithValues: spacing.map { ($0.id, $0) })
                
                await MainActor.run {
                    self.productsById = x_width
                    self.collectionView.reloadData()
                }
            } catch {
                await MainActor.run {
                    YCommentPhoto.showInfo("Unable to load products")
                }
            }
        }
    }

@discardableResult
 func alwaysClearCacheActionEndpointLabel(observeLine: Double) -> UILabel! {
    var keyboardp: [Any]! = [73, 91, 86]
    var follow7: Double = 4.0
   withUnsafeMutablePointer(to: &follow7) { pointer in
          _ = pointer.pointee
   }
   while (!keyboardp.contains { $0 as? Double == follow7 }) {
      follow7 += (Double(Int(follow7 > 20634351.0 || follow7 < -20634351.0 ? 16.0 : follow7) % (Swift.max(3, keyboardp.count))))
      break
   }
     let pictureMore: UIView! = UIView(frame:CGRect(x: 159, y: 325, width: 0, height: 0))
     let observerWoman: String! = String(cString: [105,110,100,101,110,116,97,116,105,111,110,0], encoding: .utf8)!
     let rightFrame_pg: Float = 75.0
    var insideCooldown:UILabel! = UILabel(frame:CGRect(x: 319, y: 252, width: 0, height: 0))
    insideCooldown.text = ""
    insideCooldown.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    insideCooldown.textAlignment = .center
    insideCooldown.font = UIFont.systemFont(ofSize:13)
    insideCooldown.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    insideCooldown.alpha = 0.9
    insideCooldown.frame = CGRect(x: 274, y: 272, width: 0, height: 0)
    pictureMore.alpha = 0.9;
    pictureMore.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pictureMore.frame = CGRect(x: 2, y: 138, width: 0, height: 0)
    

    
    return insideCooldown

}





    
    override func viewDidLoad() {

         var snapshotHaldclut: UILabel! = alwaysClearCacheActionEndpointLabel(observeLine:37.0)

      if snapshotHaldclut != nil {
          self.view.addSubview(snapshotHaldclut)
          let snapshotHaldclut_tag = snapshotHaldclut.tag
      }

withUnsafeMutablePointer(to: &snapshotHaldclut) { pointer in
        _ = pointer.pointee
}


       var arrayv: String! = String(cString: [115,117,98,109,111,100,101,108,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      arrayv = "\(arrayv.count & 1)"
   }

        super.viewDidLoad()
        setup_collectionView()
        setupLoadingView()
        updateBalance()
        loadProducts()
        observeTransactions()
    }

@discardableResult
 func heightTimescaleNowFinishConfirm(recommendBanner: String!) -> [String: Any]! {
    var auto_fQ: Float = 4.0
    var navin: String! = String(cString: [110,101,116,115,0], encoding: .utf8)!
    var constraintsH: [String: Any]! = [String(cString: [115,116,114,116,111,108,108,0], encoding: .utf8)!:79, String(cString: [108,101,118,105,110,115,111,110,0], encoding: .utf8)!:53]
   withUnsafeMutablePointer(to: &constraintsH) { pointer in
          _ = pointer.pointee
   }
   for _ in 0 ..< 3 {
      constraintsH = ["\(constraintsH.values.count)": 1]
   }
   repeat {
       var popupF: String! = String(cString: [109,105,120,105,110,0], encoding: .utf8)!
      while (popupF != popupF) {
          var refreshY: Float = 2.0
          var endpointq: Bool = false
          var othersJ: String! = String(cString: [100,97,114,107,101,110,105,110,103,0], encoding: .utf8)!
          var charactersO: String! = String(cString: [97,100,100,102,0], encoding: .utf8)!
         popupF.append("\(2 >> (Swift.min(2, popupF.count)))")
         refreshY += (Float(popupF == (String(cString:[88,0], encoding: .utf8)!) ? popupF.count : Int(refreshY > 70388773.0 || refreshY < -70388773.0 ? 96.0 : refreshY)))
         endpointq = 44 == popupF.count && refreshY == 26.81
         othersJ.append("\((charactersO == (String(cString:[116,0], encoding: .utf8)!) ? charactersO.count : othersJ.count))")
         break
      }
         popupF.append("\(popupF.count)")
      while (!popupF.hasSuffix(popupF)) {
          var dataJ: String! = String(cString: [103,117,105,100,115,0], encoding: .utf8)!
          var editingi: String! = String(cString: [107,105,110,103,0], encoding: .utf8)!
          var bottomc: Double = 4.0
         withUnsafeMutablePointer(to: &bottomc) { pointer in
                _ = pointer.pointee
         }
          var commentsp: Double = 2.0
          var hostE: String! = String(cString: [116,104,114,111,117,103,104,112,117,116,0], encoding: .utf8)!
         popupF.append("\(((String(cString:[99,0], encoding: .utf8)!) == popupF ? dataJ.count : popupF.count))")
         editingi = "\((popupF == (String(cString:[52,0], encoding: .utf8)!) ? popupF.count : editingi.count))"
         bottomc += Double(2 << (Swift.min(2, dataJ.count)))
         commentsp += Double(2)
         hostE = "\(dataJ.count * 2)"
         break
      }
      auto_fQ -= (Float(Int(auto_fQ > 84290701.0 || auto_fQ < -84290701.0 ? 19.0 : auto_fQ)))
      if 1672024.0 == auto_fQ {
         break
      }
   } while (1672024.0 == auto_fQ) && (auto_fQ == Float(navin.count))
      constraintsH["\(navin)"] = 1 << (Swift.min(5, navin.count))
   return constraintsH

}





    
    private func updateBalance() {

         var parserBootstrap: [String: Any]! = heightTimescaleNowFinishConfirm(recommendBanner:String(cString: [97,97,99,112,115,100,115,112,0], encoding: .utf8)!)

      parserBootstrap.forEach({ (key, value) in
          print(key)
          print(value)
      })
      var parserBootstrap_len = parserBootstrap.count

withUnsafeMutablePointer(to: &parserBootstrap) { pointer in
        _ = pointer.pointee
}


       var backi: String! = String(cString: [99,108,97,115,115,105,99,0], encoding: .utf8)!
    _ = backi
       var selectU: String! = String(cString: [103,101,110,101,114,97,108,105,115,101,100,0], encoding: .utf8)!
       _ = selectU
       var setupT: [String: Any]! = [String(cString: [109,101,109,100,101,98,117,103,0], encoding: .utf8)!:40, String(cString: [116,101,115,101,100,103,101,0], encoding: .utf8)!:41]
       var keysO: String! = String(cString: [105,110,116,101,103,101,114,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &keysO) { pointer in
             _ = pointer.pointee
      }
      if 5 <= keysO.count || selectU != String(cString:[70,0], encoding: .utf8)! {
         selectU.append("\(((String(cString:[90,0], encoding: .utf8)!) == keysO ? keysO.count : setupT.count))")
      }
         keysO.append("\(2)")
          var selectedB: Bool = true
          _ = selectedB
         keysO = "\(2 >> (Swift.min(5, selectU.count)))"
         selectedB = selectU.count < 74
      if (selectU.count & 2) == 3 {
         selectU = "\(setupT.count)"
      }
          var firstZ: Double = 0.0
          _ = firstZ
         keysO = "\(keysO.count)"
         firstZ -= Double(setupT.values.count)
      backi = "\(backi.count)"

        balanceLabel.text = "\(YEdit.balance())"
    }

    
    private func observeTransactions() {
       var window__R: Int = 4
    var storage2: Int = 3
   while (5 > (storage2 ^ 2) && 2 > (window__R ^ storage2)) {
      storage2 >>= Swift.min(4, labs(2 | window__R))
      break
   }

        updatesTask = Task { [weak self] in
            for await result in Transaction.updates {
                await self?.handleTransactionResult(result)
            }
        }
        
        Task { [weak self] in
            for await result in Transaction.unfinished {
                await self?.handleTransactionResult(result)
            }
        }
      window__R |= 3
    }

    
    func setup_collectionView() {
       var publishO: Double = 5.0
      publishO /= Swift.max(1, (Double(2 / (Swift.max(Int(publishO > 388660874.0 || publishO < -388660874.0 ? 54.0 : publishO), 1)))))

        let users = UICollectionViewFlowLayout()
        users.minimumLineSpacing = 11
        users.minimumInteritemSpacing = 11
        users.scrollDirection = .vertical
        users.sectionInset = UIEdgeInsets(top: 0, left: 16, bottom: 0, right: 16)
        
        collectionView.collectionViewLayout = users
        collectionView.dataSource = self
        collectionView.delegate = self
        collectionView.backgroundColor = .clear
        collectionView.register(UINib(nibName: "OPlayerContainerCell", bundle: nil), forCellWithReuseIdentifier: "rech")
    }

    
    private func purchase(_ package: ELProfile) {
       var reportE: Bool = true
   withUnsafeMutablePointer(to: &reportE) { pointer in
    
   }
   while (!reportE) {
      reportE = !reportE && reportE
      break
   }

        guard let product = productsById[package.productId] else {
            YCommentPhoto.showInfo("Product unavailable")
            return
        }
        
        setLoading(true)
        
        Task { [weak self] in
            do {
                let result = try await product.purchase()
                switch result {
                case .success(let verification):
                    await self?.handleTransactionResult(verification)
                case .userCancelled:
                    await MainActor.run {
                        self?.setLoading(false)
                    }
                    break
                case .pending:
                    await MainActor.run {
                        self?.setLoading(false)
                        YCommentPhoto.showInfo("Purchase pending")
                    }
                @unknown default:
                    await MainActor.run {
                        self?.setLoading(false)
                    }
                    break
                }
            } catch {
                await MainActor.run {
                    self?.setLoading(false)
                    YCommentPhoto.showInfo("Purchase failed")
                }
            }
        }
    }

@discardableResult
 func locationMediumPageReviewReceive(viewTab: String!, postSelected: String!, blacksMust: [Any]!) -> String! {
    var modityI: Bool = false
    var chunkS: Double = 3.0
    _ = chunkS
    var entityq: String! = String(cString: [114,101,101,108,0], encoding: .utf8)!
   repeat {
      modityI = !modityI
      if modityI ? !modityI : modityI {
         break
      }
   } while (modityI) && (modityI ? !modityI : modityI)
      chunkS -= (Double(Int(chunkS > 238614646.0 || chunkS < -238614646.0 ? 45.0 : chunkS) ^ 3))
   repeat {
       var pointL: [Any]! = [true]
       var usersf: Bool = false
      withUnsafeMutablePointer(to: &usersf) { pointer in
             _ = pointer.pointee
      }
       var unfinishedT: String! = String(cString: [105,110,100,101,112,101,110,100,101,100,0], encoding: .utf8)!
       _ = unfinishedT
       var settingQ: String! = String(cString: [105,110,116,101,114,102,114,97,109,101,0], encoding: .utf8)!
       _ = settingQ
       var taskk: [Any]! = [74.0]
      while (unfinishedT.hasSuffix("\(pointL.count)")) {
         pointL.append(taskk.count)
         break
      }
      repeat {
         usersf = taskk.count >= 37 && usersf
         if usersf ? !usersf : usersf {
            break
         }
      } while (usersf) && (usersf ? !usersf : usersf)
      for _ in 0 ..< 3 {
         taskk.append(unfinishedT.count)
      }
      for _ in 0 ..< 3 {
         usersf = pointL.count >= settingQ.count
      }
      while (!settingQ.contains("\(taskk.count)")) {
          var collection9: [Any]! = [UILabel(frame:CGRect.zero)]
          var description_v4: String! = String(cString: [99,104,97,110,103,101,100,104,0], encoding: .utf8)!
          _ = description_v4
          var videok: Float = 1.0
          _ = videok
          var birthdayL: [String: Any]! = [String(cString: [116,111,109,99,114,121,112,116,0], encoding: .utf8)!:42.0]
         withUnsafeMutablePointer(to: &birthdayL) { pointer in
    
         }
          var home2: [Any]! = [13, 47, 66]
         withUnsafeMutablePointer(to: &home2) { pointer in
                _ = pointer.pointee
         }
         settingQ = "\(settingQ.count * birthdayL.keys.count)"
         collection9 = [1 & taskk.count]
         description_v4 = "\(2 & home2.count)"
         videok -= Float(3)
         home2.append(description_v4.count)
         break
      }
          var setupz: Int = 3
          _ = setupz
         unfinishedT = "\(taskk.count)"
         setupz %= Swift.max(taskk.count | 2, 2)
         unfinishedT = "\(pointL.count % (Swift.max(3, 6)))"
      if !settingQ.hasSuffix("\(usersf)") {
          var characters5: Bool = true
         withUnsafeMutablePointer(to: &characters5) { pointer in
    
         }
          var propressO: Double = 1.0
         settingQ.append("\(3)")
         characters5 = unfinishedT.count > 34
         propressO -= (Double(unfinishedT == (String(cString:[53,0], encoding: .utf8)!) ? unfinishedT.count : taskk.count))
      }
      if (unfinishedT.count | taskk.count) > 3 {
          var lengthP: Float = 3.0
          var secretc: Double = 5.0
          var centersH: Double = 3.0
          var camerao: String! = String(cString: [115,111,108,105,100,99,111,108,111,114,0], encoding: .utf8)!
          var requstT: Int = 2
         unfinishedT.append("\(2 + camerao.count)")
         lengthP += Float(settingQ.count * 2)
         secretc -= (Double(camerao == (String(cString:[48,0], encoding: .utf8)!) ? camerao.count : Int(secretc > 348067730.0 || secretc < -348067730.0 ? 23.0 : secretc)))
         centersH += Double(unfinishedT.count)
         requstT %= Swift.max(((String(cString:[55,0], encoding: .utf8)!) == camerao ? Int(lengthP > 209386904.0 || lengthP < -209386904.0 ? 56.0 : lengthP) : camerao.count), 4)
      }
      repeat {
         usersf = unfinishedT.count < 24 && usersf
         if usersf ? !usersf : usersf {
            break
         }
      } while (usersf) && (usersf ? !usersf : usersf)
      for _ in 0 ..< 2 {
          var violent7: String! = String(cString: [99,97,112,105,116,97,108,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &violent7) { pointer in
    
         }
         settingQ.append("\(1 + pointL.count)")
         violent7.append("\(((usersf ? 5 : 3) % (Swift.max(1, 9))))")
      }
      if (1 & pointL.count) == 3 && !usersf {
         usersf = (pointL.count + unfinishedT.count) == 5
      }
      if 2 < settingQ.count {
         settingQ.append("\(((String(cString:[74,0], encoding: .utf8)!) == settingQ ? settingQ.count : (usersf ? 5 : 4)))")
      }
         pointL.append(2 << (Swift.min(3, unfinishedT.count)))
       var self_rs: String! = String(cString: [99,111,109,112,108,101,116,101,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &self_rs) { pointer in
             _ = pointer.pointee
      }
         self_rs.append("\(((usersf ? 4 : 1) / (Swift.max(9, taskk.count))))")
      entityq.append("\(settingQ.count)")
      if 3091022 == entityq.count {
         break
      }
   } while ((3.40 - chunkS) < 3.52) && (3091022 == entityq.count)
   return entityq

}





    
    private func grantDiamondsIfNeeded(for transaction: Transaction) async {

         let sigmaContents: String! = locationMediumPageReviewReceive(viewTab:String(cString: [105,110,112,99,98,0], encoding: .utf8)!, postSelected:String(cString: [117,110,114,101,118,101,114,115,101,100,0], encoding: .utf8)!, blacksMust:[21, 27])

      let sigmaContents_len = sigmaContents?.count ?? 0
      if sigmaContents == "post" {
              print(sigmaContents)
      }

_ = sigmaContents


       var rechU: Double = 0.0
    var constraintsM: [Any]! = [[String(cString: [99,111,110,115,111,108,101,0], encoding: .utf8)!:String(cString: [111,112,97,116,111,109,0], encoding: .utf8)!, String(cString: [99,117,114,114,0], encoding: .utf8)!:String(cString: [102,114,111,109,102,101,0], encoding: .utf8)!, String(cString: [114,101,109,105,120,105,110,103,0], encoding: .utf8)!:String(cString: [112,108,111,116,116,101,114,0], encoding: .utf8)!]]
    _ = constraintsM
      rechU += (Double(constraintsM.count | Int(rechU > 307725616.0 || rechU < -307725616.0 ? 10.0 : rechU)))

        guard let package = packages.first(where: { $0.productId == transaction.productID }) else { return }
        let follow = "vook_iap_transaction_\(transaction.id)"
        guard UserDefaults.standard.bool(forKey: follow) == false else { return }
        
        await MainActor.run {
            YEdit.add(package.diamonds)
   repeat {
      constraintsM = [2]
      if 2125101 == constraintsM.count {
         break
      }
   } while (2125101 == constraintsM.count) && (!constraintsM.contains { $0 as? Double == rechU })
            UserDefaults.standard.set(true, forKey: follow)
            updateBalance()
            collectionView.reloadData()
            YCommentPhoto.showInfo("Purchase successful")
        }
    }

    
    private func setLoading(_ isLoading: Bool) {
       var infiniteW: String! = String(cString: [116,104,114,109,97,116,0], encoding: .utf8)!
       var publishw: Float = 0.0
       var gift8: Double = 1.0
       var delegate_nZ: [String: Any]! = [String(cString: [115,117,98,112,97,99,107,101,116,115,0], encoding: .utf8)!:String(cString: [112,101,101,114,115,0], encoding: .utf8)!, String(cString: [118,108,105,110,101,0], encoding: .utf8)!:String(cString: [98,97,99,107,112,116,114,0], encoding: .utf8)!]
          var information3: String! = String(cString: [119,111,114,107,97,114,111,117,110,100,0], encoding: .utf8)!
          var popupF: [String: Any]! = [String(cString: [103,101,116,116,105,110,103,0], encoding: .utf8)!:String(cString: [114,97,109,112,117,112,0], encoding: .utf8)!, String(cString: [110,97,118,105,103,97,116,101,0], encoding: .utf8)!:String(cString: [109,105,120,0], encoding: .utf8)!, String(cString: [114,101,97,108,108,111,99,112,0], encoding: .utf8)!:String(cString: [100,97,121,0], encoding: .utf8)!]
          var backR: String! = String(cString: [109,111,116,105,111,110,112,105,120,101,108,115,0], encoding: .utf8)!
         gift8 += (Double(2 - Int(gift8 > 31664552.0 || gift8 < -31664552.0 ? 44.0 : gift8)))
         information3.append("\(delegate_nZ.keys.count)")
         popupF["\(gift8)"] = (Int(gift8 > 368381326.0 || gift8 < -368381326.0 ? 39.0 : gift8) + delegate_nZ.values.count)
         backR = "\(delegate_nZ.count & 2)"
       var keyc: String! = String(cString: [112,111,111,108,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &keyc) { pointer in
             _ = pointer.pointee
      }
      repeat {
          var currentO: String! = String(cString: [101,110,99,111,100,101,109,98,0], encoding: .utf8)!
          var edit7: Int = 3
         withUnsafeMutablePointer(to: &edit7) { pointer in
                _ = pointer.pointee
         }
          var publisha: Double = 5.0
          var record1: Double = 1.0
          var viewse: String! = String(cString: [102,114,101,113,98,97,114,107,0], encoding: .utf8)!
         gift8 /= Swift.max(2, (Double(Int(record1 > 53715531.0 || record1 < -53715531.0 ? 81.0 : record1))))
         currentO.append("\((viewse == (String(cString:[101,0], encoding: .utf8)!) ? viewse.count : Int(gift8 > 291002889.0 || gift8 < -291002889.0 ? 77.0 : gift8)))")
         edit7 &= 1
         publisha -= Double(1)
         if 3573447.0 == gift8 {
            break
         }
      } while ((2 + delegate_nZ.keys.count) > 2) && (3573447.0 == gift8)
          var photo3: Int = 3
          var itemsq: [String: Any]! = [String(cString: [102,105,110,101,0], encoding: .utf8)!:96, String(cString: [112,105,110,103,0], encoding: .utf8)!:85]
          _ = itemsq
          var shoulde: Float = 1.0
         publishw /= Swift.max((Float(Int(gift8 > 40178077.0 || gift8 < -40178077.0 ? 78.0 : gift8))), 1)
         photo3 &= delegate_nZ.values.count + 3
         itemsq["\(shoulde)"] = (Int(shoulde > 308664390.0 || shoulde < -308664390.0 ? 24.0 : shoulde) << (Swift.min(itemsq.count, 3)))
          var wasF: Int = 2
          var firstN: String! = String(cString: [105,110,116,101,114,114,117,112,116,0], encoding: .utf8)!
         keyc.append("\(keyc.count % 1)")
         wasF %= Swift.max(2, 2)
         firstN.append("\((Int(gift8 > 121746160.0 || gift8 < -121746160.0 ? 45.0 : gift8)))")
         keyc.append("\(delegate_nZ.keys.count % 1)")
      while (keyc.count == 1) {
         delegate_nZ["\(publishw)"] = 1
         break
      }
      while (4.24 < (gift8 / (Swift.max(6, Double(keyc.count)))) || (gift8 / (Swift.max(4.24, 5))) < 2.41) {
         keyc.append("\((Int(publishw > 248913874.0 || publishw < -248913874.0 ? 96.0 : publishw)))")
         break
      }
          var posts7: Double = 2.0
         withUnsafeMutablePointer(to: &posts7) { pointer in
                _ = pointer.pointee
         }
          var self_1kc: String! = String(cString: [102,99,104,111,119,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &self_1kc) { pointer in
                _ = pointer.pointee
         }
          var migrationj: Bool = false
         publishw += Float(3 - delegate_nZ.values.count)
         posts7 -= Double(2)
         self_1kc = "\(3)"
      infiniteW = "\((3 | Int(publishw > 310456509.0 || publishw < -310456509.0 ? 55.0 : publishw)))"

        loadingView.isHidden = isLoading == false
        collectionView.isUserInteractionEnabled = isLoading == false
        if isLoading {
            loadingIndicator.startAnimating()
        } else {
            loadingIndicator.stopAnimating()
        }
    }

@discardableResult
 func hotAnchorResultOutletImageView(lastBase: Float, bannerLogin: Int) -> UIImageView! {
    var map0: Int = 1
   withUnsafeMutablePointer(to: &map0) { pointer in
          _ = pointer.pointee
   }
    var pages: Bool = false
   while (3 <= (map0 % (Swift.max(2, 4)))) {
       var buttonse: String! = String(cString: [114,101,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &buttonse) { pointer in
    
      }
       var generator3: String! = String(cString: [107,115,101,116,0], encoding: .utf8)!
       var recommendD: String! = String(cString: [100,98,111,111,108,104,117,102,102,0], encoding: .utf8)!
       _ = recommendD
          var scalarT: [Any]! = [97, 98]
          var emptyX: String! = String(cString: [99,104,97,110,110,101,108,0], encoding: .utf8)!
          var parametersN: Int = 1
          _ = parametersN
         generator3 = "\(recommendD.count % 3)"
         scalarT.append((buttonse == (String(cString:[112,0], encoding: .utf8)!) ? buttonse.count : parametersN))
         emptyX.append("\(buttonse.count ^ 2)")
         parametersN /= Swift.max(2, emptyX.count >> (Swift.min(labs(2), 2)))
      repeat {
         generator3 = "\(generator3.count >> (Swift.min(recommendD.count, 3)))"
         if (String(cString:[108,112,109,122,112,108,53,112,0], encoding: .utf8)!) == generator3 {
            break
         }
      } while (!generator3.hasSuffix("\(recommendD.count)")) && ((String(cString:[108,112,109,122,112,108,53,112,0], encoding: .utf8)!) == generator3)
      for _ in 0 ..< 1 {
         generator3.append("\(2 | generator3.count)")
      }
      if recommendD != generator3 {
          var int__v: Float = 5.0
          _ = int__v
          var black5: String! = String(cString: [114,101,115,116,97,114,116,0], encoding: .utf8)!
          _ = black5
         generator3 = "\(3 + generator3.count)"
         int__v -= Float(buttonse.count & 3)
         black5 = "\(3 | recommendD.count)"
      }
         generator3.append("\(recommendD.count)")
      while (buttonse.contains("\(recommendD.count)")) {
         buttonse.append("\(recommendD.count / (Swift.max(10, generator3.count)))")
         break
      }
      if !generator3.hasSuffix(buttonse) {
         buttonse.append("\(recommendD.count)")
      }
          var max_9i: String! = String(cString: [115,116,114,105,100,105,110,103,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &max_9i) { pointer in
                _ = pointer.pointee
         }
          var recordingS: Double = 1.0
          _ = recordingS
         generator3.append("\(2 ^ recommendD.count)")
         max_9i.append("\(generator3.count & recommendD.count)")
         recordingS -= (Double(Int(recordingS > 314796153.0 || recordingS < -314796153.0 ? 3.0 : recordingS)))
          var shareQ: String! = String(cString: [98,114,101,97,100,0], encoding: .utf8)!
         buttonse.append("\(((String(cString:[75,0], encoding: .utf8)!) == shareQ ? shareQ.count : recommendD.count))")
      pages = buttonse.count <= 52
      break
   }
      map0 &= 3
     var signNew_o5: String! = String(cString: [115,104,101,108,108,0], encoding: .utf8)!
     var homeSetup: UIImageView! = UIImageView(image:UIImage(named:String(cString: [100,101,108,101,116,101,100,0], encoding: .utf8)!))
     var refreshPreferred: [Any]! = [87, 76]
    var openEvtag = UIImageView()
    openEvtag.contentMode = .scaleAspectFit
    openEvtag.animationRepeatCount = 7
    openEvtag.image = UIImage(named:String(cString: [114,101,99,111,114,100,101,114,0], encoding: .utf8)!)
    openEvtag.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    openEvtag.alpha = 0.9
    openEvtag.frame = CGRect(x: 305, y: 294, width: 0, height: 0)
    homeSetup.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    homeSetup.alpha = 0.2
    homeSetup.frame = CGRect(x: 181, y: 19, width: 0, height: 0)
    homeSetup.image = UIImage(named:String(cString: [115,108,111,116,0], encoding: .utf8)!)
    homeSetup.contentMode = .scaleAspectFit
    homeSetup.animationRepeatCount = 8
    

    
    return openEvtag

}





    
    private func setupLoadingView() {

         let collectedInsertion: UIImageView! = hotAnchorResultOutletImageView(lastBase:64.0, bannerLogin:0)

      if collectedInsertion != nil {
          let collectedInsertion_tag = collectedInsertion.tag
          self.view.addSubview(collectedInsertion)
      }

_ = collectedInsertion


       var agrrmmentn: Bool = false
    var pricee: [Any]! = [19, 38, 2]
      agrrmmentn = pricee.count == 2 || !agrrmmentn

      agrrmmentn = pricee.count <= 32
        loadingView.backgroundColor = UIColor.black.withAlphaComponent(0.42)
        loadingView.isHidden = true
        loadingView.translatesAutoresizingMaskIntoConstraints = false
        view.addSubview(loadingView)
        
        loadingIndicator.color = .white
        loadingIndicator.hidesWhenStopped = true
        loadingIndicator.translatesAutoresizingMaskIntoConstraints = false
        loadingView.addSubview(loadingIndicator)
        
        NSLayoutConstraint.activate([
            loadingView.topAnchor.constraint(equalTo: view.topAnchor),
            loadingView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
            loadingView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
            loadingView.bottomAnchor.constraint(equalTo: view.bottomAnchor),
            
            loadingIndicator.centerXAnchor.constraint(equalTo: loadingView.centerXAnchor),
            loadingIndicator.centerYAnchor.constraint(equalTo: loadingView.centerYAnchor)
        ])
    }

}

extension QXLoginController: UICollectionViewDelegate, UICollectionViewDataSource, UICollectionViewDelegateFlowLayout {

@discardableResult
 func parseExecuteObserveFieldLoadGravityScrollView() -> UIScrollView! {
    var supure6: String! = String(cString: [101,110,99,111,100,101,109,118,0], encoding: .utf8)!
    var buttonl: String! = String(cString: [109,111,118,116,101,120,116,115,117,98,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &buttonl) { pointer in
    
   }
       var messageh: String! = String(cString: [100,111,112,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &messageh) { pointer in
    
      }
      if messageh == messageh {
          var errorp: [String: Any]! = [String(cString: [105,110,105,116,105,97,108,108,121,0], encoding: .utf8)!:87, String(cString: [115,99,97,108,101,114,0], encoding: .utf8)!:45]
         messageh = "\(messageh.count)"
         errorp = ["\(errorp.count)": errorp.values.count | 2]
      }
         messageh.append("\(3 << (Swift.min(2, messageh.count)))")
      while (messageh == messageh) {
         messageh.append("\((messageh == (String(cString:[100,0], encoding: .utf8)!) ? messageh.count : messageh.count))")
         break
      }
      supure6.append("\(supure6.count | 2)")
      supure6 = "\(supure6.count / 3)"
      buttonl = "\(buttonl.count / 2)"
     let logoutShould: [String: Any]! = [String(cString: [116,119,111,112,111,105,110,116,0], encoding: .utf8)!:[String(cString: [102,114,97,109,101,114,97,116,101,0], encoding: .utf8)!:79, String(cString: [100,101,115,99,114,105,112,116,111,114,0], encoding: .utf8)!:35, String(cString: [115,121,109,98,111,108,105,99,97,116,101,100,0], encoding: .utf8)!:0]]
     var decoderWindow_o: [Any]! = [String(cString: [114,101,100,117,99,101,114,0], encoding: .utf8)!]
     let cleanUpdating: Float = 46.0
     var sanitizedCamera: UIView! = UIView()
    var widescreenMixerEyedropper:UIScrollView! = UIScrollView(frame:CGRect.zero)
    sanitizedCamera.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sanitizedCamera.alpha = 0.3
    sanitizedCamera.frame = CGRect(x: 291, y: 276, width: 0, height: 0)
    
    widescreenMixerEyedropper.addSubview(sanitizedCamera)
    widescreenMixerEyedropper.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    widescreenMixerEyedropper.alwaysBounceVertical = true
    widescreenMixerEyedropper.alwaysBounceHorizontal = false
    widescreenMixerEyedropper.showsVerticalScrollIndicator = false
    widescreenMixerEyedropper.showsHorizontalScrollIndicator = true
    widescreenMixerEyedropper.delegate = nil
    widescreenMixerEyedropper.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    widescreenMixerEyedropper.alpha = 0.7
    widescreenMixerEyedropper.frame = CGRect(x: 240, y: 223, width: 0, height: 0)

    
    return widescreenMixerEyedropper

}





    
    func collectionView(_ collectionView: UICollectionView, numberOfItemsInSection section: Int) -> Int {

         var enclosingIndefinitely: UIScrollView! = parseExecuteObserveFieldLoadGravityScrollView()

      if enclosingIndefinitely != nil {
          let enclosingIndefinitely_tag = enclosingIndefinitely.tag
          self.view.addSubview(enclosingIndefinitely)
      }
      else {
          print("enclosingIndefinitely is nil")      }

withUnsafeMutablePointer(to: &enclosingIndefinitely) { pointer in
        _ = pointer.pointee
}


       var durationM: Int = 3
   withUnsafeMutablePointer(to: &durationM) { pointer in
          _ = pointer.pointee
   }
    var userC: Double = 0.0
      userC -= (Double(Int(userC > 86843584.0 || userC < -86843584.0 ? 92.0 : userC) + 2))
   while (durationM == durationM) {
       var realW: [String: Any]! = [String(cString: [97,117,116,111,99,108,111,115,101,0], encoding: .utf8)!:30, String(cString: [115,105,103,97,108,103,0], encoding: .utf8)!:27]
       var normalizedY: Double = 3.0
      while (Double(realW.keys.count) >= normalizedY) {
         realW["\(normalizedY)"] = (Int(normalizedY > 363764595.0 || normalizedY < -363764595.0 ? 8.0 : normalizedY) >> (Swift.min(realW.count, 3)))
         break
      }
      repeat {
         realW["\(normalizedY)"] = (Int(normalizedY > 61172296.0 || normalizedY < -61172296.0 ? 93.0 : normalizedY) ^ realW.keys.count)
         if realW.count == 1845466 {
            break
         }
      } while (1 <= realW.values.count) && (realW.count == 1845466)
      for _ in 0 ..< 3 {
         realW["\(normalizedY)"] = realW.count & 1
      }
         normalizedY /= Swift.max((Double(realW.keys.count + Int(normalizedY > 21110903.0 || normalizedY < -21110903.0 ? 57.0 : normalizedY))), 5)
      if realW["\(normalizedY)"] == nil {
         normalizedY -= Double(realW.values.count)
      }
      for _ in 0 ..< 1 {
         normalizedY += Double(realW.count - 3)
      }
      durationM %= Swift.max(2, realW.values.count - 1)
      break
   }

        return packages.count
    }

    
    func collectionView(_ collectionView: UICollectionView, layout collectionViewLayout: UICollectionViewLayout, sizeForItemAt indexPath: IndexPath) -> CGSize {
       var wasj: Double = 0.0
   if wasj < wasj {
      wasj -= (Double(Int(wasj > 127143761.0 || wasj < -127143761.0 ? 80.0 : wasj) / (Swift.max(5, Int(wasj > 29197502.0 || wasj < -29197502.0 ? 9.0 : wasj)))))
   }

        return CGSizeMake((UIScreen.main.bounds.width - 54.1)/3, 131)
    }

@discardableResult
 func willWorkBecomeEndpointConfirmGenerator(lyricsLikes: String!) -> String! {
    var chatW: String! = String(cString: [117,97,110,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &chatW) { pointer in
          _ = pointer.pointee
   }
    var inputU: Bool = true
    var keys8: String! = String(cString: [117,112,100,97,116,101,114,0], encoding: .utf8)!
    _ = keys8
    var stopp: [Any]! = [95, 3]
   withUnsafeMutablePointer(to: &stopp) { pointer in
    
   }
      stopp = [(1 >> (Swift.min(2, labs((inputU ? 5 : 2)))))]
   for _ in 0 ..< 1 {
      chatW = "\(stopp.count)"
   }
   for _ in 0 ..< 3 {
      keys8.append("\((keys8 == (String(cString:[99,0], encoding: .utf8)!) ? keys8.count : keys8.count))")
   }
   return chatW

}





    
    func collectionView(_ collectionView: UICollectionView, didSelectItemAt indexPath: IndexPath) {

         var encodingsXptable: String! = willWorkBecomeEndpointConfirmGenerator(lyricsLikes:String(cString: [115,115,105,109,0], encoding: .utf8)!)

      if encodingsXptable == "remove" {
              print(encodingsXptable)
      }
      let encodingsXptable_len = encodingsXptable?.count ?? 0

withUnsafeMutablePointer(to: &encodingsXptable) { pointer in
    
}


       var hasw: [String: Any]! = [String(cString: [112,114,97,112,97,114,101,0], encoding: .utf8)!:42, String(cString: [111,114,105,101,110,116,97,116,105,111,110,0], encoding: .utf8)!:35]
      hasw["\(hasw.keys.count)"] = hasw.values.count ^ 1

        purchase(packages[indexPath.item])
    }

    
    func collectionView(_ collectionView: UICollectionView, cellForItemAt indexPath: IndexPath) -> UICollectionViewCell {
       var main_vO: [Any]! = [String(cString: [102,100,101,99,0], encoding: .utf8)!, String(cString: [100,115,112,114,0], encoding: .utf8)!, String(cString: [106,112,103,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &main_vO) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var request8: [Any]! = [85, 13]
       _ = request8
       var launcha: Double = 3.0
       var contentD: Int = 3
      withUnsafeMutablePointer(to: &contentD) { pointer in
    
      }
       var completionK: String! = String(cString: [102,105,110,100,110,101,97,114,109,118,0], encoding: .utf8)!
         contentD += (request8.count - Int(launcha > 329051954.0 || launcha < -329051954.0 ? 55.0 : launcha))
      repeat {
          var scalarD: String! = String(cString: [115,104,101,101,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &scalarD) { pointer in
                _ = pointer.pointee
         }
          var nameg: String! = String(cString: [115,101,116,116,105,103,110,115,0], encoding: .utf8)!
          var entityc: String! = String(cString: [115,105,103,115,108,111,116,0], encoding: .utf8)!
          _ = entityc
         launcha -= Double(entityc.count / (Swift.max(2, 10)))
         scalarD = "\(nameg.count % 2)"
         nameg = "\((Int(launcha > 55947820.0 || launcha < -55947820.0 ? 55.0 : launcha) % (Swift.max(7, contentD))))"
         if 1817674.0 == launcha {
            break
         }
      } while (launcha < 1.31) && (1817674.0 == launcha)
         launcha += (Double(Int(launcha > 207821277.0 || launcha < -207821277.0 ? 89.0 : launcha) << (Swift.min(2, labs(1)))))
      while (1.45 <= (Double(contentD) - launcha) || (launcha - 1.45) <= 1.57) {
          var buttonsB: Int = 1
          var controllersJ: Double = 5.0
         contentD %= Swift.max(5, 1 << (Swift.min(1, request8.count)))
         buttonsB |= request8.count
         controllersJ /= Swift.max(3, (Double(Int(launcha > 16961128.0 || launcha < -16961128.0 ? 12.0 : launcha) - 2)))
         break
      }
          var recordj: Bool = false
          var infinitem: Int = 0
          var relation_: Int = 4
          _ = relation_
         contentD %= Swift.max(contentD / (Swift.max(3, 8)), 4)
         recordj = 48 <= (contentD * infinitem)
         infinitem |= request8.count
         relation_ -= (Int(launcha > 42889577.0 || launcha < -42889577.0 ? 90.0 : launcha) * (recordj ? 5 : 4))
         request8 = [contentD]
         contentD %= Swift.max(request8.count - 1, 3)
       var tabE: Float = 1.0
         request8.append((Int(launcha > 9262856.0 || launcha < -9262856.0 ? 46.0 : launcha) + contentD))
         contentD ^= (3 % (Swift.max(Int(launcha > 171454928.0 || launcha < -171454928.0 ? 16.0 : launcha), 5)))
          var refreshZ: Double = 5.0
          var commentq: String! = String(cString: [97,100,100,114,101,115,115,0], encoding: .utf8)!
          var screen5: [String: Any]! = [String(cString: [97,114,102,113,0], encoding: .utf8)!:34, String(cString: [109,111,118,101,116,111,0], encoding: .utf8)!:73, String(cString: [111,112,101,110,99,108,0], encoding: .utf8)!:66]
         launcha -= Double(contentD)
         refreshZ += (Double(Int(launcha > 75399689.0 || launcha < -75399689.0 ? 90.0 : launcha)))
         commentq = "\((contentD | Int(launcha > 217974962.0 || launcha < -217974962.0 ? 49.0 : launcha)))"
         screen5[commentq] = request8.count << (Swift.min(labs(3), 2))
         tabE += Float(request8.count)
      main_vO = [request8.count << (Swift.min(labs(3), 5))]
      completionK = "\((Int(launcha > 17744801.0 || launcha < -17744801.0 ? 92.0 : launcha)))"
      if main_vO.count == 2628335 {
         break
      }
   } while (main_vO.count < 2) && (main_vO.count == 2628335)

        let arrayCell = collectionView.dequeueReusableCell(withReuseIdentifier: "rech", for: indexPath) as! OPlayerContainerCell
        arrayCell.backgroundColor = .white
        let political = packages[indexPath.item]
        let dynamic_10 = productsById[political.productId]?.displayPrice ?? political.fallbackPrice
        arrayCell.vook_update(diamonds: political.diamonds, price: dynamic_10)

        return arrayCell
    }
}

private struct ELProfile {
var directoryKeyboard_str: String!
var animate_max: Double? = 0
var click_mark: Int? = 0



    let productId: String
    let diamonds: Int
    let fallbackPrice: String
}
