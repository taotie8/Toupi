
import UIKit

class AVookTextView: UIView {
    private var size_vfEncryptedView: OBMainView!
private var followsImageviewView: XQRecommendView!
private var navDescription_hShow: String!
private var propressUpdate_s0Edit: [Any]!
private var followersArrayChangeScrollView: UIScrollView!
private var punctuationAgeOthers: Double = 0.0

 var mediaHasGender: String!
 var dismissProtocol_tw: Double = 0.0


    override init(frame: CGRect) {
        super.init(frame: frame)
        size_vfEncryptedView = OBMainView()
        followsImageviewView = XQRecommendView()
        self.navDescription_hShow = String(cString: [117,110,102,101,116,99,104,0], encoding: .utf8)!
        propressUpdate_s0Edit = [57, 20]
        self.followersArrayChangeScrollView = UIScrollView(frame:CGRect(x: 242, y: 89, width: 0, height: 0))
        self.punctuationAgeOthers = 9.0

        self.init_custom()
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    private func init_custom() -> Void {
        
    }

    @discardableResult
 func agreementSignCount(clientBottom: [String: Any]!) -> UIButton! {
    var scroll5: Int = 1
    var urlz: Bool = false
    _ = urlz
       var itemf: Double = 1.0
         itemf /= Swift.max((Double(Int(itemf > 94739143.0 || itemf < -94739143.0 ? 78.0 : itemf) % 2)), 3)
         itemf += (Double(3 - Int(itemf > 376799393.0 || itemf < -376799393.0 ? 46.0 : itemf)))
      repeat {
          var supureV: Double = 0.0
          var promptV: Float = 3.0
          var nextE: [String: Any]! = [String(cString: [97,114,109,99,97,112,0], encoding: .utf8)!:61, String(cString: [108,111,103,105,115,116,105,99,0], encoding: .utf8)!:61, String(cString: [97,99,113,117,105,114,101,0], encoding: .utf8)!:93]
          _ = nextE
          var vookg: Double = 4.0
         withUnsafeMutablePointer(to: &vookg) { pointer in
                _ = pointer.pointee
         }
          var popuph: Int = 1
         withUnsafeMutablePointer(to: &popuph) { pointer in
                _ = pointer.pointee
         }
         itemf /= Swift.max(4, (Double(2 & Int(promptV > 124233735.0 || promptV < -124233735.0 ? 3.0 : promptV))))
         supureV -= (Double(1 << (Swift.min(labs(Int(itemf > 119177171.0 || itemf < -119177171.0 ? 45.0 : itemf)), 5))))
         nextE["\(itemf)"] = (Int(itemf > 362310108.0 || itemf < -362310108.0 ? 56.0 : itemf) ^ 2)
         vookg += Double(nextE.keys.count)
         popuph %= Swift.max(3, (Int(supureV > 201690726.0 || supureV < -201690726.0 ? 5.0 : supureV) & 3))
         if itemf == 2327007.0 {
            break
         }
      } while (1.84 <= itemf) && (itemf == 2327007.0)
      scroll5 /= Swift.max(3 ^ scroll5, 3)
   for _ in 0 ..< 1 {
      scroll5 -= (scroll5 >> (Swift.min(4, labs((urlz ? 5 : 3)))))
   }
     let othersClean: UIView! = UIView(frame:CGRect.zero)
     var linesLength: Double = 10.0
    var emoryKps = UIButton(frame:CGRect(x: 71, y: 278, width: 0, height: 0))
    othersClean.alpha = 0.6;
    othersClean.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    othersClean.frame = CGRect(x: 126, y: 7, width: 0, height: 0)
    
    emoryKps.titleLabel?.font = UIFont.systemFont(ofSize:12)
    emoryKps.setImage(UIImage(named:String(cString: [112,114,111,109,112,116,0], encoding: .utf8)!), for: .normal)
    emoryKps.setTitle("", for: .normal)
    emoryKps.setBackgroundImage(UIImage(named:String(cString: [111,112,101,110,0], encoding: .utf8)!), for: .normal)
    emoryKps.alpha = 0.8;
    emoryKps.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    emoryKps.frame = CGRect(x: 223, y: 213, width: 0, height: 0)

    
    return emoryKps

}


@discardableResult
 func modelConsumeStatus(controllerSource: [String: Any]!, migrationReported: String!) -> [String: Any]! {
    var encryptedE: Bool = false
    var permissionm: String! = String(cString: [115,101,116,115,97,114,0], encoding: .utf8)!
    var insetsN: [String: Any]! = [String(cString: [110,101,119,116,101,107,0], encoding: .utf8)!:String(cString: [99,119,110,100,0], encoding: .utf8)!, String(cString: [105,110,116,101,103,101,114,105,102,121,0], encoding: .utf8)!:String(cString: [108,105,110,117,120,0], encoding: .utf8)!]
    _ = insetsN
   if 1 == (3 / (Swift.max(2, permissionm.count))) {
      insetsN[permissionm] = insetsN.keys.count % (Swift.max(permissionm.count, 5))
   }
      insetsN = ["\(encryptedE)": 1 + permissionm.count]
   return insetsN

}


@discardableResult
 func followPlayDismiss(settingsFollow: Double) -> [String: Any]! {
    var curvef: Bool = false
    var valueso: String! = String(cString: [101,110,116,114,111,112,121,109,118,0], encoding: .utf8)!
    var apanceU: [String: Any]! = [String(cString: [112,117,116,99,0], encoding: .utf8)!:50, String(cString: [117,110,101,109,98,101,100,0], encoding: .utf8)!:63, String(cString: [101,108,101,118,97,116,101,100,0], encoding: .utf8)!:42]
   repeat {
       var dataY: String! = String(cString: [101,110,99,105,114,99,108,101,100,0], encoding: .utf8)!
       _ = dataY
       var appearancev: String! = String(cString: [118,99,101,110,99,0], encoding: .utf8)!
       var k_centeri: [Any]! = [75, 19, 47]
          var nickw: String! = String(cString: [99,97,112,105,116,97,108,105,122,101,0], encoding: .utf8)!
          var buttonsL: Bool = true
          var workW: Double = 0.0
          _ = workW
         appearancev.append("\(((String(cString:[116,0], encoding: .utf8)!) == nickw ? k_centeri.count : nickw.count))")
         buttonsL = !buttonsL
         workW /= Swift.max(4, (Double(1 << (Swift.min(5, labs((buttonsL ? 2 : 4)))))))
      if appearancev.count > k_centeri.count {
          var outgoingY: Double = 3.0
          var prefix_wdH: String! = String(cString: [109,109,97,112,0], encoding: .utf8)!
         k_centeri = [((String(cString:[101,0], encoding: .utf8)!) == dataY ? dataY.count : k_centeri.count)]
         outgoingY += Double(prefix_wdH.count / 2)
         prefix_wdH = "\(dataY.count)"
      }
         dataY = "\(k_centeri.count >> (Swift.min(labs(2), 3)))"
      repeat {
          var normalizedV: String! = String(cString: [99,101,110,116,101,114,105,110,103,0], encoding: .utf8)!
          _ = normalizedV
          var success8: String! = String(cString: [114,101,110,116,97,108,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &success8) { pointer in
                _ = pointer.pointee
         }
          var comments4: String! = String(cString: [114,97,100,102,0], encoding: .utf8)!
         k_centeri = [2]
         normalizedV.append("\(comments4.count - 3)")
         success8.append("\(3)")
         comments4.append("\(appearancev.count ^ 2)")
         if 4849735 == k_centeri.count {
            break
         }
      } while (4849735 == k_centeri.count) && ((4 | k_centeri.count) <= 4 || (4 | k_centeri.count) <= 3)
       var dotsa: [String: Any]! = [String(cString: [99,99,105,112,0], encoding: .utf8)!:UILabel(frame:CGRect(x: 296, y: 368, width: 0, height: 0))]
       var update_n5: [String: Any]! = [String(cString: [97,118,114,101,115,97,109,112,108,101,0], encoding: .utf8)!:79, String(cString: [110,101,97,114,101,110,100,0], encoding: .utf8)!:25]
       _ = update_n5
         dotsa[dataY] = (dataY == (String(cString:[79,0], encoding: .utf8)!) ? dataY.count : appearancev.count)
         update_n5 = ["\(dotsa.count)": k_centeri.count]
      curvef = appearancev.count >= 21
      if curvef ? !curvef : curvef {
         break
      }
   } while (curvef ? !curvef : curvef) && (1 <= apanceU.values.count)
      apanceU = [valueso: (valueso == (String(cString:[89,0], encoding: .utf8)!) ? valueso.count : (curvef ? 4 : 1))]
   repeat {
       var prefix_mnT: String! = String(cString: [102,114,101,111,112,101,110,0], encoding: .utf8)!
       var g_player3: [String: Any]! = [String(cString: [119,114,105,116,101,98,105,116,115,0], encoding: .utf8)!:62, String(cString: [100,101,102,105,110,101,115,0], encoding: .utf8)!:71, String(cString: [115,105,98,108,105,110,103,0], encoding: .utf8)!:65]
      withUnsafeMutablePointer(to: &g_player3) { pointer in
    
      }
       var isdeleteW: [Any]! = [53, 11]
       var reusep: [Any]! = [39, 29, 5]
       var stringo: String! = String(cString: [111,118,101,114,105,100,100,101,110,0], encoding: .utf8)!
      while (prefix_mnT.count > 5) {
         isdeleteW = [3]
         break
      }
      while (stringo.count < prefix_mnT.count) {
         prefix_mnT = "\((stringo == (String(cString:[56,0], encoding: .utf8)!) ? isdeleteW.count : stringo.count))"
         break
      }
       var requstg: String! = String(cString: [116,111,111,108,98,97,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &requstg) { pointer in
    
      }
      for _ in 0 ..< 3 {
         isdeleteW.append(prefix_mnT.count)
      }
       var formatterW: Int = 0
       _ = formatterW
          var chatW: Float = 1.0
         withUnsafeMutablePointer(to: &chatW) { pointer in
                _ = pointer.pointee
         }
         requstg = "\(2)"
         chatW += Float(requstg.count + 2)
      if 1 > (reusep.count ^ 2) || (2 ^ reusep.count) > 3 {
         requstg = "\(2 ^ g_player3.count)"
      }
          var delegate_n4: String! = String(cString: [99,111,110,100,101,110,115,101,100,0], encoding: .utf8)!
          var f_count2: String! = String(cString: [99,97,108,108,111,117,116,0], encoding: .utf8)!
          _ = f_count2
         prefix_mnT.append("\(stringo.count | 1)")
         delegate_n4 = "\(3)"
         f_count2.append("\(2)")
       var generatorJ: Int = 1
         g_player3[requstg] = (requstg == (String(cString:[66,0], encoding: .utf8)!) ? stringo.count : requstg.count)
      if reusep.count < 1 {
          var sources: String! = String(cString: [111,110,97,118,99,100,97,116,97,0], encoding: .utf8)!
          var replyJ: Bool = true
         reusep.append(isdeleteW.count - reusep.count)
         sources = "\(3)"
         replyJ = (reusep.contains { $0 as? Bool == replyJ })
      }
         prefix_mnT = "\(reusep.count)"
         requstg = "\(g_player3.values.count + 1)"
      repeat {
          var l_imagen: [String: Any]! = [String(cString: [99,108,105,101,110,116,0], encoding: .utf8)!:86, String(cString: [97,108,98,117,109,0], encoding: .utf8)!:2, String(cString: [112,105,99,107,108,112,102,0], encoding: .utf8)!:79]
          _ = l_imagen
          var childn: Double = 3.0
          _ = childn
         isdeleteW.append(3 ^ prefix_mnT.count)
         l_imagen[requstg] = 1
         childn -= Double(formatterW)
         if isdeleteW.count == 1412338 {
            break
         }
      } while (isdeleteW.count == 1412338) && (5 < (reusep.count >> (Swift.min(labs(2), 5))) || 2 < (2 >> (Swift.min(3, reusep.count))))
         isdeleteW = [stringo.count]
         generatorJ |= formatterW
      curvef = isdeleteW.count >= 66 && stringo == (String(cString:[76,0], encoding: .utf8)!)
      if curvef ? !curvef : curvef {
         break
      }
   } while (apanceU.keys.count > 4 && (apanceU.keys.count + 4) > 1) && (curvef ? !curvef : curvef)
   return apanceU

}


@discardableResult
 func clearData(cleanAgrrmment: Float) -> Int {
    var buttonsv: String! = String(cString: [99,111,108,117,109,115,0], encoding: .utf8)!
    var mnewsP: String! = String(cString: [100,111,119,110,114,105,103,104,116,0], encoding: .utf8)!
    var containerN: Int = 5
   repeat {
       var navigationq: String! = String(cString: [100,111,108,108,97,114,0], encoding: .utf8)!
       var handlem: Double = 0.0
       var shouldX: String! = String(cString: [115,111,97,98,111,114,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &shouldX) { pointer in
             _ = pointer.pointee
      }
       var actionk: Int = 2
         actionk /= Swift.max(1, 5)
         handlem -= Double(3)
         navigationq.append("\(actionk ^ 1)")
      repeat {
          var cacheJ: String! = String(cString: [105,110,112,111,115,0], encoding: .utf8)!
          var arrayR: String! = String(cString: [109,98,114,116,104,114,101,97,100,0], encoding: .utf8)!
          _ = arrayR
          var didw: String! = String(cString: [112,114,105,110,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &didw) { pointer in
                _ = pointer.pointee
         }
          var clickj: Float = 2.0
         withUnsafeMutablePointer(to: &clickj) { pointer in
                _ = pointer.pointee
         }
         shouldX.append("\(navigationq.count / 2)")
         cacheJ = "\((cacheJ.count | Int(handlem > 142971590.0 || handlem < -142971590.0 ? 65.0 : handlem)))"
         arrayR = "\(arrayR.count)"
         didw.append("\(2)")
         clickj -= (Float(Int(clickj > 266915617.0 || clickj < -266915617.0 ? 69.0 : clickj) / 1))
         if shouldX == (String(cString:[54,98,113,54,51,121,51,97,112,0], encoding: .utf8)!) {
            break
         }
      } while (shouldX == (String(cString:[54,98,113,54,51,121,51,97,112,0], encoding: .utf8)!)) && (shouldX.count < 5)
          var pictureZ: String! = String(cString: [112,116,120,99,0], encoding: .utf8)!
          _ = pictureZ
          var size_3g: String! = String(cString: [109,101,109,97,108,105,103,110,0], encoding: .utf8)!
          var presentc: String! = String(cString: [112,114,101,118,111,105,117,115,0], encoding: .utf8)!
         shouldX = "\(shouldX.count - 1)"
         pictureZ.append("\((shouldX == (String(cString:[81,0], encoding: .utf8)!) ? shouldX.count : presentc.count))")
         size_3g.append("\(pictureZ.count - 2)")
         presentc.append("\(actionk / (Swift.max(shouldX.count, 5)))")
      while (2.48 >= (handlem / 5.31) && 2 >= (shouldX.count ^ 1)) {
          var identifierP: String! = String(cString: [116,111,109,98,117,102,0], encoding: .utf8)!
          _ = identifierP
          var like4: Double = 3.0
          var insetse: String! = String(cString: [99,109,97,115,107,0], encoding: .utf8)!
          _ = insetse
          var decryptedI: Float = 2.0
          var type_vb: Double = 2.0
         withUnsafeMutablePointer(to: &type_vb) { pointer in
                _ = pointer.pointee
         }
         shouldX.append("\(shouldX.count)")
         identifierP = "\((Int(type_vb > 113375969.0 || type_vb < -113375969.0 ? 18.0 : type_vb)))"
         like4 -= Double(identifierP.count * 1)
         insetse = "\(navigationq.count + shouldX.count)"
         decryptedI -= Float(actionk * identifierP.count)
         type_vb += Double(actionk << (Swift.min(2, labs(3))))
         break
      }
          var recommend3: Int = 1
         shouldX = "\((Int(handlem > 183372589.0 || handlem < -183372589.0 ? 76.0 : handlem)))"
         recommend3 %= Swift.max(2, (navigationq.count ^ Int(handlem > 167714445.0 || handlem < -167714445.0 ? 61.0 : handlem)))
      if navigationq.count <= shouldX.count {
         navigationq.append("\(shouldX.count | 2)")
      }
         shouldX.append("\((Int(handlem > 54114994.0 || handlem < -54114994.0 ? 43.0 : handlem)))")
      while ((Int(handlem > 122039445.0 || handlem < -122039445.0 ? 88.0 : handlem) / (Swift.max(navigationq.count, 2))) >= 3) {
         handlem -= Double(shouldX.count / (Swift.max(1, 10)))
         break
      }
      while (navigationq.hasPrefix("\(actionk)")) {
          var resulte: String! = String(cString: [110,117,109,112,97,100,0], encoding: .utf8)!
         actionk |= (shouldX.count + Int(handlem > 34255385.0 || handlem < -34255385.0 ? 40.0 : handlem))
         resulte.append("\(navigationq.count)")
         break
      }
         navigationq.append("\((navigationq == (String(cString:[74,0], encoding: .utf8)!) ? navigationq.count : Int(handlem > 181484270.0 || handlem < -181484270.0 ? 37.0 : handlem)))")
      mnewsP = "\(buttonsv.count | mnewsP.count)"
      if (String(cString:[56,48,115,108,53,109,110,107,53,105,0], encoding: .utf8)!) == mnewsP {
         break
      }
   } while (4 < (1 - mnewsP.count)) && ((String(cString:[56,48,115,108,53,109,110,107,53,105,0], encoding: .utf8)!) == mnewsP)
      mnewsP = "\((mnewsP == (String(cString:[48,0], encoding: .utf8)!) ? buttonsv.count : mnewsP.count))"
      buttonsv = "\(containerN)"
   return containerN

}


@discardableResult
 func sessionFollow(keyScreen: String!, currentToupi: String!) -> UIView! {
    var editingy: Bool = true
    var storagei: Bool = false
   withUnsafeMutablePointer(to: &storagei) { pointer in
    
   }
   if editingy && storagei {
       var idsy: [String: Any]! = [String(cString: [110,115,115,101,0], encoding: .utf8)!:44, String(cString: [110,97,118,105,103,97,116,105,111,110,0], encoding: .utf8)!:86]
       var introW: Double = 2.0
       var loading9: [String: Any]! = [String(cString: [101,122,105,101,114,0], encoding: .utf8)!:41.0]
       _ = loading9
         idsy["\(introW)"] = 3
       var creator4: Double = 4.0
       var type_l0w: Double = 4.0
          var type_k_8: Bool = true
          _ = type_k_8
         creator4 -= (Double(1 - Int(creator4 > 73289205.0 || creator4 < -73289205.0 ? 17.0 : creator4)))
         type_k_8 = loading9.count < 57
      repeat {
         introW -= (Double(Int(introW > 233924386.0 || introW < -233924386.0 ? 69.0 : introW) & idsy.values.count))
         if introW == 4287946.0 {
            break
         }
      } while (1.99 >= introW) && (introW == 4287946.0)
       var errorG: String! = String(cString: [112,111,119,0], encoding: .utf8)!
      repeat {
         loading9["\(introW)"] = (Int(introW > 41146544.0 || introW < -41146544.0 ? 84.0 : introW))
         if loading9.count == 149570 {
            break
         }
      } while (errorG.contains("\(loading9.count)")) && (loading9.count == 149570)
       var messagesl: Float = 2.0
       _ = messagesl
      repeat {
         errorG.append("\((Int(type_l0w > 58415008.0 || type_l0w < -58415008.0 ? 82.0 : type_l0w)))")
         if 1955815 == errorG.count {
            break
         }
      } while (1955815 == errorG.count) && (3 < (idsy.keys.count & 2))
         introW /= Swift.max(3, (Double(Int(type_l0w > 164436757.0 || type_l0w < -164436757.0 ? 77.0 : type_l0w) * 1)))
         messagesl -= (Float(idsy.values.count & Int(messagesl > 239509481.0 || messagesl < -239509481.0 ? 44.0 : messagesl)))
      storagei = 6.84 < introW
   }
   if !editingy {
       var znewsz: [Any]! = [93, 16, 1]
       var shared8: [Any]! = [6, 71, 67]
      while (znewsz.contains { $0 as? Int == shared8.count }) {
         shared8.append(shared8.count + znewsz.count)
         break
      }
      if 5 < (znewsz.count ^ shared8.count) {
         shared8.append(znewsz.count)
      }
      for _ in 0 ..< 1 {
         shared8.append(znewsz.count & shared8.count)
      }
      for _ in 0 ..< 2 {
          var containerd: Bool = true
          var horizontal_: [String: Any]! = [String(cString: [119,97,116,99,104,100,111,103,0], encoding: .utf8)!:13, String(cString: [118,116,101,115,116,0], encoding: .utf8)!:20, String(cString: [115,117,109,102,0], encoding: .utf8)!:49]
          var messageS: [String: Any]! = [String(cString: [112,97,107,116,0], encoding: .utf8)!:49, String(cString: [111,116,111,115,0], encoding: .utf8)!:22]
          var clientP: Double = 2.0
         withUnsafeMutablePointer(to: &clientP) { pointer in
                _ = pointer.pointee
         }
         znewsz = [(Int(clientP > 105623514.0 || clientP < -105623514.0 ? 90.0 : clientP) * 3)]
         containerd = 80 <= horizontal_.count || znewsz.count <= 80
         horizontal_ = ["\(shared8.count)": (Int(clientP > 79617125.0 || clientP < -79617125.0 ? 27.0 : clientP) ^ shared8.count)]
         messageS["\(containerd)"] = 3
      }
      if znewsz.contains { $0 as? Int == shared8.count } {
         shared8.append(1)
      }
          var listF: Double = 2.0
         withUnsafeMutablePointer(to: &listF) { pointer in
    
         }
          var task1: String! = String(cString: [111,110,116,97,99,116,0], encoding: .utf8)!
          _ = task1
         znewsz.append(shared8.count - 2)
         listF += Double(3)
         task1.append("\(znewsz.count)")
      storagei = (shared8.contains { $0 as? Bool == editingy })
   }
     var insetError: Double = 46.0
     var keysChange: Bool = false
     var trailingVisible: [Any]! = [50.0]
    var shouldXavs: UIView! = UIView()
    shouldXavs.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    shouldXavs.alpha = 0.7
    shouldXavs.frame = CGRect(x: 119, y: 59, width: 0, height: 0)

    
    return shouldXavs

}


@discardableResult
 func frame_4WillReview(streamingWill: Bool) -> UIScrollView! {
    var insetsK: String! = String(cString: [115,101,116,100,97,114,0], encoding: .utf8)!
    var recordsI: [String: Any]! = [String(cString: [105,110,115,101,114,116,101,114,0], encoding: .utf8)!:[String(cString: [112,101,114,109,97,110,101,110,116,0], encoding: .utf8)!:String(cString: [117,110,115,116,97,107,101,0], encoding: .utf8)!, String(cString: [101,102,102,101,99,116,115,0], encoding: .utf8)!:String(cString: [109,97,114,107,100,111,119,110,0], encoding: .utf8)!]]
      recordsI[insetsK] = insetsK.count | recordsI.count
   repeat {
      recordsI["\(insetsK)"] = insetsK.count
      if 536892 == recordsI.count {
         break
      }
   } while (536892 == recordsI.count) && (1 < recordsI.values.count)
     var durationDecryption: UILabel! = UILabel()
     var signClean: Bool = false
     let lengthScreen: String! = String(cString: [122,101,114,111,118,0], encoding: .utf8)!
    var zbinConstructive:UIScrollView! = UIScrollView()
    zbinConstructive.showsVerticalScrollIndicator = true
    zbinConstructive.showsHorizontalScrollIndicator = true
    zbinConstructive.delegate = nil
    zbinConstructive.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    zbinConstructive.alwaysBounceVertical = false
    zbinConstructive.alwaysBounceHorizontal = true
    zbinConstructive.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    zbinConstructive.alpha = 0.0
    zbinConstructive.frame = CGRect(x: 277, y: 116, width: 0, height: 0)
    durationDecryption.frame = CGRect(x: 258, y: 228, width: 0, height: 0)
    durationDecryption.alpha = 0.3;
    durationDecryption.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    durationDecryption.font = UIFont.systemFont(ofSize:10)
    durationDecryption.text = ""
    durationDecryption.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    durationDecryption.textAlignment = .left
    
    zbinConstructive.addSubview(durationDecryption)

    
    return zbinConstructive

}




}